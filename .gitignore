# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# database backups (scripts/backup-database.mjs) — full row dumps of every
# client's contracts, invoices, and contact data. Never commit these.
/backups/

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code
.claude/
.gstack/

# Graviss SEO data layer — service-account/OAuth credentials, never committed.
# clients.json is intentionally NOT ignored — property IDs are identifiers,
# not secrets, and the registry is meant to be committed.
*.json.key
*-credentials.json
analytics-key.json
service-account*.json
__pycache__/
*.pyc

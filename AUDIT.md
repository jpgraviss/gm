# GravHub Audit Log

Tracks holes found by `/audit` runs: incomplete features, fake/stub data presented as real,
security gaps, and silent failures. This file is meant to be edited directly — change `Status`,
add to `Notes`, or add new rows by hand at any time. Re-running `/audit` adds newly-found items
and never overwrites your edits to existing ones (matched by the finding's title).

**Status values:** `Open` · `Fixed` · `In Progress` · `Won't Fix` · `Needs Decision`

---

## Critical / High — Security & Data Integrity

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 1 | BCC email webhook accepted unsigned payloads when `RESEND_WEBHOOK_SECRET` unset — could inject fake CRM activities | `app/api/email/inbound/route.ts` | Fixed | Now fails closed in production, matching `SESSION_SIGNING_KEY` pattern |
| 2 | `admin/seed-clients` and `admin/data-audit` POST had zero auth — could overwrite real client/contract data | `app/api/admin/seed-clients/route.ts`, `app/api/admin/data-audit/route.ts` | Fixed | Added `requireAdmin` to both |
| 3 | Service worker cached authenticated `/api/*` responses in a shared, unscoped, un-purged cache — risk of leaking one user's data to the next on a shared device or after login/logout | `public/sw.js` | Fixed | Removed API response caching entirely; `Cache-Control: no-store` doesn't stop manual `cache.put()` |
| 4 | Company/Contact detail panels didn't reset local state when switching records without closing the panel — editing notes on record A while B's data was stale in state could overwrite B's real notes with A's | `app/crm/companies/page.tsx`, `app/crm/contacts/page.tsx` | Fixed | Added `key={record.id}` to force remount on selection change |
| 5 | `handleAddNote`/`handleAddTask` read stale closure state — two rapid additions before a re-render could silently drop one | `app/crm/contacts/page.tsx` | Fixed | Added `useRef` mirrors so persistence always reads the true latest array |
| 6 | WordPress plugin API-key retry loop aborted entirely on the first key's network error instead of trying the next key | `app/api/wordpress/seo/sync/route.ts` | Fixed | Per-candidate try/catch |
| 7 | Service worker precached `/favicon.ico`, which 404s — `cache.addAll()` fails atomically, silently breaking SW install/activate entirely | `public/sw.js` | Fixed | Found while verifying PWA Stage A; removed dead entry, also fixed same dead path in push-notification icons |
| 8 | Delivery Dashboard crashes (`TypeError`) as soon as any real workflow exists — API returns raw DB row shape, UI expects a transformed `{steps: [...]}` shape | `app/crm/delivery-dashboard/page.tsx`, `app/api/delivery/workflow/route.ts` | Fixed | Added a server-side `mapWorkflow()` transform on both GET and POST; also added a defensive empty-array guard in `isWorkflowCompleted` |
| 9 | GravIntel `visit_count` self-resets to 1 on nearly every event because the upsert unconditionally writes `visit_count: 1` regardless of event type | `app/api/intelligence/track/route.ts` | Fixed | Split into an insert-only upsert (`ignoreDuplicates: true`) for first-visit defaults, plus a separate update for mutable per-visit fields that never touches `visit_count` |
| 10 | GravIntel lead scoring (`gi_score_visitor` SQL function) is never called anywhere — `lead_score`/`is_hot_lead` permanently sit at defaults while the UI renders a live-looking score bar and 🔥 flame | `app/api/intelligence/track/route.ts`, `app/intelligence/page.tsx` | Fixed | Now called after every tracked event so score/hot-lead flag stay current |
| 11 | Email-to-ticket conversion queries `app_settings.gmail_access_token`, a column that doesn't exist — Gmail tokens are stored per-user on `team_members`. Fails 100% of the time, silently, every cron tick | `app/api/tickets/from-email/route.ts` | Fixed | Now checks every team member's connected, non-expired Gmail account instead of a nonexistent global token |
| 12 | Automation builder never sends per-action config (email subject, tag name, task assignee, etc.) to the backend — `automations.actions` is just `text[]`, no config column exists. Actions execute with no real parameters and mostly no-op | `app/automation/builder/page.tsx`, `lib/automations-engine.ts` | Needs Decision | Requires a schema change (`config jsonb` column) + engine rewrite to read from it instead of trigger context — sizable, not attempted this pass |
| 13 | "Wait / Delay" automation action doesn't pause anything — falls through immediately, and its resume mechanism (`automation_pending_steps`) is fed blank `automation_id`/`run_id` that nothing ever populates, so cron resume always fails | `lib/automations-engine.ts` | Needs Decision | Needs a real scheduling design, not a quick patch |
| 14 | 4 action types and 6 trigger types were selectable in the automation UI (including in 4 of 8 built-in templates) but had no matching implementation — automations built with them silently no-op'd forever | `app/automation/page.tsx`, `lib/automations-engine.ts` | Fixed | Removed the dead options and the 4 templates that relied on dead triggers, matching UI to what `lib/automations-engine.ts` actually implements. The bigger issue in #12/#13 (builder config not persisting) remains open |
| 15 | `app/api/portal-clients/[id]/route.ts` PATCH/DELETE has no per-resource authorization — any authenticated caller can modify or delete any portal client by ID | `app/api/portal-clients/[id]/route.ts` | Fixed | PATCH now requires authentication; non-staff callers may only update their own record and only `lastLogin`/`access` (the one legitimate self-service call site). DELETE now requires `requireRole(req, 'Team Member')` |

## Medium

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 16 | Uptime monitoring UI promises checks "every 5 minutes" / configurable interval, but the cron that drives it only runs once a day (`vercel.json`) — a site going down and back up between runs is never detected | `app/monitoring/page.tsx`, `vercel.json` | Open | Needs either a paid Vercel cron tier or an external scheduler trigger — infra/cost decision |
| 17 | WordPress credential fields (`wpUsername`/`wpAppPassword`) are fully implemented server-side for plugin/theme scanning, but there's no UI input to actually enter them | `app/monitoring/page.tsx` | Fixed | Added a WordPress Credentials form (username + Application Password, masked input) to the site detail panel's WordPress tab, wired to the existing PATCH endpoint; `GET .../wordpress` now also reports `hasCredentials` |
| 18 | Funnel conversion tracking always credits the funnel's first page regardless of which step the form was actually on — skews per-step conversion/drop-off analytics for multi-step funnels | `app/api/forms/public/funnel-submit/route.ts` | Fixed | The public funnel page now passes the actual `pageId` (from its `?step=` route) through to the submit endpoint, which credits that page's conversion counter — falls back to the first page only if `pageId` is missing or invalid |
| 19 | Admin "System Health" panel hardcodes Authentication/Database rows to always show "Operational"/"Healthy" — never actually checked | `app/admin/page.tsx` | Fixed | New `database`/`auth` fields on `GET /api/admin/integration-health`: database does a real round-trip query, auth checks `SESSION_SIGNING_KEY` is configured. Rows and the "ALL SYSTEMS GO"/"DEGRADED" banner now reflect real state |
| 20 | Admin "Permissions" role×module matrix is fully static/decorative — no fetch, no save, no `onChange`; doesn't reflect the real (much simpler) RBAC hierarchy in `lib/rbac.ts` | `app/admin/page.tsx` | Fixed | Relabeled as "Reference only" with copy explaining the real hierarchy, since building true per-module enforcement is a much larger feature than a copy fix |
| 21 | Renewals page "Renewal Sequence" panel describes automatic 60/30/14/7-day notifications to rep + client; only 90/30-day triggers actually exist, and even those require a manually-built Active automation to send anything | `app/renewals/page.tsx` | Fixed | Copy now describes the real 90/30-day triggers; page fetches `/api/automations` and shows an explicit warning + link to Automation Builder when no Active automation is actually listening for either trigger |
| 22 | Settings' "Delivery Steps" claims to be a configurable list (add/remove/reorder), but the backend hard-locks to exactly 8 fixed-semantic steps tied to specific DB columns | `app/settings/page.tsx`, `app/api/delivery/workflow/[id]/step/route.ts` | Fixed | Real per-tenant configurability would need a schema change (`delivery_workflows` has one dedicated column per step); instead made the list honest — Settings now shows the 8 fixed steps read-only (new `lib/services`-style single source `lib/delivery-steps.ts`), removing the add/remove/reorder controls that never actually persisted anywhere real. Also fixed a related bug found during this pass: the Delivery Dashboard's "new workflow" create flow inserted an optimistic row with a fake local id that was never reconciled with the server's real record, so a page reload silently diverged from what was created |
| 23 | No Gmail refresh-token usage anywhere despite `gmail_refresh_token` being stored — tokens silently stop working ~1hr after connecting until the user manually reconnects (affects sequence reply-detection and inbox) | `app/api/sequences/reply-check/route.ts`, `contexts/AuthContext.tsx` | Fixed (partial) | Root cause: Gmail connect uses Google Identity Services' browser token client, which by design never issues a refresh token — only a server-side authorization-code OAuth flow does, which is a much larger change (new redirect/callback route, consent screen config) not attempted this pass. `reply-check`/`tickets/from-email` already skipped expired tokens gracefully (no crash); the actual gap was that Settings silently showed "Not Connected" instead of telling the user to reconnect. Settings now shows a distinct "Reconnect needed" state with a one-click reconnect button when a Gmail account is known but its token has expired |
| 24 | A/B testing for email sequences is dead scaffolding — types, analytics computation, and enrollment fields exist, but no editor UI to configure a variant and no execution code that ever assigns/sends one | `app/crm/sequences/page.tsx`, `app/api/sequences/analytics/route.ts` | Fixed | Built the previously-missing step editor (`components/crm/SequenceStepEditor.tsx`) with an A/B test toggle/split/Variant B fields; `execute/route.ts`'s `resolveAbVariant()` assigns and persists a variant once per enrollment; webhook/reply-check activity inserts stamp the variant; the sequence detail page's new Performance tab renders real per-step and A/B results (with an honest "need ≥5 sends per variant" fallback instead of a fabricated winner) |
| 25 | `proxy.ts`'s outer auth gate fails open (`NextResponse.next()`) on any unhandled exception, and `crm/companies/[id]` and `crm/contacts/[id]` PUT/PATCH have no independent auth check of their own — if the proxy ever throws, those become briefly unauthenticated | `proxy.ts`, `app/api/crm/companies/[id]/route.ts`, `app/api/crm/contacts/[id]/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` directly to both routes' PUT/PATCH as defense-in-depth (DELETE already had role checks) |
| 26 | Logout doesn't await/verify the server-side cookie-clear call — if it fails (network blip, tab closed), the UI shows "logged out" but the signed cookie remains valid for up to 7 days with no revocation mechanism | `contexts/AuthContext.tsx` | Fixed | `clearAuthCookie()` now awaits the `DELETE /api/auth/session` call with up to 3 retries and logs an error if all attempts fail; `logout()` awaits it before resolving |
| 27 | Google Sign-In audience check silently no-ops if both `GOOGLE_CLIENT_ID`/`NEXT_PUBLIC_GOOGLE_CLIENT_ID` are unset — would accept a validly-signed token from any Google OAuth client, not just this app | `app/api/auth/google-verify/route.ts` | Fixed | Now rejects sign-in with a 500 if neither env var is configured, instead of silently skipping the audience check |
| 35 | Sequence email engagement (opened/clicked/bounced) and step-task activities were mirrored to `crm_activities` with `company_id` hardcoded to `null` (or never set) — always attributed to the contact, never surfaced on the company record, since `CompanyPanel`'s Activity tab filters strictly on `company_id`. The engagement-event insert also used column names (`description`/`logged_by`) that don't exist on `crm_activities`, so it was silently failing on every event | `app/api/sequences/webhooks/route.ts`, `app/api/sequences/execute/route.ts` | Fixed | Both inserts now resolve `company_id` from the enrollment's `contact_id` via `crm_contacts.company_id` (the enrollment's own `company` field is a denormalized name, not an FK); the webhook insert also switched to the real schema (`title`/`user_name` instead of `description`/`logged_by`) |
| 36 | `app/api/sequences/[id]` PATCH/DELETE and `app/api/automations` POST/PATCH/DELETE had no auth check at all — any caller could rewrite or delete any sequence/automation. Higher blast radius than a typical missing-auth gap since this session added `Rotate Contact Owner` (reassigns a real CRM contact's owner) and dynamic-sender config to what flows through these routes | `app/api/sequences/[id]/route.ts`, `app/api/automations/route.ts`, `app/api/automations/[id]/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` to all five mutation handlers, matching the pattern already used for #15/#25 |
| 37 | Sequences' "Automatic unenrollment" UI claims reply-based unenrollment is "Always on, can't be turned off," but reply detection only works when the enrollment's assigned rep has a connected Gmail account — `reply-check`'s cron entirely skips reps without a Gmail token and treats every `message_ids[0]` as a Gmail thread id regardless of channel. Sequences configured to send via Resend (a first-class Settings option), or enrollments assigned to a rep without Gmail connected, silently never detect a reply — the contact keeps receiving scripted emails after replying | `app/api/sequences/reply-check/route.ts`, `components/crm/SequenceAutomateTab.tsx` | Fixed (partial) | UI now discloses the Gmail-only limitation inline instead of claiming unconditional coverage. The underlying capability gap is real and unfixed — detecting replies to Resend-sent mail would need new infra (IMAP polling or an inbound-email-parsing provider), not attempted this pass |
| 38 | The sequences list page's "Default Unenrollment Triggers" panel showed 4 toggle switches (reply/meeting-booked/bounce/company-level-smart-unenroll) with no `onClick` handler at all — purely decorative — and 2 of the 4 (`meeting_booked` trigger, company-level unenroll) correspond to zero real implementation anywhere in the codebase, not even partially | `app/crm/sequences/page.tsx` | Fixed | Removed the two fully-fake rows and the non-functional toggle-switch affordance for the two real ones (reply/bounce, which aren't actually per-sequence togglable — they're blanket cron behavior); added an explicit "not yet available" note for meeting-booked and company-level unenroll |
| 39 | `PATCH /api/deals/[id]` unconditionally reset `last_activity` to today on every field edit — a probability tweak, a close-date typo fix, or a contact/company relink all made a deal with zero real customer contact look "active today," corrupting Deal Score's engagement factor and silently hiding stale deals from the Prospecting Workspace's guided actions | `app/api/deals/[id]/route.ts` | Fixed | `last_activity` now only bumps on an actual stage change — the one field on this route that represents a real pipeline event |
| 40 | `getGuidedActions`' "closing this week" bucket excluded any deal closing today — `close_date` (a date-only string) parses to UTC midnight, which is always earlier than `now()` for the rest of the day, so `>= now` was false for the single most urgent case | `lib/guided-actions.ts` | Fixed | Compares against today's UTC midnight instead of the current instant |
| 41 | This session's services-catalog consolidation (`lib/services.ts`) repointed 5 forms' service dropdown options to the new canonical list but left their `useState` default values pointing at the old literal `'Website'`, which is no longer a valid option — a user who opens the form and saves without touching the dropdown silently persists a stale, non-canonical value | `app/maintenance/page.tsx`, `app/renewals/page.tsx`, `components/admin/NewClientModal.tsx`, `app/admin/new-client/page.tsx`, `app/portal/page.tsx` | Fixed | Updated defaults to the first real entry in each file's own options list (`'Website Build'` for the two files using the canonical `SERVICE_NAMES`; each local `SERVICES[0]` for the two client-intake forms that use their own separate onboarding-interest taxonomy; `SERVICE_NAMES` directly for the portal client form, which previously had a fully hardcoded, unrelated `<option>` list) |
| 42 | Settings' multi-pipeline editor (`ManagePipelinesPanel`) supported adding a pipeline and adding/renaming/reordering its stages, but had no way to rename or delete a whole pipeline — and even if it had, the save handler POSTed each pipeline individually via the upsert-by-id branch of `/api/pipelines`, which can add or update a pipeline but can never remove one, so a delete control would have silently failed to persist | `app/crm/pipeline/page.tsx` | Fixed | Added rename (inline edit) and delete (guarded — can't delete the last pipeline, warns and requires confirmation if the pipeline has deals in it) controls; save handler now POSTs the full array once via `/api/pipelines`' array-replace branch, which correctly persists removals; `activePipelineId` resets to a valid pipeline if the active one is deleted |

## Medium — Newly flagged, not fixed this pass

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 43 | "Rotate Contact Owner" round-robin does a read-then-write on `rotation_state` with no locking — two automations firing close together (a funnel traffic burst, a retried webhook) can both read the same `last_index`, both compute the same next rep, and both assign the *same* rep to two different contacts, silently desyncing the rotation counter | `lib/automations-engine.ts` | Fixed | Added `next_rotation_index()` — a Postgres function that does the increment inside a single atomic UPSERT (the row lock the UPSERT holds makes the read-modify-write safe under concurrency). App code now calls it via `.rpc()` instead of select-then-upsert. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 44 | Sequence enrollment does a check-then-insert (dedupe query, then a separate insert) with no DB-level unique constraint — two near-simultaneous enroll calls for the same contact (a double-click, or a form automation firing alongside a manual bulk enroll) can both pass the dedupe check and create two active enrollments, breaking the "one sequence at a time" guarantee | `app/api/sequences/[id]/enroll/route.ts`, `lib/automations-engine.ts` | Fixed | Added a partial unique index — at most one `status='active'` row per `contact_email` — as the real DB-level invariant. Both insert call sites now insert per-contact (not batched) and catch a `23505` conflict as "already active elsewhere, skip" instead of erroring. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 45 | `training_progress`'s PATCH does a read-then-full-overwrite upsert of `checklist_state` — toggling two checklist items on the same content in quick succession can lose one (both requests read the same pre-update state, the second write clobbers the first) | `app/api/training/progress/route.ts` | Fixed | Added `upsert_training_progress()` — a Postgres function that does the JSONB `||` merge inside a single atomic UPSERT. Route now calls it via `.rpc()` instead of select-then-upsert. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 46 | Public, unauthenticated `funnel-submit` endpoint matches a submitted email against existing `crm_contacts` and fires `form_submitted` using that contact's real id — combined with this session's new `Enroll in Sequence`/`Rotate Contact Owner` automation actions, anyone who knows a funnel slug and an existing contact's email can trigger real CRM side effects (sequence enrollment, owner reassignment) for that contact with no authentication, if an admin has an "any form" automation configured | `app/api/forms/public/funnel-submit/route.ts` | Needs Decision | Pre-existing "trust the submitted email" pattern, but this session's new automations meaningfully raise its blast radius — flagging for a deliberate accept/reject decision rather than changing unilaterally |
| 47 | `training_progress` and `rotation_state`'s RLS policies allow any authenticated user to read every row (`using (true)` for `authenticated`) via a direct Supabase REST call, wider than the API layer's own per-user scoping. Same `using (true)` pattern is used on ~24 other tables in this codebase — systemic, not a one-off | `supabase/migrations/add_training_progress.sql`, `supabase/migrations/add_automation_sequence_config.sql` | Needs Decision | Not currently exploitable through the app UI (no code path uses the anon-key/user-JWT client to read these tables directly), but a logged-in user holding their own Supabase token could bypass the API's restriction. Security-sensitive, systemic — flagged for a decision rather than changed across 24+ tables unilaterally |
| 48 | Pipeline board (Kanban) and List view fetch deals via `GET /api/deals`, which paginates at 100 rows server-side (`X-Next-Cursor` header) — neither the pipeline page nor the Prospecting Workspace reads that header or requests more, so once total deals exceed 100, board totals, weighted value, and workspace stats silently omit older deals with no indication anything is missing | `app/crm/pipeline/page.tsx`, `app/workspace/page.tsx` | Fixed | Added `lib/fetch-all-pages.ts`, a shared helper that follows `X-Next-Cursor` until exhausted; both pages' deal fetches now use it |
| 49 | `DealListView` (pipeline's List view) has no selection checkboxes, unlike `DealCard` in Kanban — selection state itself survives switching views, but bulk select/export/delete can only be *initiated* from Kanban; switching to List view leaves a rep unable to select anything for bulk action | `app/crm/pipeline/page.tsx` | Fixed | Added a checkbox column wired to the same `selectedIds`/`toggleSelect` state Kanban uses |

## Low

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 28 | "Add Deliverable" button in Delivery Dashboard timeline is a no-op toast despite a working API existing | `app/crm/delivery-dashboard/page.tsx:387` | Fixed | Button now opens a real modal (name/type/file URL/description) that POSTs to the existing `/api/delivery/add-deliverable` |
| 29 | Monthly/client report "Recommendations" section always hard-coded to `[]` | `app/api/delivery/monthly-report/[id]/route.ts`, `lib/seo-report-sender.ts` | Fixed | Added `buildReportRecommendations()` to `lib/client-reports.ts` — rule-based thresholds against the real metrics already computed for the report (traffic/SEO trend, keyword ranking declines, review activity, uptime incidents), shared across all three call sites so none of them fabricate anything not backed by the data on the report |
| 30 | Sales-enablement training completion/checklist/quiz progress lives only in `localStorage` — no team-wide visibility, lost on storage clear or device switch | `app/sales-enablement/page.tsx` | Fixed | Added `training_progress` table + `GET`/`PATCH /api/training/progress`; page now persists through the API, and Leadership/Super Admin/admins can view another user's progress via `?userEmail=`. Follow-up: `toggleCompletion`/`toggleChecklistItem`'s optimistic update didn't revert on a failed PATCH, silently showing a checked box that was never actually saved — now reverts with an error toast, matching #31's pattern |
| 31 | Time-entry edit (PATCH) failures are silently swallowed client-side — optimistic UI shows success even if the server rejected it | `app/time-tracking/page.tsx` | Fixed | `handleSave`/`handleDelete` now check `res.ok` and revert the optimistic update with an error toast if the server rejected it, instead of only catching network-level failures |
| 32 | CSRF check is skipped for the entire `/api/wordpress/seo/` prefix (needed for the plugin's own unauthenticated calls), including the staff-only company-assignment PATCH | `proxy.ts` | Open | Security-sensitive carve-out — flagged for a decision rather than changed without review |
| 33 | GravIntel visitor geolocation/ISP fields (`city`, `region`, `country`, `rdns_company`, `isp`) are rendered in the UI but never populated — no IP lookup exists | `app/api/intelligence/track/route.ts`, `app/intelligence/page.tsx` | Open | Needs a paid IP geolocation service — flagged for a decision |
| 34 | `withErrorHandler` returns raw `err.message` to the client on any thrown error, including internal config errors — minor information disclosure about deployment misconfig | `lib/api-handler.ts` | Fixed | Client now gets a generic "Internal server error" message when `NODE_ENV === 'production'`; full detail still goes to console + Sentry. Dev/test behavior unchanged |
| 50 | `ProposalBuilderPanel`'s `getServiceType()` never considered the "Website Management" monthly checkbox — a proposal consisting only of Website Management got tagged `serviceType: 'Custom'` instead of the accurate value. Pre-existing gap, not introduced by this session's services-catalog work | `components/crm/ProposalBuilderPanel.tsx` | Fixed | Added `websiteMgmt` to the selected-services list `getServiceType()` checks |
| 51 | Sequence step delay is labeled "N business day(s) after previous step" in the step editor, but the executor schedules it as raw calendar days (no weekend-skipping in the day-offset math) — a "wait 2 business days" step actually waits 2 calendar days | `components/crm/SequenceStepEditor.tsx`, `app/api/sequences/execute/route.ts` | Fixed | Relabeled to "N day(s) after previous step" — accurate to what's actually scheduled. Real business-day arithmetic would be a larger follow-up if it's wanted |
| 52 | `{{sender_name}}`/`{{sender_email}}` merge-field tokens render blank with no warning when neither the enrollment nor the sequence has an `assigned_rep_id` resolvable to a real team member, even though the step editor's token bar advertises them unconditionally | `app/api/sequences/execute/route.ts`, `components/crm/SequenceStepEditor.tsx` | Open | |
| 53 | "Closed" stage detection is inconsistent between screens: the Prospecting Workspace uses an exact match (`stage !== 'Closed Won' && stage !== 'Closed Lost'`) while the Pipeline page uses `!stage.startsWith('Closed')` — a custom stage name like "Closed — Duplicate" (pipelines now support custom stages, see #42) would count as closed on one screen and open on the other, producing mismatched pipeline totals | `app/workspace/page.tsx`, `app/crm/pipeline/page.tsx` | Fixed | Workspace now uses the same `startsWith('Closed')` check as the Pipeline page for its "open deals" bucket |

---

## Confirmed working (no action needed)

Verified real and functioning during the 2026-07 audit pass — listed so future runs don't re-flag them:
Google Calendar OAuth/sync/availability/booking emails, public booking widget, delivery-dashboard backend CRUD, sales-enablement playbooks/templates, all six `/reports` pages' underlying math, renewals/maintenance CRUD, Gmail inbox integration, unified inbox, uptime-check engine logic itself (only the trigger cadence is the problem), WordPress security/plugin scanning logic itself, email marketing/broadcasts (full audience filtering, A/B subject tests, real sends), funnel builder + public serving (except conversion attribution), Maverick visitor intelligence, GravIntel event capture pipeline itself (except visit_count/scoring), operations/admin dashboard KPIs, data-audit computations, core automation dispatch + the ~25 action types that ARE reachable from real triggers, the full email-sequence send pipeline (throttling, windows, merge fields, dual-channel send, webhook engagement tracking), ticket CRUD + routing rules, time-entry CRUD + approval workflow, knowledge-base CRUD + portal-scoped visibility.

Plus everything from the prior (2026-07, earlier) pass: social publishing, Resend/email, Mercury, Maverick, HubSpot import, e-signing, global search, the WordPress plugin as an installable artifact, CRM core, forms, proposals, rank tracker, billing/invoices core, portal, reputation management.

Plus, verified during the follow-up pass auditing this session's sequencing/workspace/services-catalog work: A/B variant assignment/reuse and A/B analytics/winner computation, sequence enrollment dedupe against already-enrolled/suppressed/active-elsewhere contacts (logic itself correct, modulo the race in #44), the funnel→contact→sequence bridge's field-type-based email/name resolution, Gmail token expiry buffering and Gmail→Resend send fallback, `rotation_state`'s durable persistence (just not atomic, see #43), Resend webhook signature verification (Svix + HMAC fallback) and bounce/complaint→suppression wiring, `rep-activity`'s RBAC scoping, `computeDealScore`'s math and its consistency between the deals list and detail routes, `DealListView`'s sort logic and asc/desc toggling, selection state and the open deal panel surviving Kanban↔List view switches, the training-progress upsert's `onConflict` key matching a real unique index, and the delivery-dashboard's create-workflow optimistic-then-reconcile logic against the real `mapWorkflow()` response shape.

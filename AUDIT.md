# GravHub Audit Log

Tracks holes found by `/audit` runs: incomplete features, fake/stub data presented as real,
security gaps, and silent failures. This file is meant to be edited directly — change `Status`,
add to `Notes`, or add new rows by hand at any time. Re-running `/audit` adds newly-found items
and never overwrites your edits to existing ones (matched by the finding's title).

**Status values:** `Open` · `Fixed` · `In Progress` · `Won't Fix` · `Needs Decision`

---

## Critical / High — Security & Data Integrity

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 82 | CRITICAL — 9 API routes had ZERO authentication and were direct paths to full account takeover: `app/api/team-members/route.ts` (GET/POST/PATCH) let anyone plant a `team_members` row with `role: 'Super Admin', isAdmin: true` for an arbitrary Gmail address, then sign in as that identity via the app's own Google Sign-In (`google-verify` finds the planted row and mints a real session — no credentials needed); `app/api/portal-clients/route.ts` (GET/POST/DELETE) leaked every client's roster and let anyone create a portal account (with `tempPassword` returned in the body) or mass-delete a company's portal users; `app/api/portal-clients/invite/route.ts` let anyone self-register a portal identity under any real client's company name and receive a working magic-link invite at an attacker email; `app/api/portal-clients/company-config/route.ts` let anyone rewrite any company's portal module visibility; `app/api/portal-clients/notifications/route.ts` had no ownership check on read/write; `app/api/portal-clients/complete-setup/route.ts` never actually validated the verification code it claimed to require, so anyone who knew/guessed a client's email could set that account's password directly; `app/api/audit-logs/route.ts` (GET/POST) exposed and let anyone forge the internal audit trail — including entries about the exploits above; `app/api/email/scheduled/route.ts` (GET/POST/DELETE) was an open phishing/spam relay through the org's verified sending domain; `app/api/portal/preview/route.ts` leaked any company's full contracts/projects/invoices/tickets to a fully unauthenticated caller | see above | Fixed | Found by an 11-agent full-app audit sweep. Added `requireAdmin`/`requireRole` to every route; `complete-setup` now requires and validates the real `verification_code` (previously collected by the UI's earlier step but never sent to or checked by this route) and clears it on use |
| 83 | HIGH — systemic pattern across the entire app: dozens of GET (read) and several POST/PATCH (write) routes had no auth check at all while their sibling routes on the same resource did, discovered independently by 8 of the 11 audit agents in modules that don't otherwise overlap. Full list by module — **CRM core**: `deals` GET/POST, `deals/[id]` PATCH, `pipelines` GET/POST (could silently rewrite the org's entire shared pipeline config), `crm/companies` POST, `crm/contacts` GET/POST, `crm/companies/[id]/files` GET/POST/DELETE (also had no file size/type validation), `crm/activities` GET, `crm/duplicates` + `/ignore`, `crm/enrich` (also a live SSRF vector — server-side fetch of a caller-supplied URL with no private-IP/localhost/cloud-metadata blocklist). **Proposals/contracts/finance**: `proposals` GET/POST, `contracts` GET/POST, `renewals` GET/POST + `[id]` PATCH, `maintenance` GET/POST + `[id]` PATCH/DELETE, `invoices` GET/POST + `[id]` PATCH, `mercury/accounts`+`/transactions` (live bank balances/transactions), `contracts/[id]/addendums` GET/POST/PATCH, `signatures` GET/POST (a real contract-forgery vector — anyone could generate a signing link for any contract to any email). **Sequences**: `sequences` GET/POST, `[id]/enroll`, `[id]/enrollments` GET/DELETE, `[id]/test-send` (real email sends), `activities`, `analytics`, `attribution`. **Social/tickets/tasks**: `social-posts` GET/POST, `[id]` GET/PATCH (no company scoping — a portal client could read or modify another company's draft posts), `[id]/publish` (no auth AND no server-side approval-status check, so a draft or rejected post could be pushed live), `tickets` GET/POST (portal-client company scoping was also entirely absent — any authenticated caller could read every company's tickets including internal-only notes), `tickets/[id]` PATCH (no ownership/field scoping), `tasks/[id]` PATCH/DELETE, `gmail/send` (any authenticated caller could send real email from any team member's connected Gmail by naming a different `userEmail` in the body — no verification the caller IS that user). **Rank tracker/monitoring**: `rank-tracker/export` (CSV of every company's keywords), `rank-tracker/keywords` GET + `[id]` GET + `[id]/history`, `rank-tracker/competitors` GET, `rank-tracker/reports` GET (leaked scheduled-report recipient emails), `tracked-keywords` GET (portal-facing — previously 401'd for every real portal-client request and was silently swallowed client-side, meaning the portal's live keyword-ranking table never showed real data at all) + `[id]` GET + `[id]/history`, `monitored-sites` GET + `[id]` GET + `[id]/check` POST + `[id]/wordpress` GET/POST (leaked WP credentials-configured status and full plugin/theme/security scan results, and let anyone trigger a live scan against any client's site) | see above | Fixed | Where a route is legitimately portal-client-facing (`proposals`, `contracts`, `invoices`, `social-posts`, `tickets`, `tracked-keywords`), added company-scoped access via `requirePortalClient` instead of a blanket staff-only gate — this also fixed the `tickets` POST/PATCH and `tracked-keywords` GET routes, which were unconditionally staff-only despite being called directly by real portal UI (ticket creation/reply, live keyword rankings), meaning those specific portal features have been silently broken (401, swallowed client-side) since they were built, independent of the security gap. `social-posts/[id]` PATCH now whitelists the exact fields the real portal approval UI sends (`approvalStatus`, `rejectionReason`), matching the pattern already established for contracts/proposals (#77). `publishSocialPost()` now refuses to publish anything with `approval_status !== 'approved'`, closing the gap where the "approval workflow" the schema comment describes was enforced only by the staff UI conditionally showing a button, not the server |
| 84 | MEDIUM — OAuth callback routes generate a CSRF `state` parameter but never validate it on return: `google-marketing/callback`, `linkedin/callback`, `meta/callback`, `drive/callback`. Since these integrations are workspace-wide singletons (one connection per product, not per-user), an attacker can complete their own OAuth consent, capture the resulting `code`/`state` redirect URL, and get a logged-in staff member to open it — silently swapping the org's Google Marketing/LinkedIn/Meta/Drive connection to the attacker's account | `app/api/integrations/google-marketing/callback/route.ts`, `app/api/integrations/linkedin/callback/route.ts`, `app/api/integrations/meta/callback/route.ts`, `app/api/drive/callback/route.ts` | Fixed | Added `lib/oauth-state.ts` — each `connect`/`auth-url` route now generates a random per-flow `state`, stamps it into a short-lived (10 min) httpOnly cookie, and passes it to the provider. Each `callback` route reads the same cookie back and rejects (redirect to an error state, cookie cleared) unless it matches the `state` query param exactly — binding the callback to the same browser that started the flow, closing the cross-account-swap path. Applied uniformly to all 4 integrations |
| 85 | LOW — `linkedin/disconnect` has no role check, unlike its siblings `google-marketing/disconnect`/`meta/disconnect` which both require `requireRole(req, 'Leadership')` — any authenticated staff member can disconnect the org's LinkedIn connection | `app/api/integrations/linkedin/disconnect/route.ts` | Fixed | Added the same `requireRole(req, 'Leadership')` gate its siblings already have |
| 86 | HIGH — the newer `/go/book/[slug]` booking system (booking types with intake questions/buffers/colors, promoted as the "Public Link" from the staff-facing Booking Types manager) was completely non-functional for real anonymous visitors — its APIs (`/api/calendar/booking-types`, `/api/calendar/bookings`) were never added to `proxy.ts`'s public-route allowlist, so every unauthenticated booking attempt 401'd. The older `/book/[slug]` flow (`calendar_settings`/`bookings` tables) was correctly public and is the one this repo's "Confirmed working" section refers to | `proxy.ts`, `app/api/calendar/booking-types/route.ts`, `app/api/calendar/bookings/route.ts` | Fixed | Added both routes to `PUBLIC_PREFIXES`. Since a prefix match can't distinguish HTTP method or query params, added route-level `requireRole` guards for the parts that must stay staff-only: `booking-types` POST (creating/editing booking types) and `bookings` GET's no-`slug`/`date` branch (the internal cross-org booking list) — the public slot-check (`slug`+`date`) and guest booking creation (POST) remain unauthenticated by design, matching the older flow |
| 87 | HIGH — `/go/review/[token]` (the public review-request page real customers get emailed) 401'd on every star-rating/feedback submission — `/api/reputation/review-request/[token]` was never added to `PUBLIC_PREFIXES`, unlike every other token-based public route (`/api/signatures/`, `/api/unsubscribe/`, `/api/proposals/view/`) | `proxy.ts` | Fixed | Added `/api/reputation/review-request/` to `PUBLIC_PREFIXES` — the route's own auth is token-only by design, matching the established pattern |
| 88 | MEDIUM — `forms` POST/PATCH and `funnels` POST/PATCH/DELETE (+`[id]/pages` POST/PATCH) had no role check at all — any authenticated caller, including a "Contractor"-level account, could create/edit/publish/delete any form or funnel | `app/api/forms/route.ts`, `app/api/forms/[id]/route.ts`, `app/api/funnels/route.ts`, `app/api/funnels/[id]/route.ts`, `app/api/funnels/[id]/pages/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` to all writes, `requireRole(req, 'Leadership')` to funnel delete (matching forms' existing delete gate) |
| 89 | MEDIUM — deleting a form silently cascade-deleted every one of its submissions (`form_submissions.form_id ... on delete cascade`), directly contradicting the delete confirm dialog's own text ("Submissions will be kept") | `app/forms/page.tsx` | Fixed | Corrected the confirm text to accurately describe the real (destructive) behavior rather than changing the FK, since submissions genuinely orphaned without their parent form aren't clearly better and changing cascade behavior is a larger schema decision |
| 90 | MEDIUM — the funnel-embedded form block showed "Thank you! We'll be in touch soon." even when the submission silently failed server-side (missing `res.ok` check, empty `catch`) — a visitor could see a false success while no lead was ever captured. The standalone Forms product's public page already handled this correctly | `app/go/page/[slug]/PublicFunnelPage.tsx` | Fixed | `FormBlock` now checks `res.ok` and shows an inline error message instead of the success state on failure |
| 91 | HIGH — 6 automation action types (`Log Activity`, `Send Notification`'s activity mirror, `Notify Sales/Finance/Delivery/Assigned Rep`, `Log Touchpoint`, `Flag in Dashboard`, `Apply Service Template`) inserted into `crm_activities` using `description`/`logged_by` — neither is a real column (`title` and `user_name` are, per `schema.sql`), and `title` is `not null` with no default. Every one of these inserts failed silently (result never checked) — 4 of the 6 action types have zero other effect, so building an automation with them looked successful but permanently no-op'd | `lib/automations-engine.ts` | Fixed | Corrected all 6 insert sites to use `title`/`user_name`, matching the real schema and the already-correct pattern used elsewhere in this same file (Send Email's activity log) and in `app/api/sequences/execute/route.ts`/`webhooks/route.ts` |
| 92 | MEDIUM — `app/api/crm/contacts/[id]/ai-score/route.ts` read engagement data (email opens, clicks, proposal views, meetings) from `contact_timeline`, a table nothing in the codebase ever inserts into — the "Email Engagement/Proposal Views/Meetings Held" AI Lead Score factors were permanently zero for every contact, including genuinely highly-engaged ones, since the query against the always-empty table succeeds silently rather than erroring | `app/api/crm/contacts/[id]/ai-score/route.ts` | Fixed | Replaced with the same real-source computation `app/api/crm/contacts/[id]/timeline/route.ts` already uses (`broadcast_recipients` by contact email, `proposals` by company, meetings from `crm_activities`) |
| 93 | MEDIUM — `app/api/crm/companies/[id]/social-analysis/route.ts` selected a `social_links` column that doesn't exist on `crm_companies` (confirmed against `schema.sql`) — the PostgREST error was silently swallowed (destructured as `undefined`), so the Social Insights panel showed a generic "Company not found" for every real company, every time | `app/api/crm/companies/[id]/social-analysis/route.ts` | Fixed | Removed the nonexistent column from the select and the dead code path that read it; the website-scrape fallback (which was the only path that could ever actually populate social links) now always runs |
| 94 | MEDIUM — the Deal Panel's and Company Panel's "New Proposal" button opened a real, fully-functional form panel, but `onSave` discarded the collected data entirely (`onSave={(_data) => setCreatingProposal(false)}`) — clicking "Create Draft" silently closed the modal with no toast, no persistence, and no error, losing everything the user just filled in | `app/crm/pipeline/page.tsx`, `app/crm/companies/page.tsx` | Fixed | Both call sites now actually `POST /api/proposals` with the collected data (company, service type, rep, value, notes as a line item) and show a success/error toast, matching the standalone Proposals page's own create flow |
| 95 | HIGH — `app/api/crm/merge/route.ts` uses invalid PostgREST filter-path syntax (`'contact->>id'`) as if it were an update key when reassigning a merged-away contact's deals to the primary contact — the malformed call's result is never checked, so it silently fails; since `deals.contact_id` is `ON DELETE SET NULL`, the later delete of the merged-away contact instead **nulls** the deal's contact link (rather than repointing it), and the deal card keeps showing the deleted contact's name from the stale `contact` jsonb blob. Company merge has the same root issue — `company_id` FKs on deals/contracts/invoices/projects/proposals are never repointed to the primary company, only the denormalized name string is updated, so deleting the merged-away companies nulls those FKs too | `lib/` or `app/api/crm/merge/route.ts` | Fixed | Contact merge now updates the real `deals.contact_id` FK (was targeting a nonexistent `contact->>id` column) and rewrites the `deals.contact` display blob to the primary contact's merged info. Company merge now reassigns `company_id` on deals/contracts/invoices/projects/proposals directly by id (was name-only), with a name-matched fallback restricted to rows where `company_id` is still null, so a same-named unrelated company's rows are never touched |
| 96 | MEDIUM — single-company delete (`DELETE /api/crm/companies/[id]`) only deletes the `crm_companies` row — no cascade to contacts/deals/contracts/invoices/projects/proposals/activities — directly contradicting the Companies page's own bulk-delete confirm text ("This will also remove associated contacts, deals, and contracts") and the Deal Panel's "Delete Company" button ("Remove and all its data permanently"). Only `POST /api/crm/bulk-delete`'s company path actually cascades (and only 3 of 7 related tables), and even that matches by company **name** rather than id, risking deleting an unrelated company's data if two companies share a name | `app/api/crm/companies/[id]/route.ts`, `app/api/crm/bulk-delete/route.ts` | Fixed | Explicit decision: **block, don't cascade** — irreversible data loss outweighs the inconvenience of requiring reassignment first, and this matches the codebase's one existing precedent (the pipeline stage-delete guard also blocks rather than cascades). Added `lib/crm-cascade.ts`: both delete routes now return 409 (with a message listing exactly what's still attached) if the company has any related contacts/deals/contracts/invoices/projects/proposals, matched by `company_id` FK first with a null-company_id-only name fallback (same safe pattern as the merge-route fix). The one exception is the company's own activity log — it cascades along with the company rather than blocking, since it's just interaction history with no independent value or reachable UI once the company is gone. Bulk-delete now does partial success: companies with no related records are deleted, blocked ones are skipped and reported back by name+reason, so one company with real data attached doesn't stop cleanup of the rest of the batch. Fixed a related bug found while wiring this: both the single-delete and bulk-delete frontend call sites optimistically removed companies from the UI and only caught network-level failures, never checking `res.ok` — a blocked (409) delete would have shown the company as gone in the UI while it was still fully intact server-side. Both now revert the optimistic removal and surface the real error on any non-OK response. Updated both UI surfaces' copy to describe the actual (block, not cascade) behavior |
| 97 | MEDIUM — timezone bug in the booking/calendar system: `lib/ics-generator.ts` and `lib/calendar-links.ts` (Google Calendar "Add to calendar" link) both parse a plain `"YYYY-MM-DDTHH:MM"` string with `new Date(dateStr)`, which JS treats as the **server's local time** (UTC on Vercel) rather than the booking's configured timezone, then reformats into the target timezone — a booking intended for "2:00 PM America/Chicago" can end up on the `.ics` file/calendar link as 8:00 AM. The same root-cause bug (server-local `new Date()`/`getHours()` instead of the calendar's real timezone) also affects `lib/google-calendar.ts`'s slot-availability computation (today/lead-time cutoff, Google busy-slot day boundaries) and the calendar-sync/import routes' stored busy-time timezone stamping | `lib/ics-generator.ts`, `lib/calendar-links.ts`, `lib/google-calendar.ts`, `app/api/calendar/sync/route.ts`, `app/api/calendar/import-link/route.ts`, `app/api/calendar/subscriptions/route.ts` | Fixed | Added `lib/timezone.ts` (Intl-based, no new dependency) with `zonedWallTimeToUtc`/`dateAndTimeInZone`/`nowInZone`/`compactWallClock`, covered by new unit tests pinning known DST offsets (CDT/CST/JST) since this sandbox can't do live calendar verification. Fixed: ICS `DTSTART;TZID=` and the Google Calendar link (both already declare the zone separately, so they now just format the wall-clock digits, no conversion needed — the old round-trip through `new Date()` was the bug); the Outlook links (need a real UTC instant, now correctly converted); `computeAvailableSlots`'s today/lead-time cutoff (now reads "now" in the calendar's own configured zone, not server UTC); `getGoogleBusySlots`'s day-boundary window; and `app/api/calendar/sync/route.ts`'s Google-event-to-booking date/time extraction (feeds real availability blocking for that calendar's slug). Deliberately left `app/api/calendar/import-link/route.ts` and `app/api/calendar/subscriptions/route.ts` alone — those write to a fixed `calendar_slug: 'imported'`/subscription bucket with a self-consistent (always-UTC) timezone label that no real booking calendar's availability computation ever reads, so the inconsistency there doesn't corrupt anything customer-facing; lower priority, can revisit if that changes |
| 98 | MEDIUM — `app/api/broadcasts/[id]/send/route.ts` silently ignores 3 of the 12 audience filters exposed in the UI (`hasOpenedPrevious`, `hasClickedPrevious`, `excludeRecentRecipientsDays`) — they're correctly applied in the Preview endpoint (so the shown recipient count looks right) but never applied at actual send time, so a broadcast configured to "exclude contacts who received an email in the last 7 days" sends to the full, unfiltered list anyway. Separately, A/B subject-line testing is pure UI/schema scaffolding — the send route always uses `broadcast.subject`, no split logic or per-recipient variant assignment exists, and the `variant_a/b_sent/opens`/`ab_winner` columns are never written by any code path. Per-link click tracking (the "Link Click Tracking" panel) is also dead — `rewriteLinksForTracking()` is defined but never called by the real send path, so `broadcast_link_clicks` never receives rows from an actual send (aggregate open/click counts, which come from Resend webhooks independently, are unaffected and correct) | `app/api/broadcasts/[id]/send/route.ts`, `lib/broadcasts.ts`, `lib/email-tracking.ts` | Fixed | Extracted the preview endpoint's engagement/recency lookup into a shared `resolveEngagementFilters()` in `lib/broadcasts.ts` and applied it to the real send route too, so all 12 filters now match what the preview counted. Also added the missing `requireRole('Leadership')` to `PATCH /api/broadcasts/[id]`, matching its `DELETE`/`POST /send` siblings. A/B subject testing and per-link click tracking remain unbuilt features — not touched, still need real design decisions |
| 99 | MEDIUM — `app/api/delivery/send-template/route.ts`'s monthly-report template renders with empty `customizationData` (the Delivery Dashboard's "Template" button never sends it), producing a visibly blank branded email — no metrics, "No recommendations this period," "No changes logged this period" — sent straight to the client. The *correct*, fully-built version (`app/api/delivery/send-monthly-report/route.ts`, which aggregates real GSC/GA4/reputation/ranking/uptime data) is never called from any UI. Separately, `app/api/delivery/monthly-report/[id]/route.ts`'s reputation query has no fallback to `company_name` when `company_id` is null (unlike every other query in the same function) — and `company_id` is in practice *always* null for real workflows (the create-workflow modal discards the id `CompanySelect` provides), so a client's monthly report "Reputation" section is actually aggregated across every client's reviews, not just theirs — a cross-tenant data leak in an outbound white-label email | `app/api/delivery/send-template/route.ts`, `app/crm/delivery-dashboard/page.tsx`, `app/api/delivery/monthly-report/[id]/route.ts` | Fixed | The `reviews` table has no `company_id` column at all (only `company_name`, added later) — the `companyId`-based filter always silently no-op'd/errored and the no-id fallback returned every client's reviews. Now filters by `company_name` directly, matching every other query in the function; `companyId` threading through `NewWorkflowModal` wouldn't have helped since the column doesn't exist. Separately, the Delivery Dashboard's step-8 "Template" button now calls `send-monthly-report` (real aggregated data) instead of the blank generic `send-template` path; `send-monthly-report` gained an optional `recipientEmail` override so the existing "type an email" UX still works |
| 100 | HIGH — a fresh 6-agent audit sweep found ~15 more routes with the same systemic missing-auth gap already fixed elsewhere: `app/api/knowledge-base/route.ts`+`[id]` (portal clients could vandalize/publish/delete any internal article), `app/api/intelligence/{identify,visitors,companies,stats}/route.ts` (cross-tenant visitor PII — any authenticated caller, including one portal client, could read every client's tracked visitor data), `app/api/training/route.ts`, `app/api/playbooks/route.ts`+`[id]`, `app/api/sales-templates/route.ts`+`[id]` (sales-enablement content writable by any authenticated account), `app/api/time-entries/**` (read anyone's hours, create fake entries, approve/reject any pending entry), `app/api/chatbots/route.ts`+`[id]` GET (POST/GET(list)/GET([id]) inconsistent with PATCH/DELETE's `requireAdmin`; `[id]` GET also fully public via the proxy prefix needed for the chat widget, leaking `system_prompt`/`knowledge`), `app/api/reputation/{reviews,sync,send-request}/route.ts` (any authenticated caller could inject fake reviews attributed to any company or post a live reply to the connected Google Business Profile), `app/api/courses/route.ts`+`[id]/enrollments` (create/enroll open to any role while edit/delete required Leadership; roster had no scoping), `app/api/projects/route.ts`+`[id]` (GET had no portal scoping — any portal client could read any other client's full project data by changing the `company` query param; POST/PATCH had no role check at all), `app/api/tasks/route.ts` POST (sibling PATCH/DELETE both require auth), `app/api/delivery/workflow/route.ts` (no auth at all) | see above | Fixed | Added `requireRole`/`requireAdmin`/`requirePortalClient` matching each route's real caller population and its own siblings' existing gate level. Bulk time-entry approve now requires Dept Manager+ server-side, matching the UI's own (previously client-side-only) gate. The public `/chat` chatbot endpoint was also given real IP rate limiting via `proxy.ts`'s existing `memoryLimited` helper — it was the only public route with zero throttling and a real LLM-cost/DoS exposure |
| 101 | CRITICAL — `app/api/calendar/auth/route.ts` took `userEmail` directly from the request body with no check it matched the caller's own identity, then encoded it into the OAuth `state` param; `app/api/calendar/callback/route.ts` upserts `calendar_settings` keyed by that `user_email`. Any authenticated staff member could `POST` an arbitrary victim `userEmail`, complete the Google OAuth flow with their OWN Google account, and silently redirect the victim's public `/book/[slug]` link to create every future booking's Meet link/calendar event on the attacker's calendar instead — worse than the already-fixed AUDIT #84 (OAuth CSRF) since no victim interaction is required at all, the attacker drives the entire flow themselves. Separately, `app/api/calendar/settings/route.ts` (POST/DELETE) and `app/api/calendar/subscriptions/route.ts` (POST/PATCH/DELETE) had zero ownership checks — any authenticated caller could delete or overwrite another team member's booking calendar or ICS subscriptions | `app/api/calendar/auth/route.ts`, `app/api/calendar/callback/route.ts`, `app/api/calendar/settings/route.ts`, `app/api/calendar/subscriptions/route.ts` | Fixed | `calendar/auth` now derives `userEmail`/`userName` from the caller's own verified session (`getAuthUser`) and ignores any client-supplied identity fields. `calendar/settings`/`calendar/subscriptions` now require the caller to own the target record (or be Leadership/admin) before mutating it, looking up the real owner from the DB row where the id doesn't directly carry it (subscriptions DELETE/PATCH) |
| 102 | HIGH — `lib/crm-cascade.ts`'s block check (AUDIT #96) and `app/api/crm/merge/route.ts`'s FK reassignment (AUDIT #95) both omitted `renewals`/`maintenance_records`, which carry the same `company_id` FK as the six checked/reassigned tables — a company with only an active maintenance contract or a pending renewal (and no rows in the other six) would pass the delete-block check with no warning and silently orphan those rows; the equivalent company merge would leave them pointing at the now-deleted merged-away company. Separately, an adversarial review found the one destructive operation the delete-block design still performs (cascading the company's own `crm_activities`) used the same id-then-name-fallback matching as the read-only block check — but `crm_companies.name` has no unique constraint, so on an actual `DELETE` that fallback could destroy a different, still-live company's activity history if it shares a display name and has legacy orphaned rows. Both delete routes also deleted activities BEFORE the company row itself, non-atomically — if the company delete then failed, the activity log was already gone even though the company was untouched | `lib/crm-cascade.ts`, `app/api/crm/merge/route.ts`, `app/api/crm/companies/[id]/route.ts`, `app/api/crm/bulk-delete/route.ts` | Fixed | Added `renewals`/`maintenance_records` to both the block check and the merge's `fkTables`. `deleteCompanyActivities` now matches by `company_id` only (no name fallback) — any such legacy rows are left behind (safe) rather than risk deleting the wrong company's data. Both delete routes now delete the company row first, activities second, so a failed company delete never touches the activity log |
| 103 | HIGH — `app/social/page.tsx`'s composer never forwarded `status` on post creation (`onCreate({...})` dropped it entirely), and `POST /api/social-posts` hardcoded `status: 'draft'` regardless of what was sent — no post could ever reach `status: 'pending_approval'`, so the client portal's approval queue (`GET /api/social-posts?status=pending_approval`) was always empty and the composer's "Submit for Approval" flow was completely non-functional end-to-end. The only way a post ever got published was a staff member self-clicking the "Awaiting approval" Approve/Reject banner inside the same composer, bypassing the client entirely | `app/social/page.tsx`, `app/api/social-posts/route.ts` | Fixed | `handleSave`'s create branch now forwards `status`; the POST route accepts `'draft'`/`'pending_approval'` (the only two ever legitimately client-set on create) instead of hardcoding `'draft'` |
| 104 | HIGH — two more routes had the pre-#97 server-local timezone bug, missed by that pass's scope: `app/api/bookings/route.ts`'s past-booking check (`new Date(dateStr) < new Date()`) wrongly rejected legitimate same-day afternoon bookings for any US-timezone calendar, and the newer `/go/book` flow's `app/api/calendar/bookings/route.ts` slot-availability computation had the identical bug plus a missing `calendar_slug` filter on its imported-events conflict query — pulling every booking org-wide instead of just the `'imported'` bucket, so any staff member's unrelated personal meeting could block every booking type's availability for that whole date (directly contradicting AUDIT #97's own claim that the `'imported'` bucket is isolated from real availability computation). Separately, `lib/calendar-links.ts`'s Yahoo calendar link was left on the broken no-conversion pattern when Outlook was fixed in #97, and `app/go/book/[slug]/page.tsx` reimplemented calendar-link generation client-side with a hardcoded `'America/Chicago'` and no real UTC conversion for Outlook links | `app/api/bookings/route.ts`, `app/api/calendar/bookings/route.ts`, `lib/calendar-links.ts`, `app/go/book/[slug]/page.tsx` | Fixed | Both booking routes now use `lib/timezone.ts`'s `zonedWallTimeToUtc`/`nowInZone` with the calendar's real configured (or connected-Google-Calendar-derived) timezone. The imported-events query now filters `calendar_slug = 'imported'`. Yahoo's link now uses the same real UTC conversion as Outlook. The client-side booking confirmation page now reuses the shared, already-fixed `lib/calendar-links.ts` helpers with the real timezone threaded through from the slots API response instead of reimplementing link generation |
| 105 | MEDIUM — `lib/guided-actions.ts` and `app/sales/page.tsx` both still used exact `'Closed Won'`/`'Closed Lost'` stage matching, inconsistent with the `!stage.startsWith('Closed')` check already fixed elsewhere (AUDIT #53) to handle custom pipeline stage names (AUDIT #42) — a deal in a custom "Closed - Duplicate"-style stage would still incorrectly surface in Guided Actions as needing follow-up, or miscount on the Sales hub's KPIs. `app/sales/page.tsx`'s KPIs also silently truncated past 100 rows (same bug class as AUDIT #48, missed on this hub page) | `lib/guided-actions.ts`, `app/sales/page.tsx` | Fixed | Both now use `!stage.startsWith('Closed')`; `app/sales/page.tsx` now uses `fetchAllPages()` for its deals/proposals/contracts fetches |
| 106 | MEDIUM — `app/api/inbox/unified/route.ts`'s activity-type filter compared against capitalized values (`'Email'`, `'Call'`, `'Meeting'`, `'Note'`) that no real write path ever produces (`lib/types.ts`'s `ActivityType` is lowercase) — the Unified Inbox's activity source silently never matched any real logged CRM activity | `app/api/inbox/unified/route.ts` | Fixed | Filter now uses the real lowercase values (`'email'`, `'call'`, `'meeting'`, `'note'`, `'call_note'`) |
| 107 | MEDIUM — `app/api/seo-reports/[companyId]/route.ts`'s response shape never matched what `app/reports/seo-reports/page.tsx`'s `ReportSnapshot` interface expects — the route groups snapshots by period with metrics nested under each product name (`search_console`/`analytics`), while the frontend expects a flat `{period, clicks, impressions, avg_position, sessions, *_change}` per row. `formatPosition(snapshot.avg_position)` was called on `undefined` and threw — any company that has ever received a report would crash the first time its "Report History" row was expanded | `app/api/seo-reports/[companyId]/route.ts` | Fixed | Route now returns the flat shape the frontend expects, including real period-over-period `*_change` percentages computed against the prior period (previously absent entirely) |
| 108 | MEDIUM — `app/funnels/page.tsx`'s "Edit funnel" list button linked to `?id=${f.id}`, but `app/funnels/editor/page.tsx` reads `?funnel=` and `?page=` — the primary, most prominent edit affordance on the whole page always dead-ended on "No page selected," since the list-level `FunnelSummary` type doesn't carry individual page ids (only a `pageCount`) | `app/funnels/page.tsx` | Fixed | Button now fetches the funnel's detail (same endpoint the expand/detail view already uses) to find its first real page, then navigates with both required params; shows an error toast if the funnel genuinely has no pages yet instead of navigating to a dead URL |
| 109 | MEDIUM — a portal client's contract/proposal `status` PATCH was restricted to allowed field *names* (AUDIT #77) but not allowed *values* — `VALID_TRANSITIONS` on contracts governs which transitions are legal at all, for any caller, not who's allowed to make a given legal one, so a portal client whose contract was at `'Countersign Needed'` could `PATCH {status: 'Fully Executed'}` directly, skipping the actual internal countersignature process (and firing `contract_executed` for a contract that was never really countersigned). Proposals have no transition graph at all, so a portal client could set their own proposal directly to `'Approved'`, an internal-workflow status meant to represent staff sign-off | `app/api/contracts/[id]/route.ts`, `app/api/proposals/[id]/route.ts` | Fixed | Both routes now also check `body.status` against the specific values the real portal Approvals UI actually ever sends, when the caller isn't staff |
| 110 | MEDIUM — social post approval workflow config/status-value gaps aside (#103), the automation builder's trigger-level config (deal-stage filter dropdown for `deal_stage_changed`, overdue-days threshold for `invoice_overdue`) is collected in the UI but `handleSave` never includes the trigger node's `config` in the persisted payload at all — silently discarded on every save, and the engine has no equivalent check for either field regardless (a `deal_stage_changed` automation fires on every stage change; `invoice_overdue` fires at whatever days-overdue the cron job hardcodes). Same "config collected but never used" pattern as the already-fixed AUDIT #12 | `app/automation/builder/page.tsx`, `lib/automations-engine.ts`, `app/api/cron/route.ts` | Fixed | `automations.config` was already a real, persisted column (the `form_submitted` trigger's `formScope`/`formId` already round-trip through it) — this was purely a save/load-path gap in the builder, same class as #12. `handleSave` now sends the trigger node's `config`; the load-on-edit effect now hydrates it back instead of resetting to `{}`. Added matching engine-side checks: `deal_stage_changed` now skips execution unless `config.stage` matches (or is unset, meaning any stage). For `invoice_overdue`, `checkTimeBasedTriggers()` only ever fires two real checkpoints (the initial Sent→Overdue transition, and a 3-day-overdue recheck) — a free-form "days" number input would have let a user configure e.g. 7 and silently never fire, so the field was changed to a dropdown of the two real options plus "Any," with a matching exact-equality engine check. Covered by 5 new unit tests |
| 111 | MEDIUM — the "Review Campaigns" bulk request system (`app/reputation/requests/page.tsx`) is entirely non-functional decoration: `POST /api/reputation/requests` only inserts a `review_campaigns` row with counts hardcoded to 0 — `review_campaigns` is referenced nowhere else in the codebase, no cron/worker ever processes a campaign regardless of `status`/`scheduled_at`, so "Schedule for later" schedules nothing and Sent/Opened/Reviews stay stuck at 0 forever. `GET /api/reputation/requests` also hardcodes `templates: {}`, so the 3 pre-built template previews can never render. Contrast with the working single-send "Request Review" button on the main `/reputation` page, which makes this page look equally real while doing nothing | `app/reputation/requests/page.tsx`, `app/api/reputation/requests/route.ts`, `app/api/reputation/review-request/[token]/route.ts`, `app/api/cron/route.ts`, `lib/review-campaigns.ts` | Fixed | Built a real dispatch mechanism. `lib/review-campaigns.ts` resolves each audience label into real `crm_companies`/`deals`/`crm_contacts` recipients (status = Active Client, service-type keyword match for the 4 service-scoped audiences, `created_date` cutoff for the 2 tenure-scoped audiences) and sends the same tokenized review-request email as the existing single-send flow, tagging each `review_requests` row with the new `campaign_id` FK. Unscheduled campaigns dispatch synchronously on create (`POST` now sends, not just inserts); scheduled ones are picked up by a new cron step (`dispatchScheduledReviewCampaigns`) with an atomic scheduled→active claim so two overlapping ticks can't double-send. `sent_count`/`opened_count`/`reviews_count` are now real, updated via a new `increment_review_campaign_counts` RPC (atomic, avoids read-then-write races) — opened tracked via a conditional `opened_at IS NULL` update on the public review-request GET, reviews via the existing rating-submission POST. `GET /api/reputation/requests` now returns real template previews instead of `templates: {}`. Also found and fixed a fresh gap while building this: both `GET`/`POST /api/reputation/requests` had zero auth — added `requireRole(req, 'Team Member')`. Needs the new `add_review_campaign_dispatch.sql` migration applied (adds `review_requests.campaign_id`/`opened_at` + the RPC function) |
| 112 | MEDIUM — Rank Tracker's "Scheduled Reports" (`app/rank-tracker/page.tsx`'s Reports tab) never sends anything — `rank_tracker_reports`/`getScheduledReports()` is only ever read/written by the CRUD route the UI calls directly; nothing in `app/api/cron/route.ts` or elsewhere ever reads it. Creating a report with real recipients and a "Weekly"/"Daily" frequency just inserts a row; `lastSentAt` can never change despite the page copy explicitly promising "Automated weekly email ranking reports" | `app/rank-tracker/page.tsx`, `app/api/rank-tracker/reports/route.ts`, `lib/rank-tracker.ts`, `app/api/cron/route.ts` | Fixed | Added `sendDueScheduledReports()` in `lib/rank-tracker.ts`, called every cron tick. For each report, cadence-due is computed from `lastSentAt` (or `createdAt` if never sent) against a per-frequency interval (daily/weekly/biweekly/monthly). When due, builds a real ranking-snapshot HTML email (keyword, company, current position, position change, best position) from `tracked_keywords` (respecting `filters.companyId` if one is ever set, though the UI has no filter controls today so it's effectively all tracked keywords), sends it to the report's recipients via `sendEmail`, and updates `last_sent_at` — so the table's "Last Sent" column and the page's "Automated ... email ranking reports" copy are now both honest. A report with zero matching tracked keywords is skipped rather than sending an empty email. (The Competitors tab's empty comparison data is NOT a bug — the UI honestly discloses live SERP data isn't wired up; confirmed working-as-labeled) |
| 113 | MEDIUM — the Forms product has a fully-built, working submissions backend (`GET /api/forms/[id]/submissions`, paginated, real `data`/`sourceUrl`/`contactId`/`status`) but no UI anywhere to view it — `app/forms/` has no submissions list/detail page, and no fetch call to this endpoint exists anywhere in the codebase, despite the embed instructions explicitly promising twice ("Submissions appear in GravHub → Forms → click the form → Submissions"). Leads that map to a contact field still land in CRM Contacts (mitigating the impact), but any answer that doesn't (multi-select, checkbox, rating, custom fields) is captured in `form_submissions.data` and permanently unreachable through the UI. Separately, `notifyEmails`/`redirectUrl` are real, working backend fields with no UI control to ever set them — every form's submission-notification email is silently dead since the array is always empty | `app/forms/page.tsx`, `app/api/forms/[id]/submissions/route.ts` | Fixed | Added a "Submissions" tab to the form editor (`SubmissionsTabContent`) that fetches all pages via `fetchAllPages`, maps each `data` key through the form's `fields` (`name → label`) for human-readable display, and shows `sourceUrl`/`status`/`createdAt` with loading/empty/error states. Added "Notify team (emails)" and "Redirect after submit" inputs wired to the already-real `notifyEmails`/`redirectUrl` backend fields. Also found and fixed a fresh gap while building this: `GET /api/forms/[id]/submissions` had zero auth — added `requireRole(req, 'Team Member')` matching sibling forms routes |
| 114 | MEDIUM — `MaintenancePanel`'s "Upload Document" (`app/maintenance/page.tsx`) reads the file via `FileReader` and only calls `setDocuments(prev => [...])` — pure React state, no `fetch` call anywhere, even though the backend fully supports it (`PATCH /api/maintenance/[id]` reads/writes a real `documents` column). Since `documents` state re-initializes from the record's real (unchanged) array every time the panel reopens, any uploaded file silently vanishes the moment the panel is closed and reopened or the page reloads — looks successful (appears in the list immediately) but nothing is ever persisted | `app/maintenance/page.tsx` | Fixed | Added an `onUpdateDocuments` prop (mirroring the existing `onUpdateBilling` PATCH pattern) — both upload and delete now persist via `PATCH /api/maintenance/[id]` |
| 115 | LOW — `app/settings/email-auth/page.tsx`'s SPF/DKIM/DMARC "configured" indicators and "Deliverability Checklist" are `useState`-only with no persistence or real DNS check anywhere — permanently stuck at "not configured" and reset to unchecked on every page reload | `app/settings/email-auth/page.tsx`, `app/api/settings/email-auth/check/route.ts` | Fixed | New `GET /api/settings/email-auth/check` does real DNS lookups (`node:dns/promises`) against the company's sending domain (`settings.company.website`): SPF via a root TXT record starting `v=spf1`, DMARC via a `_dmarc.<domain>` TXT record starting `v=DMARC1`, DKIM via any of Resend's 3 verification CNAMEs (`resend[23]?._domainkey`) resolving to a `resend.com` target. The page now fetches this on load (plus a manual "Recheck DNS" button) instead of showing hardcoded `false`. Sanity-checked live against gravissmarketing.com: SPF correctly resolves true (real record present), DKIM correctly resolves false (not yet configured) — the check reports the actual current state, not a guess. The Deliverability Checklist now persists through the existing `training_progress` backend from AUDIT #30 (`contentId: 'email-deliverability'`), reusing its atomic per-item upsert RPC instead of building new infrastructure |
| 116 | LOW — `components/layout/PageLoadingOverlay.tsx`'s capturing click-listener registers with `capture: true` on `document`, which always fires before the clicked element's own (bubble-phase) `onClick` could call `preventDefault()` — so the `e.defaultPrevented` check can never actually see a cancellation the clicked element's own handler makes, and the overlay would still show for up to `MAX_STUCK_MS` (6s) before self-healing. Separately, the overlay div is `pointer-events-none`, so it doesn't actually block interaction with the stale page underneath while showing — looks like a blocking loader but isn't one functionally | `components/layout/PageLoadingOverlay.tsx` | Fixed | The capture-phase click listener now defers its `e.defaultPrevented` check into a `queueMicrotask`, which runs after the full capture+bubble dispatch for that click has finished — so it correctly observes a `preventDefault()` called by the clicked element's own bubble-phase handler, while staying on the capture phase (still immune to an ancestor's `stopPropagation()`). Also made the overlay actually block interaction with the stale page while visible (`pointer-events: auto` except during the final fade-out, previously `pointer-events-none` throughout) so it functions as the blocking loader it visually presents as |
| 117 | MEDIUM — `app/projects/page.tsx`'s `ProjectDetailPanel` (~520 lines, fully built: status/notes/milestones editing, delete) is unreachable dead code — its `selected` state is never actually set to a real project (every grep'd `setSelected(` call and every card's `onClick` route to `/projects/[id]` instead). The reachable detail page (`app/projects/[id]/page.tsx`) has no delete button anywhere and renders milestones/notes read-only with no add/toggle affordance, so deleting a project, adding/completing milestones, and adding project notes are all unreachable in the live app despite working code existing for all three | `app/projects/page.tsx`, `app/projects/[id]/page.tsx` | Fixed | User decision: keep the page-based detail view (matches the rest of the app's navigation pattern) — ported the missing delete button (with `ConfirmModal`) and editable milestones (add + click-to-toggle-complete) and notes (add) to `app/projects/[id]/page.tsx`, wired through its existing generic `updateProject()` PATCH helper. Deleted the unreachable `ProjectDetailPanel` and its dead `selected` state/handlers/`ProgressRing` component from `app/projects/page.tsx` — its embedded-task-list tab was NOT ported, since `[id]/page.tsx` already has a more capable real `app_tasks`-backed task board that superseded it |
| 118 | LOW — `app/tickets/page.tsx`'s "Unassigned — assign this ticket to auto-create a project task" banner is fabricated on both counts: its "Start →" button only PATCHes ticket status (never sets `assignedTo`), and no `autoCreateTask`/`createTaskFromTicket` implementation exists anywhere in the codebase regardless. Separately, `sendReply` builds its PATCH payload from React closure state rather than the just-applied functional update — two replies sent in quick succession (before a re-render) can silently drop one server-side even though the UI briefly shows both | `app/tickets/page.tsx` | Fixed | Corrected the banner copy to describe the real behavior (marks In Progress, doesn't assign or create a task) rather than building real assign+auto-create-task wiring, which would be a larger feature. `sendReply` now derives `newMessages` from the functional `setLocalTickets` update's own `prev`, matching the `useRef`-mirror fix pattern already applied elsewhere (AUDIT #5) |
| 119 | LOW — `app/reputation/page.tsx`'s `handleAddReview`/`handleRespond` only branch on `res.ok`, no `else` — a validation error or server failure leaves the modal open/closed with zero user feedback, same bug class already fixed elsewhere (AUDIT #30/#31) but not applied here. Separately, `app/billing/page.tsx`'s CSV invoice importer wraps each row's POST in an empty `catch {}` and only reports a total success count, with no indication which rows failed or why | `app/reputation/page.tsx`, `app/billing/page.tsx` | Fixed | Both `handleAddReview`/`handleRespond` now show an error toast on a non-OK response or thrown error. The CSV importer now tracks failed and skipped (missing company/amount) counts separately and reports all three in the completion toast |
| 1 | BCC email webhook accepted unsigned payloads when `RESEND_WEBHOOK_SECRET` unset — could inject fake CRM activities | `app/api/email/inbound/route.ts` | Fixed | Now fails closed in production, matching `SESSION_SIGNING_KEY` pattern |
| 2 | `admin/seed-clients` and `admin/data-audit` POST had zero auth — could overwrite real client/contract data | `app/api/admin/seed-clients/route.ts`, `app/api/admin/data-audit/route.ts` | Fixed | Added `requireAdmin` to both |
| 3 | Service worker cached authenticated `/api/*` responses in a shared, unscoped, un-purged cache — risk of leaking one user's data to the next on a shared device or after login/logout | `public/sw.js` | Fixed | Removed API response caching entirely; `Cache-Control: no-store` doesn't stop manual `cache.put()` |
| 4 | Company/Contact detail panels didn't reset local state when switching records without closing the panel — editing notes on record A while B's data was stale in state could overwrite B's real notes with A's | `app/crm/companies/page.tsx`, `app/crm/contacts/page.tsx` | Fixed | Added `key={record.id}` to force remount on selection change |
| 5 | `handleAddNote`/`handleAddTask` read stale closure state — two rapid additions before a re-render could silently drop one | `app/crm/contacts/page.tsx` | Fixed | Added `useRef` mirrors so persistence always reads the true latest array |
| 6 | WordPress plugin API-key retry loop aborted entirely on the first key's network error instead of trying the next key | `app/api/wordpress/seo/sync/route.ts` | Fixed | Per-candidate try/catch |
| 7 | Service worker precached `/favicon.ico`, which 404s — `cache.addAll()` fails atomically, silently breaking SW install/activate entirely | `public/sw.js` | Fixed | Found while verifying PWA Stage A; removed dead entry, also fixed same dead path in push-notification icons |
| 8 | Delivery Dashboard crashes (`TypeError`) as soon as any real workflow exists — API returns raw DB row shape, UI expects a transformed `{steps: [...]}` shape | `app/crm/delivery-dashboard/page.tsx`, `app/api/delivery/workflow/route.ts` | Fixed | Added a server-side `mapWorkflow()` transform on both GET and POST; also added a defensive empty-array guard in `isWorkflowCompleted` |
| 9 | GravIntel `visit_count` self-resets to 1 on nearly every event because the upsert unconditionally writes `visit_count: 1` regardless of event type | `app/api/intelligence/track/route.ts` | Fixed | Split into an insert-only upsert (`ignoreDuplicates: true`) for first-visit defaults, plus a separate update for mutable per-visit fields that never touches `visit_count` |
| 10 | GravIntel lead scoring (`gi_score_visitor` SQL function) is never called anywhere — `lead_score`/`is_hot_lead` permanently sit at defaults while the UI renders a live-looking score bar and 🔥 flame | `app/api/intelligence/track/route.ts`, `app/intelligence/page.tsx` | Fixed | Now called after every tracked event so score/hot-lead flag stay current |
| 11 | Email-to-ticket conversion queries `app_settings.gmail_access_token`, a column that doesn't exist — Gmail tokens are stored per-user on `team_members`. Fails 100% of the time, silently, every cron tick | `app/api/tickets/from-email/route.ts` | Fixed | Now checks every team member's connected, non-expired Gmail account instead of a nonexistent global token |
| 12 | Automation builder never sends per-action config (email subject, tag name, task assignee, etc.) to the backend — `automations.actions` is just `text[]`, no config column exists. Actions execute with no real parameters and mostly no-op | `app/automation/builder/page.tsx`, `lib/automations-engine.ts` | Fixed | Shipped in 3 increments (see git history): `automations.actions` migrated to `jsonb` (`{type, config}[]`), the engine now merges each action's own config into context via an explicit key-translation layer (builder field names don't match engine context keys for 7/9 action types — found during design review, would have silently no-op'd if spread naively), and the builder UI's `handleSave`/load path now actually round-trips `node.config` instead of discarding it. `SequenceAutomateTab.tsx`'s request shape is unchanged — the API normalizes bare-string actions server-side |
| 13 | "Wait / Delay" automation action doesn't pause anything — falls through immediately, and its resume mechanism (`automation_pending_steps`) is fed blank `automation_id`/`run_id` that nothing ever populates, so cron resume always fails | `lib/automations-engine.ts` | Fixed | Wait now writes a real `automation_id`/`run_id` and genuinely stops executing (the loop breaks instead of falling through); `executeWorkflow` gained a `'waiting'` run status instead of mislabeling a paused run `'completed'`. Correction during rollout: the first version of this fix assumed `automation_pending_steps` had `status`/`remaining_actions` columns based on a migration file in this repo that didn't reflect the table's real live shape (confirmed via `information_schema.columns` after a migration failed with `column "status" does not exist`) — the live table tracks resume position via `step_index` instead, with no status column at all. Corrected the app code to match: a pending step is claimed atomically via `DELETE ... RETURNING` (only the invocation whose delete returns a row proceeds) rather than a status flip, and resume re-fetches the automation's current actions fresh from `step_index` instead of a frozen snapshot |
| 14 | 4 action types and 6 trigger types were selectable in the automation UI (including in 4 of 8 built-in templates) but had no matching implementation — automations built with them silently no-op'd forever | `app/automation/page.tsx`, `lib/automations-engine.ts` | Fixed | Removed the dead options and the 4 templates that relied on dead triggers, matching UI to what `lib/automations-engine.ts` actually implements. The bigger issue in #12/#13 (builder config not persisting) remains open |
| 15 | `app/api/portal-clients/[id]/route.ts` PATCH/DELETE has no per-resource authorization — any authenticated caller can modify or delete any portal client by ID | `app/api/portal-clients/[id]/route.ts` | Fixed | PATCH now requires authentication; non-staff callers may only update their own record and only `lastLogin`/`access` (the one legitimate self-service call site). DELETE now requires `requireRole(req, 'Team Member')` |
| 54 | Cron's Wait/Resume claims a pending step by re-slicing the automation's ORIGINAL, full actions array using `step_index`, but a resumed `executeWorkflow` call was passed a TRUNCATED slice — if that resumed run hit a *second* Wait step, its `step_index` got computed relative to the truncated array's own loop-local index (0-based again), not the original array cron re-slices from next time. Any automation with 2+ Wait steps would re-execute already-completed actions and loop on the same Wait forever, silently never reaching its final action | `lib/automations-engine.ts`, `app/api/cron/route.ts` | Fixed | Found by an adversarial regression-review agent during a fresh audit pass, one turn after AUDIT #12/#13 shipped — no test covered 2+ Waits in one automation, which is why it wasn't caught then. Threaded a new `indexOffset` parameter through `executeWorkflow`/`executeAction` so `step_index` is always computed against the original array regardless of how many resumes deep the call is; cron passes `claimed.step_index` as that offset. Added a regression test (`tests/unit/lib/automations-engine.test.ts`) that calls `executeWorkflow` directly simulating a resumed slice and asserts the correct global `step_index` — verified it fails without the fix by temporarily reverting and re-running |
| 55 | A run that paused on Wait never transitioned off `'waiting'` — `executeWorkflow` always minted a fresh `run_id` on resume instead of continuing the original `automation_runs` row, so the paused row sat at `'waiting'` forever while a second, disconnected row (for the same logical trigger) completed normally. Also inflated "Total Runs" / deflated "Success Rate" on the automation list, since the orphaned row counted toward `runs` but never resolved | `lib/automations-engine.ts`, `app/api/cron/route.ts` | Fixed | Resume now reuses the original run's id (`resumeRunId`) instead of minting a new one, and skips the initial `automation_runs` insert when resuming — the final status update at the end of `executeWorkflow` then lands on and correctly finalizes the same row the pause left at `'waiting'`. Also seeds the resumed call's `steps` array from the paused run's own pre-pause steps (`priorSteps`, with the trailing `'pending'` placeholders stripped) so run history isn't overwritten |
| 56 | If an automation was paused or deleted while a step was pending, cron's resume loop claimed (deleted) the pending step, found the automation inactive/missing, and just `continue`d — no error, no log, no run status update. The contact's remaining actions never ran, and the run stayed visibly `'waiting'` forever with zero indication anything went wrong | `app/api/cron/route.ts` | Fixed | Now finalizes the original run as `'failed'` with an explanatory error ("Automation was paused/deleted while a Wait step was pending") instead of silently discarding it. Same finalization added to the outer catch block, so a failure before `executeWorkflow` is even reached no longer leaves the run stuck at `'waiting'` either |
| 57 | The Runs tab in the automation builder always showed a spinning "Running" badge for every completed run — `GET /api/automations/[id]/runs` did a bare type assertion (`run.status as 'success' \| 'failed' \| 'running'`) instead of translating the real DB values (`'running' \| 'completed' \| 'failed' \| 'waiting'`), so `'completed'` and `'waiting'` both fell through the UI's default branch | `app/api/automations/[id]/runs/route.ts`, `app/automation/builder/page.tsx` | Fixed | Route now maps `'completed'` → `'success'`; builder UI gained a real amber "Waiting" badge for the `'waiting'` status instead of it rendering as "Running" |
| 58 | Moving `/api/cron` from once-daily to a 5-minute GitHub Actions schedule (#16 below) exposed a latent dedup gap: `checkTimeBasedTriggers()`'s 3+-day-overdue-invoice check and 90/30-day renewal-window check fire `fireAutomations(...)` for every matching row on *every* tick, with no state tracking to prevent re-firing. At 5-minute cadence, any automation on `invoice_overdue`/`renewal_90`/`renewal_30` would send its email or create its task up to 288x/day for the entire time the invoice stays overdue or the contract sits in its renewal window, instead of once | `app/api/cron/route.ts` | Fixed | Direct regression from this session's own #16 fix, caught by a follow-up audit pass before it reached a real cadence. Added `overdue_3d_notified`/`renewal_90_notified`/`renewal_30_notified` boolean flags (new migration) — each trigger now fires exactly once per threshold crossing. The original Sent→Overdue transition was already self-throttling (the status change removes it from that query) and didn't need a flag. **Needs migration**: `supabase/migrations/add_time_trigger_dedup_flags.sql` |
| 59 | #32's CSRF narrowing only protected 1 of 5 routes that actually needed it — `requireWordPressAuth` accepts EITHER the plugin's `X-GravHub-Key` OR a staff session cookie for `settings` (GET/PATCH), `scores` (GET/POST), and `health` (GET/POST), and `sync`/`reports` are staff-only (`requireAdmin`) with no plugin-key path at all — yet all of them remained inside the CSRF-exempt `/api/wordpress/seo/` prefix alongside the one route (`health` PATCH) the original fix specifically called out as exploitable | `proxy.ts` | Fixed | Found by a follow-up audit agent re-checking the fix's own reasoning against every route under the prefix. Root-caused: the whole prefix was public specifically so the plugin's key-only server calls wouldn't 401 at the outer auth gate, which doesn't recognize `X-GravHub-Key`. Removed `/api/wordpress/seo/` from `PUBLIC_PREFIXES` entirely and taught the outer gate to recognize `X-GravHub-Key`'s presence as a valid auth signal (same presence-only pattern already used for the `Authorization` header) — CSRF and the auth gate now apply uniformly to every route in the prefix, while the plugin's key-only calls still pass through un-broken (real key validation still happens downstream in `requireWordPressAuth`) |
| 60 | The public, token-based proposal accept/decline page (`/proposal/[token]`, the actual link clients receive by email) 401'd on every single load — unlike every other token-based public route (`/api/signatures/`, `/api/unsubscribe/`, `/api/track/`), `/api/proposals/view/[token]` was never added to `PUBLIC_PREFIXES`, so proxy's outer auth gate rejected every unauthenticated request before the route's own token check ever ran. The front-end's catch-all showed a generic "Proposal not found or link has expired" for 100% of real clients. Also found in the same trace: this route never called `fireAutomations`, so `proposal_accepted`/`proposal_declined` never actually fired from the real client-facing flow — only from the internal, staff-facing `/api/proposals/[id]` PATCH | `proxy.ts`, `app/api/proposals/view/[token]/route.ts` | Fixed | Added `/api/proposals/view/` to `PUBLIC_PREFIXES` (confirmed the route's own auth is token-only by design, matching the other public token routes). Added the missing `fireAutomations('proposal_accepted'/'proposal_declined', ...)` call to the real accept/decline handler |
| 120 | CRITICAL — `app/api/auth/setup-account/route.ts` set a new team member's password given only `{userId, password}` — anyone who knew or guessed a `team_members.id` (sequential-looking, not a secret) could set the password for any not-yet-onboarded account, including auto-provisioned Super Admins, with zero verification | `app/api/auth/setup-account/route.ts`, `app/setup-account/page.tsx` | Fixed | Rewrote to require `{email, code, password, avatarUrl}` and re-validate `code` against `team_members.verification_code`/`verification_expires` server-side before calling `db.auth.admin.updateUserById`, matching the equivalent portal-client flow. Also fixed a real regression this closed: the setup-account page's `pollApproval()` called an authenticated `team-members` endpoint before the user has a session, so it always 401'd and approved hires were stuck at "AWAITING APPROVAL" forever — added a public `check-approval` endpoint (booleans only, mirroring the existing portal-client one) and rate-limited both the verify-code and setup-account POSTs |
| 121 | CRITICAL — `getCurrentUser()`, `requireAdmin()`, and the portal-auth staff-bypass checks never checked `team_members.status` — a suspended or terminated staff member's existing session (or a fresh Google/magic-link sign-in, since nothing blocked re-authentication either) retained full access indefinitely, including the staff-bypass path that lets any "staff" caller read/write any company's portal-scoped data regardless of company match | `lib/rbac.ts`, `lib/admin-auth.ts`, `lib/portal-auth.ts`, `app/api/auth/{google-verify,session,profile}/route.ts` | Fixed | All 5 auth-resolution paths now require `status === 'active'` in addition to whatever role/admin flag they already checked |
| 122 | CRITICAL — `app/api/projects/[id]/route.ts` only exported `PATCH`/`DELETE`, no `GET` handler at all — every "View Project" deep link and the consolidated project detail view (AUDIT #117) 405'd for a single project fetch, even though the list route worked fine | `app/api/projects/[id]/route.ts` | Fixed | Added the missing `GET`, reusing the existing `mapProject()` shape and `requireRole(req, 'Team Member')` gate already used by sibling handlers in the file |
| 123 | HIGH — a fresh full-app audit sweep (8 parallel agents + 1 adversarial regression pass) found ~50 more routes across CRM/sales/automation, portal/client-data, and marketing/SEO/integrations with the same systemic missing-auth gap already fixed in prior passes (#82/#92/#100) — among them: `crm/contacts/[id]/timeline`, `sales-templates/[id]` GET/PATCH, `automations`/`automations/[id]/runs` GET, `tasks/[id]` PATCH/DELETE (no department-visibility check, unlike sibling GET), `time-entries/[id]` PATCH (any Team Member could self-approve/reject their own hours, bypassing the bulk-approve endpoint's Dept Manager gate), `files`/`files/download` (no per-file company-ownership check — any authenticated caller could mint a download URL for any path), `client-integrations`, `client-reports` POST (writes real GSC/GA4/GBP snapshot rows despite a comment claiming it was read-only), `dashboard` (trusted a client-supplied `unit`/`role` query param instead of the caller's real identity), `notifications`, `search`, `bookings/[id]` DELETE, `calendar/import-link`, all 6 `delivery/*` write routes, `drive`/`drive/upload`, `social/connections`(+`available`, which also leaked live Meta Page OAuth access tokens), `broadcasts`+3 sub-routes, `ai/{audit,recommendations,generate,insights,analyze-website}`, `brand-kits`, `seo-reports`, all 10 HubSpot integration GET/POST routes, the 3 marketing-OAuth `connect` routes, and 9 more Ads/GA4/GBP/GSC report/property routes | see above | Fixed | Same established pattern: `requireRole`/`requirePortalClient` added at each route's real gate level, inferred from sibling routes on the same resource. `courses`/`courses/[id]`/`courses/[id]/enrollments` were deliberately *relaxed* from staff-only to `getAuthenticatedEmail` — they back the portal-visible Sales Training page, and the strict gate meant it always rendered empty for real portal clients; `enrollments/[enrollmentId]` now also restricts `status`/`certificateId` fields to staff, since self-service progress updates only ever send `{progress, completed}` |
| 124 | HIGH — internal-only staff ticket notes (`messages[].isInternal`) shared the same array `GET /api/tickets`/`GET /api/tickets/[id]` returned to portal clients, with filtering only ever applied client-side in the staff UI — any portal client with API access could read every internal note ever left on their own tickets | `app/api/tickets/route.ts`, `app/api/tickets/[id]/route.ts` | Fixed | `mapTicket()` now filters `isInternal` messages server-side unless the caller is staff (`isStaffCaller()`). PATCH's append path required care: comparing a portal client's submitted array directly against the real (unfiltered) `existing` array would always fail once any internal note existed, since the client's own copy never contained them — fixed by comparing against `existing.filter(m => !m.isInternal)` instead, then appending new messages onto the real unfiltered array so internal notes already in the DB are never touched |
| 125 | MEDIUM — two real regressions found in this session's own earlier-shipped code: `lib/review-campaigns.ts`'s `dispatchReviewCampaign()` and the review-request token route both fired `.rpc('increment_review_campaign_counts', ...)` without checking the returned error (Supabase-js resolves `{data, error}` on RPC failure rather than rejecting) — a Postgres-level failure would silently freeze campaign counts forever with no log. Separately, the review-submission completion route had no atomic claim on `status`, so the same review-request link opened on two devices could both pass the read-only status check and double-count a review | `lib/review-campaigns.ts`, `app/api/reputation/review-request/[token]/route.ts` | Fixed | RPC calls now check and log `error`. Completion route's UPDATE now conditions on `.eq('status', 'pending')` and rejects with 400 if the row wasn't claimed (already completed), matching the atomic-claim pattern used elsewhere this session (`dispatchScheduledReviewCampaigns`, `resumePendingAutomationSteps`) |
| 126 | MEDIUM — `app/crm/pipeline/page.tsx`'s drag-and-drop, stage-advance, deal-update, delete, bulk-delete, new-deal, and activity-log handlers all did an optimistic local-state update and either ignored `res.ok` entirely or (`handleNewDeal`) called `res.json()` on an error-shaped response and crashed the page dereferencing `undefined.stage` | `app/crm/pipeline/page.tsx` | Fixed | All 7 handlers now check `res.ok`, revert the optimistic update, and toast the server's real error on failure; `handleNewDeal` checks `res.ok` before parsing the body, and its network-failure path now toasts an honest "not saved, please retry" instead of silently creating a phantom local-only deal |
| 127 | MEDIUM — `lib/crm-cascade.ts`'s company-delete block check (#96) and `app/api/crm/merge/route.ts`'s FK reassignment (#95, extended by #102 to cover renewals/maintenance_records) still omitted `app_tasks` and `tickets` (both carry a real `company_id` FK) and `company_files` (`company_id` is `NOT NULL ON DELETE CASCADE`, so a company delete silently destroyed its uploaded documents with zero warning, unlike every other real-record type which is blocked first) | `lib/crm-cascade.ts`, `app/api/crm/merge/route.ts` | Fixed | Added all 3 tables to the blocking-count check and merge reassignment; `company_files` uses a company_id-only match (no legacy name column exists to fall back on, unlike the other tables) |
| 128 | MEDIUM — `RenewalProposalSidebar`'s proposal-value calculator used `contract.value` directly as a monthly figure regardless of `billingStructure` — a Quarterly contract's renewal proposal showed 3x the real MRR, an Annual contract's showed 12x | `app/renewals/page.tsx` | Fixed | Now uses the existing `contractMonthlyValue()` helper (`lib/metrics.ts`), the same normalization already used elsewhere for MRR math |
| 129 | LOW — the default welcome-email and generic system-email footer templates linked to `{portal_url}/unsubscribe`/`{action_url}/unsubscribe` — bare paths with no matching route (only `/unsubscribe/[token]` exists, and nothing generates that token for these flows) — the one 404ing link real recipients got | `lib/templates/generate-welcome.ts`, `lib/email-templates.ts` | Fixed | Both now point at the real, already-working `/api/sequences/unsubscribe?email=...` mechanism (the same one broadcasts/sequences use) instead of the dead token-based page. `lib/email-templates.ts`'s `renderTemplate()` was confirmed to have zero live callers anywhere in the codebase — fixed anyway for consistency in the Settings editor's preview |
| 130 | MEDIUM — the atomic-column-update pattern from #66 (`adjust_sequence_counts()`) only ever got applied to 3 of 9 read-then-write call sites for `sequences.enrolled_count`/`active_count` — `execute/route.ts`'s completion branch was the worst of the six remaining: it fetches one `seq` snapshot per sequence *before* looping over every enrollment for that sequence, so two enrollments in the same sequence completing within one batch read the same stale count and one increment is lost deterministically within a single request, not just across concurrent ones. `webhooks/route.ts` (bounce + complaint), `unsubscribe/route.ts`, `reply-check/route.ts`, and `[id]/enrollments/route.ts`'s bulk DELETE had the same plain read-then-write | `app/api/sequences/{execute,webhooks,unsubscribe,reply-check}/route.ts`, `app/api/sequences/[id]/enrollments/route.ts` | Fixed | All 6 now call `adjust_sequence_counts()` via `.rpc()`. Extended the function with a new `p_completed_delta` param (execute/route.ts's completion path also increments `completed_count` alongside `active_count`) — required dropping the old 4-arg overload first, since `CREATE OR REPLACE` only replaces an identical parameter list and appending a new arg without dropping the old one would leave two ambiguous overloads. **Needs migration**: `supabase/migrations/add_sequence_completed_count_rpc.sql` |
| 131 | MEDIUM — `spawnRecurringTasks()`'s monthly-recurrence date math used raw `setMonth()`, which silently overflows for due-days 29–31 into whatever day the target month lands on (Jan 31 + 1mo → Mar 3, skipping February entirely) instead of clamping to the target month's last day. It also had no atomic claim, unlike sibling cron jobs in the same file — two overlapping ticks (now every 5 minutes via #16) could both read the same completed recurring task and both spawn a duplicate next occurrence | `app/api/cron/route.ts` | Fixed | Clamps to the target month's last day via `setDate(0)` when the day shifted. Added the same claim-before-work pattern already used by `resumePendingAutomationSteps`/`dispatchScheduledReviewCampaigns` in the same file: null out `recurrence` conditioned on it still being set before inserting the next occurrence |
| 132 | MEDIUM — `tickets/from-email`'s Gmail-to-ticket conversion did a check-then-act on `processed_emails`: check up front, create the ticket, record the bookkeeping row afterward. Two overlapping polls of the same account (this route is cron-triggered, now every 5 minutes via #16) could both pass the check and both create a ticket for the same message before either reached the record step. Separately, `processed_emails.gmail_message_id` is unique per Gmail *account*, not globally — the same physical email landing in two different staff members' inboxes (e.g. a shared alias forwarding to multiple people) got a distinct Gmail-assigned id per account, so it was never caught and created one ticket per account that received it. Also found in the same route: the created ticket's first message used `text`/`date`/`internal` field names instead of the real `body`/`timestamp`/`isInternal` the tickets UI actually reads (see #124), so every email-created ticket rendered with a blank message and no visible timestamp | `app/api/tickets/from-email/route.ts` | Fixed | Claims atomically by inserting into `processed_emails` (keyed on both the per-account `gmail_message_id` and a new cross-account-stable `rfc_message_id`, the RFC 5322 Message-ID header) before creating the ticket — a unique-constraint violation on either key means someone already claimed it. Fixed the message field names. **Needs migration**: `supabase/migrations/add_rfc_message_id_dedup.sql` |
| 133 | MEDIUM — the same optimistic-update-without-`res.ok`-check bug class as #126 across 4 more pages: `contracts/page.tsx` (`updateContractStatus`, `handleBulkDelete`), `proposals/page.tsx` (`handleBulkDelete`), `renewals/page.tsx` (`startRenewal`/`archiveRenewal` didn't revert on failure; `logRenewal` fabricated a fake local renewal with a synthetic id when the POST failed instead of reporting the real error), and `funnels/page.tsx`/`funnels/editor/page.tsx` (`deleteFunnel`/`updateFunnelName` always toasted success regardless of outcome; `reorderPage`'s two-PATCH swap failed completely silently; `publish()` proceeded to mark a funnel Published even when the preceding "save latest edits" PATCH had failed) | `app/contracts/page.tsx`, `app/proposals/page.tsx`, `app/renewals/page.tsx`, `app/funnels/page.tsx`, `app/funnels/editor/page.tsx` | Fixed | All now check `res.ok`, revert optimistic state, and toast the real error on failure |
| 134 | MEDIUM — `CompanyFilesTab`'s file-category dropdown only ever updated local React state — no `PATCH` handler existed on `/api/crm/companies/[id]/files` at all, so a category change looked like it saved but reverted on next page load. Its delete handler also never checked `res.ok`, always toasting "File deleted" success even on failure. Separately, `ContractPanel`'s "Linked Invoices" section filtered `invoices.filter(i => i.contractId === contract.id)`, but the one real caller of `POST /api/invoices` (the CSV bulk importer) never sends a `contractId` — every invoice in the app is created with `contract_id` null, so this filter always returned empty regardless of real data | `app/crm/companies/page.tsx`, `app/api/crm/companies/[id]/files/route.ts`, `app/contracts/page.tsx` | Fixed | Added the missing `PATCH` route and wired the dropdown to it with revert-on-failure; delete now reverts and surfaces the real error. Invoice filter now falls back to matching by company (id first, then name) when no `contractId` match exists |
| 135 | HIGH — the new-client wizard (`app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx`) called `POST /api/portal-clients` (creates the row + a Supabase Auth user with a discarded temp password, no email sent) and then `POST /api/portal-clients/invite` (the only route that generates a magic-link token and sends the real invite email) back to back — `/invite` always 409s when a `portal_clients` row for that email already exists, which it always did since the first call had just created it. The 409 was swallowed with `.catch(() => {})`, so the UI reported success while the new client had no working way to log in. Separately, both entry points sent a `services: string[]` array that `POST /api/portal-clients`'s insert never included at all — every new client's portal Services Hub read back `services: []` and showed "No active services" regardless of what was selected in the wizard | `app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx`, `app/api/portal-clients/route.ts`, `app/api/portal-clients/invite/route.ts` | Fixed | Call exactly one of the two routes per the pattern already used by `admin/portal-management`'s `addCompany()` — extended `/invite` to accept the `companyId`/`services`/`portalConfig` fields the plain POST supported so switching to it doesn't lose data. Fixed the missing `services` insert on the plain POST path too |
| 136 | MEDIUM — an earlier auth-hardening batch this session (#123 above) added `requireAdmin` to `GET /api/chatbots/[id]` to close a missing-auth gap for the admin dashboard — but that same route is also what `public/chatbot.js` (the embed script, loaded on external client sites with no session) calls to fetch its config and render the welcome message. Every anonymous site visitor's request now 401s; the 401 body still parses as JSON so the widget's `.catch()` never fires, and it silently falls back to its hardcoded default greeting — no visible error, but any customized `welcome_message` was dropped for every real visitor | `app/api/chatbots/[id]/route.ts`, `public/chatbot.js` | Fixed | Added a dedicated public route (`GET /api/chatbots/[id]/public`) returning only the display-safe fields (`welcome_message`, `brand_color`, `name`), gated on `active`, no auth — deliberately not just removing `requireAdmin` from `[id]` instead, since that returns the full row including `system_prompt`/`knowledge`/`settings`, real proprietary content that shouldn't be exposed to anyone who reads a chatbot id out of a public `<script data-chatbot-id>` tag |
| 137 | LOW — `ContractPanel`'s "Linked Project" section has the identical dead-filter bug as #134's invoices (`projects.find(p => p.contractId === contract.id)`), but nothing in the app ever creates a project with `contractId` set either (no automation action creates projects, and the projects creation form doesn't collect one) | `app/contracts/page.tsx` | Needs Decision | Not fixed in this pass — unlike invoices, projects don't reliably share a `company`/`companyId` fallback that would be unambiguous (a company can have several concurrent projects across different contracts), so the same "fall back to company match" fix used for #134 would be a much rougher approximation here. Worth a dedicated decision on whether to add a "link project to contract" UI affordance instead of a filter workaround |
| 138 | LOW — `app/api/drive/route.ts`'s `download` GET branch only requires `getAuthenticatedEmail` (any authenticated caller), with no per-file company-ownership check — flagged directly in the fix commit for #123 as a residual gap, since the Drive integration has no per-file owner/company column to check against today | `app/api/drive/route.ts` | Needs Decision | Would need either a new `company_id` tagging step when files are indexed from Drive, or accepting the current all-staff-can-download-any-indexed-file model as intentional (Drive files are typically already org-wide shared docs, not per-client-siloed) — a product decision, not fixed here |
| 139 | LOW — `lib/deal-score.ts` used an exact-match `CLOSED_STAGES` Set (`'Closed Won'`/`'Closed Lost'`) for its own closed-stage exclusion, a third inconsistent implementation alongside the two AUDIT #53 already reconciled (Pipeline vs Prospecting Workspace, both now `startsWith('Closed')`) — a custom stage name like "Closed — Duplicate" would be scored as still-open by deal-score math while both other screens correctly excluded it | `lib/deal-score.ts` | Fixed | Replaced the Set with an `isClosedStage()` helper matching the same `startsWith('Closed')` check #53 standardized on |
| 140 | CRITICAL — every real client/staff invite email links to `/portal/setup`, `/portal/auth/verify`, or `/setup-account`, but none were in `AppShell`'s `PUBLIC_ROUTES` — a brand-new invitee has no session yet, so the route guard's `!user && !isPublic` check bounced them to `/login` before the token/code-verification UI could ever render. Onboarding via any real invite link has been completely broken | `components/layout/AppShell.tsx` | Fixed | Added the 3 routes to `PUBLIC_ROUTES`; confirmed none of the three pages call `useAuth()`, so they're self-contained public flows |
| 141 | HIGH — `getAuthenticatedEmail()` (`lib/admin-auth.ts`) only verifies the caller holds a valid session — it never checks `team_members.status`, unlike the 5 auth paths already fixed this session for the suspended-staff-retains-access gap (suspending someone doesn't revoke their existing Supabase session). 4 routes built on this helper let a suspended employee keep acting as active staff: `gmail/send` (send real outbound email), `gmail/token` (read/rotate/clear that token), `ai/chat` (full internal AI-assistant + CRM tool-calling), and `portal-clients/[id]` PATCH (existence-only `isStaff` check granted full staff-level write access to any portal client record) | `app/api/gmail/send/route.ts`, `app/api/gmail/token/route.ts`, `app/api/ai/chat/route.ts`, `app/api/portal-clients/[id]/route.ts` | Fixed | All 4 now require `status === 'active'` in addition to a valid session |
| 142 | HIGH — `/api/auth/profile` and `/api/auth/auto-provision` sit under `proxy.ts`'s public `/api/auth/` prefix with zero internal auth check — just an `email` field in the POST body. Anyone could POST an arbitrary address to enumerate the full staff/client roster (name, role, isAdmin) and silently re-provision an active `team_members` row for any `@gravissmarketing.com` address that still has a live Supabase Auth user, even one an admin had deliberately deleted — letting a terminated employee who still controls their Google account regain full active access with zero audit trail | `app/api/auth/profile/route.ts`, `app/api/auth/auto-provision/route.ts` | Fixed | Both now require a Bearer token whose verified Supabase Auth email matches the email being looked up; updated the one real caller (`app/auth/confirm/page.tsx`) to send the session's `access_token` |
| 143 | HIGH — "Manual Trigger" was fully selectable/saveable in the automation builder, but `lib/automations-engine.ts`'s `TRIGGER_MAP` has no matching entry and nothing anywhere ever fires it (no button, no API route, no cron). A workflow built with it sat at `runs: 0` forever — same bug class AUDIT #14 already fixed for other dead trigger/action types, missed here | `app/automation/builder/page.tsx` | Fixed | Removed the option entirely, matching #14's established pattern |
| 144 | HIGH — `meta/report`, `meta/accounts`, `meta/status`, `linkedin/status`, and `google-marketing/status` had no auth check at all, unlike every sibling integration route (ads/ga4/gsc/gbp) — any authenticated caller, including a portal client, could pull live Meta ad-campaign data and ad-account lists | `app/api/integrations/meta/{report,accounts,status}/route.ts`, `app/api/integrations/linkedin/status/route.ts`, `app/api/integrations/google-marketing/status/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` to all 5 |
| 145 | HIGH — global search (Cmd+K) results linked with inconsistent query-param names (`?id=` for contacts/companies/tickets/tasks/proposals/contracts, `?deal=` for deals) that none of the destination pages actually read (they use `?open=`), and `projects`/`tickets`/`tasks`/`proposals` didn't read any deep-link param at all — clicking any of the 8 result types landed on the generic list page, never the specific record | `app/api/search/route.ts`, `app/proposals/page.tsx`, `app/tasks/page.tsx`, `app/tickets/page.tsx`, `app/projects/page.tsx` | Fixed | Standardized hrefs to `?open=` (projects use their dedicated `/projects/[id]` route instead, per AUDIT #117); added the `fetchAllPages`-backed load + `?open=` effect to the 4 pages that didn't have it, matching contacts/companies/pipeline/contracts |
| 146 | HIGH — `NewDealPanel` discarded `CompanySelect`'s `companyId` (only captured the name) — the only place deals are created in the app, so every deal ever created through the UI had `company_id` null by construction, silently defeating the id-first/name-fallback cascade/merge safety design (#95/#96/#102/#123) for the vast majority of real deals, not just legacy ones. The deal panel's "New Proposal" quick-create had the same gap despite the deal's resolved company object already being in scope | `components/crm/NewDealPanel.tsx`, `app/crm/pipeline/page.tsx` | Fixed | `CompanySelect`'s `companyId` is now captured and forwarded on both create paths |
| 147 | MEDIUM — contact delete had no related-record handling at all (unlike company delete, heavily hardened this session). `deals.contact_id` is `ON DELETE SET NULL`, but `deals.contact` is a separate denormalized `{id,name,email,phone,title}` blob the deal card renders directly with live `mailto:`/`tel:` links, never touched by the FK — a deleted contact kept showing on every deal that referenced them, indistinguishable from a live one, indefinitely | `app/api/crm/contacts/[id]/route.ts`, `app/crm/pipeline/page.tsx` | Fixed | Clears `deals.contact` alongside the delete; also made the deal panel's contact-detail rendering null-safe (it previously assumed `deal.contact` was always populated whenever `contactId` was set, which would have newly crashed once this fix could leave it null) |
| 148 | MEDIUM — `contracts.tsx`'s `updateContractStatus` revert-on-failure (shipped earlier this session, AUDIT #133) snapshotted the whole `localContracts` array before the async PATCH and replaced the whole array on failure — clobbering any other optimistic update that completed in the meantime (a second status change, a bulk-delete) instead of only reverting the one row being changed. Found by an adversarial regression-review agent | `app/contracts/page.tsx` | Fixed | Now snapshots and reverts only the one contract being changed |
| 149 | MEDIUM — `tickets/from-email`'s ticket-insert result was never checked, and a malformed Date header made `new Date(...).toISOString()` throw `RangeError`, escaping the loop and aborting the rest of that poll. Both left the atomic `processed_emails` claim (#132) in place with no ticket ever created, permanently dropping the email. Found by an adversarial regression-review agent | `app/api/tickets/from-email/route.ts` | Fixed | Guards the invalid date (falls back to today) and releases the claim (so the next poll retries) if the ticket insert itself fails, instead of silently losing the email forever |
| 150 | MEDIUM — `app/portal/workflow/page.tsx` treated `GET /api/delivery/workflow`'s response as a lone `WorkflowData` object and called `workflow?.steps.filter(...)` — the route is cursor-paginated and always returns an array (confirmed by contrast with `crm/delivery-dashboard/page.tsx`, which correctly treats it as `Workflow[]`), so this threw a TypeError on every render once a real client reached this page | `app/portal/workflow/page.tsx` | Fixed | Now reads the array and takes the first/most recent match for the requested company |
| 151 | MEDIUM — 6 more unbounded list endpoints past the Tier 1 pagination pass (roadmap A5), each with a frontend already rendering the response as "the complete list": `app/proposals/page.tsx`, `app/tasks/page.tsx`, `app/tickets/page.tsx`, `app/projects/page.tsx` (all raw single `fetch()`, despite their backend routes already being cursor-paginated from an earlier session), and `app/finance/page.tsx` (fixed `?limit=200` with no cursor follow-up, silently undercounting MRR/KPI math past 200 contracts) | `app/proposals/page.tsx`, `app/tasks/page.tsx`, `app/tickets/page.tsx`, `app/projects/page.tsx`, `app/finance/page.tsx` | Fixed | All switched to `fetchAllPages()` |
| 152 | HIGH — `app/api/push/subscribe/route.ts` does its own ad hoc identity check (`db.auth.getUser()` against only an `Authorization` header) instead of the app's real auth helpers — but the app's real session mechanism is a signed httpOnly `gravhub-auth` cookie, and neither real caller (`components/ui/PushNotificationBanner.tsx`, `lib/push-notifications.ts`) ever sends an `Authorization` header. Every real push subscription is stored with `user_id: 'anonymous'`, and `sendPushNotification()` filters by a real team-member id that can never match — every targeted push send silently returns `{sent: 0, failed: 0}` with no error anywhere. The onboarding banner's promise can never be fulfilled for any real user | `app/api/push/subscribe/route.ts`, `lib/push-notifications.ts`, `components/ui/PushNotificationBanner.tsx` | Fixed | `POST`/`DELETE` now gate on `requireRole(req, 'Client')` (the lowest tier, so both staff and portal clients qualify) and resolve the real caller via `getAuthUser()`, which goes through `lib/rbac.ts`'s `resolveVerifiedEmail()` — the same signed `gravhub-auth` cookie path every other authenticated route uses. Both real frontend callers already send same-origin `fetch()` calls with no `credentials` override, so the httpOnly cookie rides along automatically — no frontend change needed. `user_id` is now the real `team_members.id`, so `sendPushNotification()`'s targeting can actually match a row |
| 153 | HIGH — the real "Send Proposal" email's call-to-action links to the bare app root (`portalUrl`, i.e. the login page), not to the specific proposal — a dead end for any prospect who isn't yet a portal client (the normal case, since proposals typically precede becoming a client). A fully-built, working, public token-based proposal viewer already exists (`/proposal/[token]`, confirmed real per AUDIT #60) via `POST /api/proposals/[id]/share`, but no UI anywhere calls that route — it's dead code. Contracts have the same dead-end CTA (`app/api/email/send-contract/route.ts`) but severity there is Medium — a separate, genuinely working signature-request flow exists and is reachable from the UI | `app/api/email/send-proposal/route.ts`, `app/proposals/page.tsx`, `app/api/proposals/[id]/share/route.ts` | Needs Decision | Two ways to close this: wire `updateProposalStatus(id, 'Sent')` to call the existing `/share` route instead of (or before) `/api/email/send-proposal`, or change `send-proposal`'s CTA to a real per-proposal link. Deferred since it touches which of two overlapping "send" mechanisms is the intended one, not a pure bug fix |
| 154 | HIGH — a fresh full-app audit sweep found `/portal/**` (the fully-built multi-page client experience — contract/proposal e-signature & approval, SEO Strategy, delivery Workflow timeline, Help Center, 8 per-service pages, all correctly `requirePortalClient`-scoped) is structurally unreachable by any real, already-authenticated client. `AppShell`'s redirect logic (`user.userType === 'client' && pathname !== '/client'` → `router.replace('/client')`) unconditionally bounces every client session to `app/client/page.tsx`, a much smaller single-page tabbed dashboard with no equivalent for most of what `/portal/**` offers. `/portal/setup` and `/portal/auth/verify` (fixed as #140) are the only two `/portal/**` pages any real client flow actually still uses — the verify page's own code redirects into `/client` immediately after completing setup | `components/layout/AppShell.tsx`, `app/client/page.tsx`, `app/portal/**` | Needs Decision | Not a pure bug — this is a real product-direction question: is `/client` the deliberate, intentional replacement for the older multi-page `/portal/**` (in which case the rich portal pages are legacy and should be trimmed/removed), or did `/client` ship as a smaller MVP that hasn't caught up feature-wise (in which case clients should be routed back into `/portal/**`, or the missing capabilities should be ported into `/client`)? Flagging for a decision rather than guessing and either deleting built functionality or unilaterally changing the live client-facing routing |
| 155 | MEDIUM — `/portal/approvals`' Accept flow presents a full e-signature capture UI (typed cursive name or drawn HTML-canvas signature, "E-Signature Required... By signing, you agree to the terms") but never sends the captured signature anywhere — only `{status, clientSigned/respondedDate}` (a date string). `contracts`/`proposals` have no column to store one. This is a separate, non-functional parallel to the app's real e-signature system (`signature_requests.signature_data`, used by the public `/proposal/[token]`/`/sign/[token]` flows) | `app/portal/approvals/page.tsx`, `app/api/contracts/[id]/route.ts`, `app/api/proposals/[id]/route.ts` | Needs Decision | Currently moot in practice since #154 means real clients can't reach this page at all — worth resolving alongside that decision: either build real signature capture/storage for this flow, or remove the misleading signature-capture UI and just keep the accept/decline buttons |
| 156 | MEDIUM — `email-scheduler.ts`'s `processScheduledEmails()` does read-then-send-then-write with no atomic claim, unlike every sibling job in `app/api/cron/route.ts` (`resumePendingAutomationSteps`, `spawnRecurringTasks`, `dispatchScheduledReviewCampaigns`, all already using claim-before-work). Two overlapping cron ticks (GH Actions pings every 5 min, no `concurrency` group set) can both fetch the same due email, both send it (duplicate real email to the recipient), and for recurring emails both insert a duplicate "next occurrence" row | `lib/email-scheduler.ts` | Fixed | Added the same conditional-claim pattern used elsewhere: peek for due `pending` ids, then `UPDATE ... SET status='sending' WHERE id IN (...) AND status='pending' RETURNING *` — only rows this call actually flips get processed. **Needs migration**: `supabase/migrations/add_scheduled_emails_sending_status.sql` (adds `'sending'` to the status CHECK constraint and rescues any rows a crashed worker left stuck in `'sending'`) |
| 157 | MEDIUM — `checkTimeBasedTriggers()`'s invoice Sent→Overdue transition and the `overdue_3d_notified`/`renewal_30_notified`/`renewal_90_notified` flags (added by AUDIT #58 specifically to stop double-firing on overlapping cron ticks) are all set via a plain `.update({...}).eq('id', X)` with no `.eq('status'/'*_notified', <expected-prior-value>)` guard on the UPDATE itself — only the initial SELECT/in-memory filter checks the flag. Two overlapping ticks can still both read a row before either write commits and both call `fireAutomations`, duplicating overdue-invoice/renewal emails or tasks | `app/api/cron/route.ts` | Fixed | All 3 transitions (`status: 'Sent'→'Overdue'`, `overdue_3d_notified`, `renewal_30_notified`/`renewal_90_notified`) now condition the `UPDATE` itself on the pre-flag-set value and only fire the automation off the row actually returned by that conditional update — the same claim-before-fire pattern already used by `resumePendingAutomationSteps`/`spawnRecurringTasks` |
| 158 | MEDIUM — both booking backends (`/book/[slug]` and the newer `/go/book/[slug]`) do check-then-insert slot-conflict detection with no DB unique constraint or atomic claim. Two visitors booking the same slot within milliseconds can both pass the conflict check and create two confirmed, overlapping bookings, including duplicate Google Calendar events/Meet links | `app/api/bookings/route.ts`, `app/api/calendar/bookings/route.ts` | Fixed | Added partial unique indexes on `bookings(calendar_slug, date, start_time)` and `booking_type_bookings(booking_type_id, date, start_time)` (excluding cancelled rows) — the loser of a same-instant race now hits a real `23505` constraint violation instead of silently double-booking. Both routes catch that code and return 409 "slot already taken"; the `/book/[slug]` path also best-effort deletes the Google Calendar event it had already created before the DB insert failed. **Needs migration**: `supabase/migrations/add_booking_slot_unique_constraint.sql` |
| 159 | MEDIUM — `POST /api/time-entries` only requires `requireRole(req, 'Team Member')` (the lowest bar) and accepts `body.teamMember` verbatim with no check it matches the caller's own identity — the UI's own Log Time modal offers a free-choice dropdown of every team member, and there's no `created_by`/submitter column anywhere, so a Dept Manager approving an entry has no way to see who actually logged it | `app/api/time-entries/route.ts`, `app/time-tracking/page.tsx` | Needs Decision | Whether logging time on a colleague's behalf is ever a legitimate workflow (e.g. an admin backfilling for someone on leave) is a product question — if not, add an ownership check matching the caller's own identity; if so, add a real submitter/created_by column instead so approval at least shows who actually logged it |
| 160 | LOW — `app/api/sequences/attribution/route.ts` selects `deals.contact_email`/`deals.closed_at`, neither of which exists on `deals` per `schema.sql` (it has `contact_id`/`contact` jsonb and `close_date`) — the route 500s on every call. Confirmed dead/unreachable: no frontend page anywhere calls it, likely orphaned scaffolding for a "Revenue Attribution" panel never built into the sequences UI | `app/api/sequences/attribution/route.ts` | Fixed | Deleted rather than repaired — confirmed zero references anywhere in the codebase besides this file itself, and roadmap D4 (AUDIT.md "Plus, built this pass... D4 completion") already shipped a real, superior, actually-wired-up attribution feature (`/api/reports/attribution`, contact-linked UTM source tracking) that makes this old dead scaffold redundant even if it were fixed |
| 161 | LOW — `app/api/unsubscribe/[token]/route.ts`'s "reduce email frequency instead" option (prominently offered on the public unsubscribe page as an alternative to full unsubscribe, with a "done" success screen) only writes `email_preferences.frequency` on `crm_contacts` — a column read nowhere in the codebase (broadcasts/sequences suppression is entirely driven by the separate `sequence_suppression_list` table, which the real `unsubscribe` action branch correctly writes to). A recipient who explicitly asks for fewer emails keeps receiving the exact same volume, with the app having told them it was handled | `app/api/unsubscribe/[token]/route.ts`, `app/unsubscribe/[token]/page.tsx` | Fixed | Removed the option (frontend UI, state, and the backend's `reduce_frequency` action branch) rather than building real frequency-based throttling across the broadcast/sequence send paths — a materially bigger feature than this pass's other fixes. The "done" screen copy now accurately says "You've been unsubscribed" instead of the generic "preferences updated" |
| 162 | LOW — `components/layout/Header.tsx`'s always-visible search bars (desktop and mobile) are 100% decorative — no `onChange`/`onClick`/`onKeyDown` handlers at all. The real search only lives in the separate Cmd+K `CommandPalette`, which these visible search boxes never open or forward to | `components/layout/Header.tsx` | Fixed | Both inputs now open the real `CommandPalette` (a `gravhub:open-command-palette` window event `CommandPalette` listens for alongside its own Cmd+K handler) instead of accepting typed text themselves; the desktop input is `readOnly` and blurs itself on focus so it can never look like a second, separate search box. Removed the now-fully-redundant mobile expandable inline search panel (and its dead `searchOpen` state/`X` import) since the mobile search button now opens the same real palette directly |
| 163 | LOW — `app/admin/sops/page.tsx`, `app/admin/scheduled-emails/page.tsx`, and `app/admin/new-client/page.tsx` render their full interactive management UI to any authenticated user with no client-side `user?.isAdmin` check, unlike sibling admin pages which all gate on it. Backend routes correctly enforce their own role gates, so this isn't independently exploitable — just a confusing UX gap where a non-privileged user sees a fully "live" admin page that only fails on submit | `app/admin/sops/page.tsx`, `app/admin/scheduled-emails/page.tsx`, `app/admin/new-client/page.tsx` | Fixed | Added the same `useAuth()` + `useEffect` → `router.replace('/admin')` gate already used by `app/admin/portal-management/page.tsx`, verbatim, to all 3 pages |
| 164 | LOW — Settings' Team tab (role dropdown up to Super Admin, Remove button, edit-anyone's-email-signature) is visible and interactive for every authenticated user — only the Navigation tab is gated on `user?.isAdmin`. Backend `PUT`/`DELETE /api/team-members/[id]` correctly require Super Admin, so not exploitable, but non-admin staff see a misleading privileged-looking UI over the entire team roster | `app/settings/page.tsx` | Fixed | Tab bar's existing `tabs.filter(tab => tab !== 'Navigation' || user?.isAdmin)` now also excludes `'Team'` the same way — confirmed no `?tab=team` deep-link exists in the page's URL-param initializer, so hiding it from the tab bar is sufficient, matching how `'Navigation'` was already handled with no separate content-level check either |
| 165 | LOW — `app/calendar/page.tsx` hardcodes `timezone: 'America/Chicago'` when mapping `booking_type_bookings` into the shared display shape, instead of the real per-calendar timezone the backend already correctly resolves (AUDIT #97/#104) — cosmetic label mismatch only, not a scheduling bug | `app/calendar/page.tsx` | Fixed | `booking_types` has no timezone column of its own — confirmed the internal-listing branch of `GET /api/calendar/bookings` (which powers this page) never returned one at all, unlike the route's own slot-check/booking-creation branches, which already resolve "any connected staff Google Calendar's timezone, else America/Chicago." Applied that same resolution to the listing branch and stamped it onto each returned row (kept the response a bare array — the frontend's `Array.isArray(d)` check depends on that) instead of hardcoding the fallback client-side |
| 166 | LOW — `app/api/notifications/route.ts`'s `TYPE_META['deal'].href` points at `/deals`, a route that doesn't exist (the real page is `/crm/pipeline`) — currently unreachable since no code path inserts a `crm_activities` row with `type: 'deal'`, but would 404 if one ever did | `app/api/notifications/route.ts` | Fixed | One-line fix to `/crm/pipeline` |
| 167 | LOW — `sales_templates.usage_count`'s increment is a plain read-then-write in both `app/sales-enablement/page.tsx`'s `applyTemplate` and the POST route's alternate `useTemplate` path, unlike the atomic RPC pattern already used for other counters this session (`adjust_sequence_counts`, `next_rotation_index`) — two reps copying the same template in the same render window lose one increment. Cosmetic, display-only usage counter, no gating logic depends on it | `app/api/sales-templates/route.ts`, `app/api/sales-templates/[id]/route.ts`, `app/sales-enablement/page.tsx` | Fixed | Added `increment_template_usage()` (same atomic-UPDATE-under-the-row's-own-lock pattern as `next_rotation_index()`/`adjust_sequence_counts()`). `applyTemplate` in the frontend now calls the existing `POST .../sales-templates` `useTemplate` path (already fixed to use the RPC) instead of PATCHing a client-computed `usageCount + 1`. **Needs migration**: `supabase/migrations/add_sales_templates_usage_count_rpc.sql` |
| 168 | LOW — accepting a contract addendum (Value Change / Term Extension, with a `value_delta`/`term_delta_months`) flips its own status to Accepted and the UI shows "Client accepted," but the PATCH handler never applies that delta to the parent contract's `value`/`duration`/`renewal_date` — confirmed no other consumer of `contract_addendums` exists anywhere. The contract's real value used everywhere else (dashboards, MRR math, renewals) never reflects an addendum a rep believes they just got signed off on | `app/api/contracts/[id]/addendums/route.ts`, `app/contracts/page.tsx` | Needs Decision | Applying the delta automatically on Accept is the obvious fix, but needs a decision on exact semantics (does a term extension push `renewal_date` by `term_delta_months`, and does that also need to update `duration`? are multiple accepted addendums cumulative?) — not guessed at in this pass |
| 169 | HIGH — `app/api/email/portal-invite/route.ts` had zero auth check at all, unlike its named sibling `app/api/portal-clients/invite/route.ts` (which correctly requires `requireAdmin`). Any authenticated caller, including the lowest-tier portal-client login, could POST an arbitrary `email` to (a) overwrite that `portal_clients` row's `verification_code`/`verification_expires`, (b) get a branded "your portal is ready" email containing the fresh code sent to that address, and (c) mass-email every admin team member an attacker-controlled "New portal client invited" notification. Found by a coverage-gap sweep specifically targeting routes never mentioned in any prior audit pass | `app/api/email/portal-invite/route.ts` | Fixed | Added `requireAdmin(req)`, matching its sibling |
| 170 | HIGH — `app/api/email/portal-notification/route.ts` had zero auth check — an authenticated open email relay accepting `{to, clientName, title, message, link}` verbatim and sending a fully attacker-controlled branded email to any address via the org's verified sending domain, same severity class as the already-fixed AUDIT #82. Its only caller, `lib/portal-notify.ts`'s `notifyPortalClient()`, turned out to itself have zero callers anywhere in the codebase — the whole notification pipeline is currently orphaned — but the route is still live and reachable regardless | `app/api/email/portal-notification/route.ts` | Fixed | Added `requireAdmin(req)`. Since `notifyPortalClient()` has no current callers, this doesn't break anything live; if it's wired up in the future, the caller will need to thread through staff-level auth (or the route should be refactored to a direct function call instead of a self-referencing HTTP round-trip) |
| 171 | MEDIUM — `app/api/portal-clients/reset-password/route.ts` (deliberately public by design, a "forgot password" pattern) calls Supabase's `generateLink({type: 'magiclink', ...})` directly on the raw request-body email with no check the email belongs to any known client — unlike its sibling `magic-link/route.ts`, which looks up the `portal_clients` row first and 404s if none exists. Per Supabase's `generateLink` semantics, this can silently provision a brand-new Auth user for an email with no client account. Confirmed zero frontend callers anywhere — `app/login/page.tsx`'s magic-link flow calls the safe sibling route instead | `app/api/portal-clients/reset-password/route.ts`, `proxy.ts` | Fixed | Deleted rather than patched — genuinely redundant with the already-correct, actually-used `magic-link` route, and fixing a dead route just leaves dead code with less risk instead of removing it. Removed its now-stale entry from `proxy.ts`'s `PUBLIC_PREFIXES` |
| 172 | MEDIUM-HIGH — `app/api/email/scheduled/route.ts` (GET/POST/DELETE) gates on `requireRole(req, 'Team Member')`, the lowest real-staff tier — far below every sibling email-sending/broadcast route (`broadcasts/[id]/send`, `email/invite`, etc., all `'Leadership'`). GET returns every scheduled email org-wide (full HTML body, recipient, error text) to any Team Member; POST lets any Team Member schedule a new email to an arbitrary address with arbitrary subject/HTML and an arbitrary send time — functionally the same "open relay through the org's verified domain" capability AUDIT #82 rated CRITICAL, just gated behind the lowest authenticated staff tier instead of nothing | `app/api/email/scheduled/route.ts` | Fixed | Bumped all 3 handlers to `requireRole(req, 'Leadership')`, matching sibling routes |
| 173 | MEDIUM — `app/admin/scheduled-emails/page.tsx`'s Retry button has no in-flight guard — `scheduleEmail()` has no idempotency key, so two clicks landing before the row refreshes independently POST two new pending rows, and the atomic-claim cron worker (confirmed intact) genuinely sends both — a duplicate real email to the recipient | `app/admin/scheduled-emails/page.tsx` | Fixed | Added a `retryingId` state guarding both the handler (early-return while a retry is in flight) and the button (`disabled` + spin animation) |
| 174 | LOW — the WordPress plugin's live-readability AJAX call (`gravhubRunReadabilityCheck()`, shipped this session) had no `.fail()` handler, unlike the sibling internal-link-suggestions feature shipped in the same commit — a transient failure (expired nonce on a long editing session, a 5xx, a network blip) left the Readability tab silently stuck on stale data with no indication anything was wrong. Found by an adversarial regression pass on this session's own newest commits | `wordpress/gravhub-seo/includes/class-seo-metabox.php` | Fixed | Added a `.fail()` handler that resets the dedupe guard (so the next content change or scheduled tick retries) and shows an inline "will retry on your next edit" message instead of going silent |
| 175 | LOW — the WordPress plugin's redirect-CSV export (shipped this session) had no formula-injection guard — `from_path`/`to_path` were written via `fputcsv` with no leading-`'` prefix for cells starting with `=`/`+`/`-`/`@`, the classic CSV/formula-injection risk if a crafted `to_path` (settable via CSV import) is later opened in Excel by another staff member | `wordpress/gravhub-seo/includes/class-redirect-manager.php` | Fixed | Added a `csv_safe()` helper applied to both exported columns |
| 176 | HIGH — `app/admin/audit-log/page.tsx` fetches exactly one `GET /api/audit-logs?limit=5000` on mount with no cursor logic, unlike the codebase's own `lib/pagination.ts`/`lib/fetch-all-pages.ts` (built specifically for this bug class, already applied to deals/contacts/proposals/tasks/tickets/projects/finance per AUDIT #48/#76/#151). Past 5,000 rows, the pager, the User/Module filter dropdowns, the search box, and the "N entries" count all silently omit older data with zero indication. AUDIT.md's own line 201 "coverage note" claiming a dedicated audit-log pagination survey had happened turned out to be a red herring — that survey's actual verified list never mentions this page. Export CSV compounds this: it serializes the client-filtered in-memory array, never hits the network, so filtering by an old date range and exporting can silently produce a CSV missing rows that genuinely exist | `app/admin/audit-log/page.tsx`, `app/api/audit-logs/route.ts` | Fixed | `GET /api/audit-logs` rewritten to real cursor pagination (`parsePagination`/`applyCursor`/`slicePage`/`paginatedJson`, the same primitives #48/#151 already use), replacing the flat `limit=5000`. The page now calls `fetchAllPages()` instead of one capped fetch, so the pager/filters/search/count and Export CSV (still client-side, but now built from the complete fetched set, not a 5,000-row snapshot) all see the full dataset |
| 177 | HIGH — the audit trail's "User" attribution is fabricated for nearly the entire log — `logAudit({userName, ...})` takes a real actor name, but a grep of every call site (~48+ matches) shows the overwhelming majority hardcode the literal string `'system'` or `'admin'` (`crm/companies/[id]`, `deals/[id]`, `contracts/[id]`, `social/connections`, `portal-clients`, `team-members/[id]`, dozens more) even though every one of those routes already resolves the real caller's verified identity via `requireRole`/`requireAdmin` before acting. `crm/bulk-delete` is worse — it omits `user_name` entirely, landing on the DB default `''`. The one correct exception is the portal-client magic-link verify route, which logs the client's own email for their own login. Net effect: the audit log's primary "who did this" signal resolves to two generic strings for nearly the whole log | ~48 call sites across `app/api/**` | Fixed | 62 of 63 identified call sites updated to `getAuthUser(req)` → `userName: actor?.name || actor?.email || 'system'`, the same pattern already used correctly elsewhere. The one deliberate exception: `app/api/portal-clients/magic-link/route.ts`, which is genuinely pre-session (no authenticated caller exists yet at that point) and already logs the client's own email correctly. Two integration tests (`tests/integration/audits-delete.test.ts`, `tests/integration/contracts.test.ts`) had their `@/lib/rbac` mocks updated to include `getAuthUser` after this change, since their routes now call it |
| 178 | HIGH — the single most security-sensitive action class — granting/revoking admin — isn't logged at all. `app/api/team-members/route.ts` POST (creates a team member, sets `role`/`isAdmin` directly on insert — the exact "plant a Super Admin" vector AUDIT #82 closed) and PATCH (suspend/reinstate/soft-delete/schedule access) never call `logAudit`. `app/api/team-members/[id]/route.ts` PUT (promotes/demotes an existing member's role/admin/status) also never logs — only its DELETE does, and that one has finding #177's fake-`'system'`-attribution problem too. Privilege escalation, suspension, and reinstatement leave zero trace in the log this page displays | `app/api/team-members/route.ts`, `app/api/team-members/[id]/route.ts` | Fixed | Added real, correctly-attributed `logAudit()` calls to all three: POST (`team_member_created`, `warning` if `is_admin`), PATCH (`team_member_${action}`), and PUT (`team_member_updated`, `warning` if role/isAdmin changed) |
| 179 | LOW — `app/admin/audit-log/page.tsx`'s fetch failure and a genuinely-empty log render identically ("No audit entries found") — an admin checking the log during an actual API/DB outage sees a false "all clear" instead of a visible error | `app/admin/audit-log/page.tsx` | Fixed | Added a `loadFailed` state (set when a first-page probe fetch fails) and a distinct red error row ("Failed to load the audit log — this is an error, not an empty log" with a Retry button), checked before the empty-state row |
| 180 | HIGH — `app/admin/new-client/page.tsx`'s Agreement step (Contract ID, Agreement Start/End, Monthly Value) is collected and shown back in the Review step as if it'll be saved, but `handleSubmit` never includes any of the four fields in any request body (company, contacts, portal-clients/invite, or delivery/workflow) — silently discarded on submit every time | `app/admin/new-client/page.tsx` | Fixed | Resolved the "Needs Decision" by having `handleSubmit` create a real `contracts` row (status `Draft`, `billingStructure: 'Monthly'`) whenever the admin filled in any of Agreement Start/End/Monthly Value — computed `duration` in months from the two dates when both are present. Left unresolved by design: Contract ID has no matching column on `contracts` and no schema change was made for it (a blind PATCH-by-typed-id could 404 against an unrelated real contract) — it's the one field of the four still not persisted, called out inline in the code |
| 181 | MEDIUM-HIGH — `app/admin/new-client/page.tsx`'s wizard Service dropdown (`SEO, PPC, Web Design, Social Media, Email Marketing, Content Creation, Sales Training, Marketing Strategy` — the portal-facing taxonomy) is sent as-is to `POST /api/delivery/workflow`, whose `serviceType` enum is the separate billing/delivery catalog in `lib/services.ts`. `Web Design`, `Content Creation`, and `Marketing Strategy` aren't in that enum at all, and neither `page.tsx` nor `components/admin/NewClientModal.tsx` checks `res.ok` on this call — selecting any of those 3 as the client's service silently fails to create a delivery-onboarding workflow, with the wizard still reporting full success | `app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx`, `app/api/delivery/workflow/route.ts` | Fixed | Added `lib/services.ts`'s new `PORTAL_TO_CATALOG_SERVICE`/`toCatalogServiceValue()` mapping (`Web Design` → `Website Build`, `Content Creation` → `Content Marketing`, `Marketing Strategy` → `Marketing`, others pass through unchanged) applied to the `serviceType` sent to `/api/delivery/workflow` in both `page.tsx` and `NewClientModal.tsx`, plus `res.ok` checks on that call in both places |
| 182 | MEDIUM — `app/admin/new-client/page.tsx`/`components/admin/NewClientModal.tsx`'s `POST /api/crm/contacts` call result is never checked — the route can legitimately 409 (duplicate email/name+company) or 400, silently failing to create the primary contact while company/portal-login/delivery-workflow creation all proceed and the wizard reports success | `app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx` | Fixed | Added `res.ok` checks that throw the route's real error message (or a fallback), in both call sites |
| 183 | MEDIUM — `app/admin/new-client/page.tsx`'s Team Assignment step's Account Manager is only sent as `owner` on the `/api/crm/contacts` POST, never included in the `/api/crm/companies` POST/PATCH body — but "Owner" is a first-class, prominently-surfaced field on the Company itself elsewhere in the app (sortable column, detail-page row, bulk-reassign tool). Every company created via this wizard has a blank `owner`, contradicting the step's stated purpose | `app/admin/new-client/page.tsx` | Fixed | Added `owner: data.accountManager \|\| undefined` to the company create/update body |
| 184 | HIGH — `app/admin/portal-management/page.tsx`'s Visibility toggle section (Agreement/Renewal Info/Invoices/SEO Strategy/Reports) writes nested camelCase keys (`portalConfig.visibility.showAgreement` etc.), but its consumers — `components/portal/ClientDashboard.tsx` and `app/portal/reports/page.tsx` — read flat snake_case (`config.show_agreement` etc.). None of the 5 toggles have ever had any real effect; the UI shows a "Saved" checkmark on every change regardless. Custom Welcome Message has the identical bug (`portalConfig.welcomeMessage` written, `config.custom_welcome_message` read) — clients always see the generic fallback greeting, never a configured one | `app/admin/portal-management/page.tsx`, `components/portal/ClientDashboard.tsx`, `app/portal/reports/page.tsx` | Fixed | Fixed the key mismatch itself — both consumers' `PortalConfig` interfaces (and every usage site) rewritten to the nested camelCase `visibility: {showAgreement, showInvoices, showSeoStrategy, showReports}` + `welcomeMessage` shape the admin page actually writes, matching `/api/portal/seo/route.ts`'s already-correct reference shape. Fixed regardless of #154's separate `/client` vs `/portal/**` reachability question, per this pass's triage rule: a safe, reversible, symmetric key-mismatch fix doesn't need that product decision resolved first — if `/portal/**` turns out to be dead code, this fix is inert, not harmful. #154 itself remains open |
| 185 | HIGH — `app/admin/portal-management/page.tsx`'s per-service "Strategy Scheduling" panel (Update Frequency, Last Updated, Next Update Due, Strategy Content — with an "Overdue" badge) writes `services_config[key].frequency/.last_updated/.strategy`, none of which are read by any client-facing page (checked the services hub and all per-service subpages — each sources its content from unrelated, already-correct endpoints instead). The "Overdue" badge is a pure admin-side-only computation over data no client ever sees | `app/admin/portal-management/page.tsx` | Needs Decision | Same root situation as #184 (this panel also only matters once `/portal/**` reachability, #154, is resolved) plus its own separate question: was a real client-facing consumer for this panel ever intended, or should the panel be removed as pure admin-side scaffolding? Not guessed at in this pass |
| 186 | MEDIUM — `app/admin/portal-management/page.tsx`'s invite-email send (`inviteMember`) never checks `sendEmail()`'s `{success, error}` result in `app/api/portal-clients/invite/route.ts` — if Resend fails (bad key, invalid recipient, rate limit), the route still creates the `portal_clients` row + magic token and returns 201, and the admin UI shows "Invite sent to {email}" regardless. The invitee has no working link and nobody is told | `app/api/portal-clients/invite/route.ts` | Fixed | The route now captures `sendEmail()`'s result, returns `emailSent: boolean` in its response (the account + magic-link token are still committed either way — a failed send shouldn't roll those back), and logs `portal_invite_email_failed`/`warning` instead of `portal_invite_sent`/`action` on failure. `inviteMember()` and `addCompany()` in the admin page both now check `data.emailSent === false` and show a distinct error toast instead of the blanket success message |
| 187 | MEDIUM — `app/admin/portal-management/page.tsx` groups portal clients into "companies" by a free-text string match (`client.company`), and `PATCH /api/portal-clients/company-config` updates every row whose `company` text matches — if two distinct real companies happen to share an identical name string (a data-entry collision, not DB-unique), editing one's portal config (services, visibility, logo, welcome message) silently pushes the same change onto the other's clients too. Admin-gated, not externally exploitable, but a real cross-tenant data-integrity risk baked into the design | `app/admin/portal-management/page.tsx`, `app/api/portal-clients/company-config/route.ts` | Open | Not fixed this pass — would need `company_id`-based grouping/scoping instead of a name match, a larger refactor of how `portal_clients` relates to `crm_companies` |
| 188 | LOW-MEDIUM — `app/admin/portal-management/page.tsx`'s "Add Company" Service Type dropdown only sets the legacy `service` text column via `/api/portal-clients` or `/invite` — nothing populates `services`/`services_config`, so a newly-added company shows 0 active services in the client portal until an admin manually reopens the panel and toggles it in the Services grid | `app/admin/portal-management/page.tsx` | Fixed | `addCompany()`'s invite payload now also sends `services: [selected service]` and `portalConfig: {services_config: {[service]: {enabled: true}}}` — the latter is what the actual consumer (the Services grid / client-facing pages) reads, not the flat `services` array alone |
| 189 | LOW — `app/admin/portal-management/page.tsx`'s service-toggle and visibility-toggle autosaves each independently PATCH the *entire* `portal_config` blob with no merge/version check — rapid successive toggles, or a manual "Save Configuration" click while an autosave is in flight, could let a stale write clobber a newer one. Separately, Logo URL/Brand Color/Welcome Message/legacy SEO Strategy/per-service Frequency fields are pure local state with no autosave and no "unsaved changes" warning (only the Reports section warns) — collapsing the panel or navigating away silently loses them | `app/admin/portal-management/page.tsx` | Fixed | Serialized per-company autosaves/manual-saves into a save queue that always reads the LATEST config at execution time (via a `companiesRef`), so a rapid second toggle's save can't be overtaken by a slower earlier request landing after it. Added a `beforeunload` warning plus a visible "Unsaved changes" badge near Save Configuration for the local-only fields (Logo URL/Brand Color/Welcome Message/SEO Strategy/Frequency) that don't autosave |
| 190 | MEDIUM — the WordPress plugin's new broken-link scanner (`class-broken-link-scanner.php`, shipped this session) has no private-IP/loopback/link-local blocklist on the URLs it fetches — only excludes the site's own host. Any post author who can publish content with an `<a href>` can cause the WordPress server to issue real HTTP requests (weekly cron, or immediately via "Scan Now") against arbitrary internal targets. Impact is bounded (only status code/error string persisted, not response bodies), and post authors already control page content, so this is plausibly an intentional tradeoff rather than a bug — flagging per the adversarial review's explicit brief rather than silently accepting it | `wordpress/gravhub-seo/includes/class-broken-link-scanner.php` | Fixed | `check_link()` switched from `wp_remote_head`/`wp_remote_get` to `wp_safe_remote_head`/`wp_safe_remote_get` — WP core's SSRF-safe variants, which resolve the target host and reject loopback/private/link-local ranges (including the `169.254.169.254` cloud metadata address) via the `reject_unsafe_urls` request arg, rather than a hand-rolled blocklist |
| 191 | LOW — the WordPress plugin's broken-link scanner never checks `$wpdb->insert()`'s return value in `record_result()` — if the weekly cron and a manual "Scan Now" run concurrently for the same `(post_id, url)`, the `UNIQUE KEY post_url` constraint can reject a second concurrent insert and the failure is silently swallowed (self-heals on the next scan, so low impact) | `wordpress/gravhub-seo/includes/class-broken-link-scanner.php` | Fixed | Captures the insert's return value and `error_log()`s a message (including `$wpdb->last_error`) on failure instead of discarding it silently |
| 192 | LOW — the WordPress plugin's CSV redirect importer (`handle_import_redirects()`) has no row/size cap — a very large file can exceed `max_execution_time` mid-loop, and since there's no per-chunk commit, a timeout kills the request before the imported/skipped-count redirect is reached, leaving a partially-applied import with zero feedback | `wordpress/gravhub-seo/includes/class-redirect-manager.php` | Fixed | Added a `MAX_IMPORT_ROWS` (5,000) cap — the loop stops reading past it instead of running unbounded, and the success notice now shows a "file had more rows than the limit, only the first N were processed" message when the cap was hit. Full chunked/resumable batching for arbitrarily large files was not built — 5,000 rows comfortably fits one request for this use case |
| 193 | LOW — the WordPress plugin's `GravHub_Broken_Link_Scanner::get_broken_link_count()` (modeled explicitly on `get_redirect_count()`/`get_404_count()`, per its own docblock) was never actually wired into `class-admin-page.php`'s dashboard notification feed/module grid, unlike its two siblings — the broken-links count never surfaces on the main dashboard, purely a completeness gap | `wordpress/gravhub-seo/includes/class-broken-link-scanner.php`, `wordpress/gravhub-seo/admin/class-admin-page.php` | Fixed | `GravHub_Admin_Page` now takes the broken-link scanner instance as a constructor dependency (wired in `gravhub-seo.php`) and `build_notifications()` surfaces a "N outbound link(s) are broken" notice the same way `get_redirect_count()`/`get_404_count()` already do, linking to the Redirects page's Broken Outbound Links section |
| 194 | CRITICAL — `app/api/calendar/callback/route.ts` (public, no auth check) derives which staff member's `calendar_settings` row to overwrite entirely from an unsigned, attacker-decodable `state` param (`base64(JSON({userEmail, userName, slug}))`, set by `app/api/calendar/auth/route.ts`). Google's OAuth flow round-trips `state` verbatim with no server-side binding to who started the flow. An attacker with zero GravHub access can build their own Google OAuth consent URL with `state` set to a victim staff email, consent with their OWN Google account, and get the callback to upsert the attacker's refresh token into the victim's row (`onConflict: 'user_email'`) — silently hijacking every future booking on that staff member's public `/book/{slug}` page onto the attacker's calendar. This is the same class as the already-fixed #101 (userEmail spoofing), but #101 only hardened the route that *issues* `state`, not the callback that actually trusts it | `app/api/calendar/callback/route.ts`, `app/api/calendar/auth/route.ts` | Fixed | Added `issueOAuthStateWithPayload`/`verifyOAuthStateWithPayload` to `lib/oauth-state.ts` — the same httpOnly-cookie-bound-nonce pattern #84 already uses for the other 4 OAuth integrations, extended to also carry a small non-sensitive payload (`slug`) through the redirect. `state` is now `<nonce>.<base64url(payload)>`; the callback verifies the nonce half against the cookie `/auth` set (proving this is a continuation of a flow this server itself issued, in the same browser — an attacker building their own consent URL never gets that cookie set) and now re-derives `userEmail`/`userName` from the caller's own verified session (`getAuthUser`) at callback time rather than trusting anything in the state payload at all — even a stolen/replayed valid nonce can no longer redirect a different victim's tokens, since identity isn't sourced from the round-trip anymore |
| 195 | CRITICAL — the admin dashboard's "Pending Portal Approvals" queue implies a real two-step gate (client completes setup → `pending_approval: true` → admin must approve before real access), but NONE of the 3 real session-issuing paths for portal clients ever check `pending_approval`: `app/api/auth/google-verify/route.ts` (client branch), `app/api/auth/session/route.ts`, and `app/api/portal-clients/magic-link/verify/route.ts` (which additionally unconditionally sets `access: 'Active'` on every successful token redemption, and never checks `setup_completed` either) — all three only check `access === 'Disabled'`. Downstream, `lib/portal-auth.ts`'s `requirePortalClient()` (the actual authorization gate on nearly every portal-scoped route) has the same gap. Contrast: `app/api/auth/setup-account/route.ts` (team members) correctly does `if (member.pending_approval) return 403` — the codebase already has this exact pattern, it was just never applied to `portal_clients` | `app/api/auth/google-verify/route.ts`, `app/api/auth/session/route.ts`, `app/api/portal-clients/magic-link/verify/route.ts`, `lib/portal-auth.ts` | Fixed | Added a `pending_approval` check (403, "awaiting admin approval") to all 3 session-issuing paths and to `requirePortalClient()` as defense-in-depth, mirroring `setup-account`'s existing team-member pattern exactly. `magic-link/verify` specifically now blocks *before* its unconditional `access: 'Active'` write, so a pending client can no longer self-activate by redeeming any magic link they hold — admin-issued invites (the normal way a magic link exists) are unaffected since they never set `pending_approval` in the first place, only the self-serve-setup-then-awaiting-approval path is blocked |
| 196 | HIGH — the newer `/go/book` flow's `POST /api/calendar/bookings` only checks other rows in `booking_type_bookings` for the same `booking_type_id` before confirming — unlike its own `GET` (slot-check), it never re-checks live Google Calendar busy time or legacy/imported events. Since this is a public, unauthenticated POST, a caller who bypasses the slot-picker UI can get a `confirmed` booking (real Google Calendar event + confirmation email) for a slot that's already busy on the connected staff calendar, and two different `booking_types` can double-book the identical slot with no cross-type detection at write time | `app/api/calendar/bookings/route.ts` | Fixed | `POST /api/calendar/bookings` now re-checks legacy/imported events (`bookings` table, `calendar_slug='imported'`) and live Google Calendar busy time before confirming, mirroring the exact same conflict logic `GET`'s slot picker already applies — a Google API failure fails open (matches GET's own behavior; the DB-level unique constraint still backstops same-type double-booking either way). |
| 197 | HIGH — recurring-task respawn (`spawnRecurringTasks` in `app/api/cron/route.ts`) copies `title, description, category, priority, status, company, assigned_to, due_date, linked_id, team_service_line, recurrence, parent_task_id` onto the next occurrence but drops `department`, `company_id`, `project_id`, `section`, `sort_order`. Since `department IS NULL` is treated as globally-visible/editable by both `GET /api/tasks` and `canTouchTask` (`app/api/tasks/[id]/route.ts`), a recurring Finance/Operations task that was deliberately department-scoped (per the #123 fix) silently loses that scoping every time it respawns after completion — any Team Member can then see/edit/delete it. `project_id`/`section` loss also means a recurring task on a project kanban board disappears from that board on every recurrence | `app/api/cron/route.ts` (`spawnRecurringTasks`) | Fixed | `spawnRecurringTasks()` now copies `department`/`company_id`/`project_id`/`section`/`sort_order` onto the respawned task alongside the fields it already carried forward. |
| 198 | HIGH — `app/api/portal-clients/verify-code/route.ts` and `complete-setup/route.ts` (both in `proxy.ts`'s public prefixes) have no rate limiting on a brute-forceable 6-digit code (~900k combinations, 48h validity) — unlike the parallel *staff* onboarding flow (`/api/auth/verify-code`/`setup-account`), which `proxy.ts` explicitly throttles at 15/hour/IP specifically because success sets a real account password. `complete-setup` does exactly that for portal clients (`db.auth.admin.updateUserById`) with no equivalent throttle — a live, unrate-limited account-takeover path against any known/guessed client email | `app/api/portal-clients/verify-code/route.ts`, `app/api/portal-clients/complete-setup/route.ts`, `proxy.ts` | Fixed | Added the same `memoryLimited()` throttle (15/hour/IP) `proxy.ts` already applies to the staff onboarding equivalent, to `POST /api/portal-clients/verify-code` and `complete-setup`. |
| 199 | HIGH — 3 mutually-incompatible ticket-message JSON shapes exist across the codebase: `/client` (the only reachable real client ticket-creation path) sends `{from, body, date}`; `app/tickets/page.tsx` (staff viewer) unconditionally reads `msg.author.split(' ')...` — `author`, not `from`; the unreachable `app/portal/tickets/page.tsx` uses a third shape (`{sender, text, date}`). Any ticket a real client creates with an initial message throws a `TypeError` the instant staff open it (hits the error boundary, not the conversation). Secondary: `avgResponseTime` miscounts the client's own opening message as a staff reply since `undefined !== contactName`, skewing the metric toward zero for portal-originated tickets | `app/client/page.tsx`, `app/tickets/page.tsx`, `app/api/tickets/route.ts` (`mapTicket`) | Fixed | `/client` (the one reachable client ticket-creation path) now writes the canonical `{id, author, isInternal, body, timestamp}` shape `app/tickets/page.tsx`'s staff viewer actually reads, instead of `{from, body, date}` — both the initial-ticket POST and the new reply flow (#200) use it consistently. `app/portal/tickets/page.tsx`'s separate `{sender,text,date}` shape was left as-is since that page is unreachable per #154, not touched by this fix. |
| 200 | HIGH — `/client`'s Support tab (the one real, reachable client surface, given #154) only lists ticket subject/status/message-count — no click-through, no thread view, no reply box. The backend (`PATCH /api/tickets/[id]`) and the unreachable `app/portal/tickets/page.tsx` both fully support reply/attachments. Real clients currently have no in-app way to see or respond to a staff reply on their own support ticket | `app/client/page.tsx` | Fixed | Added a thread view + reply box to `/client`'s Support tab — clicking a ticket opens its message history (already server-filtered to non-internal entries) with a reply box that `PATCH`es `/api/tickets/[id]`, reusing the same canonical message shape as #199. |
| 201 | HIGH — `POST /api/invoices/[id]/checkout` is a real, working Stripe Checkout endpoint explicitly designed for the owning portal client to pay their own invoice (already correctly wired into the unreachable `app/portal/billing/page.tsx` and the staff `app/billing/page.tsx`), but `/client`'s Billing tab is 100% read-only ("Payment due" text, no click handler). Real clients currently cannot pay an invoice online at all, despite the feature being fully built and one prop-wire away | `app/client/page.tsx`, `app/api/invoices/[id]/checkout/route.ts` | Fixed | Added a "Pay Now" button on `/client`'s Billing tab for any non-Paid invoice, calling the existing `POST /api/invoices/[id]/checkout` and redirecting to the returned Stripe URL. Also fixed the checkout route's non-staff return URL, which pointed at the unreachable `/portal/billing` (per #154) — real clients paying would have landed on a page they could never navigate back to; it now points at `/client`, which reads the `?paid=`/`?checkout=cancelled` params on mount to switch to the Billing tab and show a toast. |
| 202 | HIGH — `PATCH /api/tickets/[id]` returns the raw DB row (`NextResponse.json(data)`) instead of routing through `mapTicket(row, includeInternal)` like `GET`/`POST` correctly do (per the real #124 fix) — internal-only staff notes (`isInternal: true` messages) are sent over the wire to a portal client replying to their own ticket. Not rendered by the current UI, but live in the network response for anyone inspecting it | `app/api/tickets/[id]/route.ts` | Fixed | `PATCH /api/tickets/[id]` now routes its response through the same `mapTicket()` filtering `GET`/`POST` already use (extracted into a shared `lib/tickets.ts` so both route files import one implementation instead of duplicating it). |
| 203 | HIGH — `GET /api/courses/[id]/enrollments` is gated only by `getAuthenticatedEmail` (any authenticated caller) with no company scoping — the table has no company column and RLS is `using(true)`. `app/portal/services/sales-training/page.tsx` fetches this for every course and filters client-side to the caller's own email, meaning every other company's students' names/emails/progress are sent to the browser first and filtered only in JS — a real cross-tenant PII leak over the wire | `app/api/courses/[id]/enrollments/route.ts` | Fixed | `GET /api/courses/[id]/enrollments` now scopes the query server-side to the caller's own `student_email` when the caller isn't staff (`isStaffCaller()`), instead of returning the full roster and relying on both real consumers to filter client-side. Staff (course management, and the `enrollmentId`-based lookup of a specific student) still get the full roster. |
| 204 | HIGH — `PATCH /api/portal-clients/[id]`'s `SELF_SERVICE_FIELDS` (`lastLogin`, `access`) let a portal client PATCH their own `access` to any value including `'Active'`, and `contexts/AuthContext.tsx` fires exactly this PATCH unconditionally on every session restore with no prior "am I disabled" check for `userType === 'client'`. Since `requirePortalClient` re-checks this same field on every API call, a disabled client simply reloading the page flips their own access back to Active | `app/api/portal-clients/[id]/route.ts`, `contexts/AuthContext.tsx` | Fixed | A self-service `PATCH` is now rejected outright (403) if the caller's own `access` is currently `'Disabled'` — closes the bypass at the value level (previously only the field *name* `access` was checked as self-service-allowed, with no check on what it could be changed to or from), so `AuthContext`'s unconditional session-restore touch can no longer flip a disabled client back to Active. |
| 205 | HIGH — `components/crm/NewTicketPanel.tsx`'s staff-side new-ticket flow uploads attachments to Storage but `handleNewTicket()` (`app/tickets/page.tsx`) never references the returned `data.attachments` in the ticket-create POST body — uploaded files are silently dropped, never attached to the ticket. The portal client's own ticket-creation flow correctly includes attachments, confirming this is a one-sided miss, not a design choice | `components/crm/NewTicketPanel.tsx`, `app/tickets/page.tsx` | Fixed | `handleNewTicket` (`app/tickets/page.tsx`) now includes `data.attachments` in the ticket-create POST body, matching the portal client's own ticket-creation flow. Also added attachment rendering to the staff message-thread view (`TicketMessage` gained an optional `attachments` field) so uploaded files are actually visible, not just stored. |
| 206 | HIGH — every list-fetching page under `app/reports/**` (`page.tsx` main hub, `health`, `team`, `revenue`) does a raw unpaginated `fetch()` against APIs that are cursor-paginated at 100 rows server-side (`lib/pagination.ts`), instead of `fetchAllPages()` (the established fix for this exact bug class, #48/#151) — Revenue totals, Team Productivity (hours/MRR/leaderboard), client Health scores, and the main Reports hub's KPIs all silently truncate past 100 rows of any one entity with zero indication. `app/reports/client/page.tsx` is the one exception (explicitly passes `?limit=500`) | `app/reports/page.tsx`, `app/reports/health/page.tsx`, `app/reports/team/page.tsx`, `app/reports/revenue/page.tsx` | Fixed | Swapped the raw `fetch()` calls in `app/reports/page.tsx`, `health/page.tsx`, `team/page.tsx`, and `revenue/page.tsx` for `fetchAllPages()`, the same fix already proven on Pipeline/Workspace/Finance/Sales for this exact bug class (#48/#151). `/api/dashboard` (a single aggregate object, not a paginated list) was correctly left as a plain fetch. |
| 207 | HIGH — Admin's "Security Settings" panel (Session Timeout, Password Policy, Two-Factor Auth, Login Attempts, Audit Logging toggle, IP Restriction) genuinely persists to `app_settings.security` via a real PATCH, but grepping every one of these field names outside `app/admin/page.tsx` itself returns zero matches — no session-timeout enforcement, no password-complexity check beyond a hardcoded 8-char minimum, no 2FA implementation anywhere, no login-attempt/lockout tracking, no IP allowlist check in `proxy.ts` or any route, and `logAudit()` inserts unconditionally regardless of the "Audit Logging" toggle. An admin can toggle on "Required 2FA" or a 3-attempt lockout, see "saved," and it has zero actual effect anywhere in the app | `app/admin/page.tsx`, `app/api/settings/route.ts` | Fixed | User explicitly requested all 6 be actually built rather than the panel removed. New `lib/settings.ts#getSecuritySettings()` (30s-cached read of `app_settings.security`) is the shared source every enforcement point reads from. **Audit Logging**: `logAudit()` now no-ops when off. **IP Restriction**: enforced in `lib/rbac.ts`'s `getCurrentUser()` (backs `requireRole`/`getAuthUser`) — deliberately NOT in `requireAdmin`, so a misconfigured list can never lock every admin out of Settings to fix it. **Login Attempts**: new `lib/login-attempts.ts`, real account-level lockout on the surfaces where a failure is actually observable server-side (Google Sign-In email lookup, both staff and portal onboarding verification codes, the new 2FA code) — password-based portal-client login goes straight to Supabase Auth client-side and a wrong password never reaches this backend, so it can't be tracked here (Supabase has its own independent brute-force protection on that path). **Password Policy**: both password-setting routes (staff `setup-account`, portal `complete-setup`) now enforce the configured tier's real minimum length instead of a hardcoded 8. **Session Timeout**: `lib/session-cookie.ts`'s `buildSessionCookie()` now takes a real configurable `maxAgeSeconds`; all 3 session-issuing routes apply it. **Two-Factor Auth**: real email-based 2FA (new `lib/two-factor.ts`, `team_members.two_factor_code`/`two_factor_code_expires` columns, `POST /api/auth/2fa-verify`, a code-entry step in `app/team-login/page.tsx`) — fully enforced on the Google Sign-In path when set to Required. **Known, documented gaps, not silently left half-covered**: (1) "Optional" has no per-user opt-in UI/column and is treated as not-enforced, same as Disabled — building self-serve enroll/disable was out of scope this pass; (2) 2FA is NOT enforced on `/api/auth/session`'s staff branch (the magic-link/email-OTP login alternative) — that route is also called for routine token-refresh on every page load with no way to distinguish a fresh login from a refresh, so gating it would force a new 2FA code on every token refresh; a staff member using "sign in with email" instead of Google currently bypasses 2FA. **Needs migration**: `supabase/migrations/add_two_factor_auth.sql`. **Verification**: `tsc --noEmit`, `next build`, `eslint` (0 errors), and the full `vitest` suite (156 tests) all pass clean. |
| 208 | HIGH — the "Schedule Access" modal's copy explicitly claims "The system will automatically enforce these windows," but `team_members.access_schedule` is only ever read by `contexts/AuthContext.tsx`'s client-side `checkAccessBlocked()` — `lib/rbac.ts`'s `getAuthUser()` (the real server-side gate behind every `requireRole`/`requireAdmin` call) never selects or checks it, and nothing in `app/api/cron/route.ts` ever flips `status` when a scheduled removal/reinstatement date passes. A user whose access is "scheduled for removal" keeps a fully valid server-side session/API access for as long as their existing cookie lives — the window is enforced nowhere except a client-side redirect a user can simply avoid triggering (an already-open tab, or a direct API call) | `app/admin/page.tsx`, `lib/rbac.ts`, `contexts/AuthContext.tsx`, `app/api/cron/route.ts` | Fixed | Added real `access_schedule` enforcement to both server-side identity-resolution paths — `lib/rbac.ts`'s `getCurrentUser()` (backs `requireRole`/`getAuthUser`) and `lib/admin-auth.ts`'s `requireAdmin()` (a separate implementation, needed its own check) — mirroring the exact removeAccessOn/reinstateOn window logic `AuthContext`'s client-side redirect already used. A user whose access is currently in a scheduled-removal window is now rejected server-side regardless of their existing cookie, closing the gap the already-fixed CRITICAL #121 didn't cover. |
| 209 | HIGH — the admin "System Backup" quick action explicitly promises "a full JSON snapshot of all GravHub data," but does 6 raw `fetch()` calls (`contacts`, `proposals`, `contracts`, `invoices`, `projects`, `deals`) against routes cursor-paginated at 100 rows, none using `fetchAllPages()` — any org with >100 rows in any entity silently gets an incomplete "full" backup with a "Backup downloaded successfully" message and no indication anything is missing. Contrast: the separate "Export Data" CSV feature (`app/api/admin/export/route.ts`) does this correctly with a real server-side `.limit(10000)` query | `app/admin/page.tsx` | Fixed | Swapped the 6 cursor-paginated raw `fetch()` calls in the System Backup modal for `fetchAllPages()`, same fix pattern as #206. `team-members` (not cursor-paginated) was left as a plain fetch. |
| 210 | HIGH — `monitored_sites.wp_app_password`/`wp_username` (a real client-site WordPress Application Password) are written and read as plain text (`app/api/monitored-sites/[id]/route.ts`, `lib/wordpress.ts`) — this codebase already has an established `encrypt()`/`decrypt()` helper (`lib/encryption.ts`, AES-256-GCM) used for every other stored credential (Google/Meta/LinkedIn OAuth tokens), just never applied here | `app/api/monitored-sites/[id]/route.ts`, `lib/wordpress.ts` | Fixed | `PATCH /api/monitored-sites/[id]` now encrypts `wp_app_password` via `lib/encryption.ts`'s existing `encrypt()` (the same AES-256-GCM helper already used for every OAuth token in this app) before writing it; `lib/wordpress.ts`'s `runAndStoreWPCheck()` decrypts it on read. `decrypt()` transparently passes through any still-plaintext legacy value, so no data migration was needed. `wp_username` was left as plaintext (not typically secret — often visible in post author bylines on the site itself). |
| 211 | MEDIUM — `app/proposals/page.tsx`'s `updateProposalStatus()` hardcodes `approvedBy: 'Jonathan Graviss'` on every Approve click regardless of who is actually logged in (the file never imports `useAuth`) — displayed back to users as `Approved by {proposal.approvedBy}`. Every proposal approved by anyone else gets a fabricated attribution baked into a real business record. Contrast: `app/time-tracking/page.tsx`'s equivalent field correctly uses `user?.name ?? user?.email ?? ''`, confirming this is an isolated miss, not a deliberate convention — same bug class as the already-fixed #177 (fake audit-log attribution), just on a live proposal field | `app/proposals/page.tsx` | Fixed | `updateProposalStatus()` now uses `useAuth()`'s real session identity (`user?.name || user?.email || 'Unknown'`) instead of the hardcoded name |
| 212 | MEDIUM — silent pagination-truncation (established #48/#151 bug class) newly found on 10 more pages that fetch cursor-paginated APIs via a raw `fetch()` instead of `fetchAllPages()`: `app/contracts/page.tsx` (feeds `totalMRR`/contract KPIs), `app/renewals/page.tsx` (feeds due30/due60/overdue KPIs), `app/maintenance/page.tsx` (feeds "MRR from Maintenance"), `app/billing/page.tsx` (feeds Overdue/Collected/Outstanding KPIs), `app/sales-enablement/page.tsx` (fixed `?limit=200`), `app/marketing/page.tsx` (broadcasts + its own KPI cards), `app/forms/page.tsx`, `app/social/page.tsx`, `app/marketing/hub/page.tsx` (3 of its 4 KPI tiles — the 4th already correctly uses `fetchAllPages`), and `app/portal/services/seo/page.tsx` (the portal client's own live keyword-ranking table) | see above | Fixed | All 10 pages swapped to `fetchAllPages()`. `app/sales-enablement/page.tsx` turned out worse than described: Playbooks/Templates were reading `json.data` off routes that return a bare array (`paginatedJson()`), so `.data` was always `undefined` and these lists were always empty, not just truncated past 200 — fixed alongside the pagination swap |
| 213 | MEDIUM — unchecked `res.ok` on optimistic-update handlers (established #31/#119/#126/#133 bug class), newly found on: `app/maintenance/page.tsx` (edit/cancel/updateBilling/updateDocuments/delete, all 5 handlers), `app/crm/contacts/page.tsx` (persistNotes/persistTasks/persistTags/lastActivity PATCH), `app/crm/companies/page.tsx` (persistTags + bulk-tag-apply + bulk-reassign loops, the latter two also showing a blanket success toast regardless of per-record outcome), `app/projects/[id]/page.tsx` (toggleTaskStatus/updateTask/deleteTask/addTask/updateProject — the last one backs every milestone/note/section/settings mutation), and `app/tasks/page.tsx` (updateStatus/updateTask/deleteTask). Each shows a false success while the write may have failed server-side (particularly relevant now that #123's department-scoping check makes a real 403 a realistic, non-transient response). Several of these files have a correctly-implemented sibling handler in the same file (e.g. `deleteProject`, `handleDeleteContact`) proving the fix pattern already exists, just wasn't applied uniformly | see above | Fixed | Added `res.ok` checks + optimistic-state revert to every listed handler across all 5 files, matching each file's own correctly-implemented sibling handler. `app/crm/companies/page.tsx`'s bulk-tag/bulk-reassign loops now track per-record failures and show a partial-failure toast instead of a blanket success |
| 214 | MEDIUM — `app/api/integrations/apollo/test/route.ts`, `maverick/test/route.ts`, and `mercury/test/route.ts` POST routes have no auth check at all, unlike structurally identical siblings `hubspot/test`, `stripe/test`, `granola/test`, and `resend/test` (the last of these also missing auth, at Low severity since it requires the caller's own API key rather than falling back to a stored org key) — any authenticated caller of any role can trigger a real outbound call using the org's stored API key, and for Mercury this leaks real bank `accountCount`. Same systemic pattern already fixed once for sibling routes at #144 — these were evidently missed | `app/api/integrations/apollo/test/route.ts`, `app/api/integrations/maverick/test/route.ts`, `app/api/integrations/mercury/test/route.ts`, `app/api/integrations/resend/test/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` to all 4 routes (apollo/maverick/mercury/resend `test`), matching the already-correct sibling `*/test` routes |
| 215 | MEDIUM — 6 automation action types (`Log Activity`, `Send Notification` + its 4 `Notify Sales/Finance/Delivery/Assigned Rep` variants, `Log Touchpoint`, `Flag in Dashboard`, `Apply Service Template`) insert `crm_activities.type` values (`'Note'`, `'Notification'`, `'Touchpoint'`, `'FLAG'`, `'Template'`) that don't match the real lowercase `ActivityType` enum (`lib/types.ts`). No DB CHECK constraint means the inserts succeed silently, but `app/api/inbox/unified/route.ts`'s activity filter (`.in('type', ['email','call','meeting','note','call_note'])`, the real #106 fix) never matches them, so these 6 action types' activity entries never surface in the Unified Inbox, and they fall through `TYPE_META`/`activityUtils.tsx`'s icon/color lookups to a generic fallback. This is the write-side counterpart of #106, left uncorrected in the same #91 pass that fixed these exact action types' column names but not their `type` values | `lib/automations-engine.ts` | Fixed | Corrected all 6 `crm_activities.type` insert values in `lib/automations-engine.ts` to the real lowercase `ActivityType` enum value `'note'` (the closest real match — these are all system-logged informational entries), so they now surface in the Unified Inbox and resolve to a real icon/color instead of a generic fallback |
| 216 | MEDIUM — `lib/automations-engine.ts`'s team-wide push-notification targeting (`'Send Notification'`'s unit-targeted branch, plus `'Notify Sales/Finance/Delivery Team'`) queries `team_members` with `.eq('status', 'Active')` (capitalized) — the real, live column value is lowercase `'active'` (confirmed against every other status check in the codebase). These 4 of 5 notification action types therefore always resolve zero recipients and never call `sendPushNotification()` for anyone, while the paired `crm_activities` log entry still gets created — masking the failure as a "successful" automation run. Only `'Notify Assigned Rep'` (which resolves a user id directly, no status filter) actually delivers a push | `lib/automations-engine.ts` | Fixed | Fixed both `.eq('status', 'Active')` calls to lowercase `'active'`. Also found and fixed a third occurrence with the identical bug in the round-robin rep-assignment action (not called out in the original finding) — same file, same root cause |
| 217 | MEDIUM — notification "read" state has no DB persistence — `GET /api/notifications` computes `unread` purely from `age < 86h`, and `Header.tsx`'s `markAllRead()`/per-item click only mutate local React state. Since `Header` is instantiated fresh per page (not a persistent layout wrapper, ~64 separate `page.tsx` call sites) and re-fetches on every mount, the unread badge and "Mark all read" effectively reset on nearly every navigation | `components/layout/Header.tsx`, `app/api/notifications/route.ts` | Fixed | Added real persistence: new `notification_reads` table (migration `add_notification_reads.sql`), `GET /api/notifications` now computes `unread` against it per-caller, new `POST /api/notifications` marks ids read. `Header.tsx`'s `markAllRead()`/per-item click now persist via the new endpoint instead of only mutating local state |
| 218 | MEDIUM — `app/api/dashboard/route.ts` (powers the main Executive/Sales/Billing dashboard, the highest-traffic page in the app) computes `openDeals`/`pipelineValue`/`winRate`/closed-deal buckets using exact-match `['Closed Won','Closed Lost'].includes(stage)` instead of `!stage.startsWith('Closed')` — the same bug class already fixed on `app/workspace/page.tsx`, `app/crm/pipeline/page.tsx`, `lib/guided-actions.ts`, `app/sales/page.tsx`, `lib/deal-score.ts` (#53/#105/#139) but never applied here. A deal in a custom "Closed — Duplicate"-style stage (custom stages are supported per #42) inflates Pipeline Value/Open Deals shown to leadership. The same exact-match bug also exists in `app/api/reports/attribution/route.ts` (Low severity — newer, less load-bearing report) | `app/api/dashboard/route.ts`, `app/api/reports/attribution/route.ts` | Fixed | Swapped the exact-match `['Closed Won','Closed Lost'].includes(stage)` to `!stage.startsWith('Closed')` in `app/api/dashboard/route.ts`, matching the fix already applied everywhere else. Also fixed the same bug class in `app/api/reports/attribution/route.ts`'s won/pipeline bucketing (was using `!isLost` instead of `!isClosed`) |
| 219 | MEDIUM — `app/api/dashboard/route.ts`'s "Contracts — Last 30 days" metric filters `contracts.renewal_date >= thirtyDaysAgo` instead of `created_at` (the route's own query doesn't even select `created_at`, though it's a real column per `schema.sql`). Since `renewal_date` is a future date for nearly every active contract, this filter is close to a no-op — it counts nearly every contract with a future renewal as "created in the last 30 days," producing a meaningless, inflated number on a KPI panel explicitly labeled "Last 30 days" | `app/api/dashboard/route.ts` | Fixed | Contracts query now selects `created_at` and the "Last 30 days" metric filters on that instead of `renewal_date` |
| 220 | MEDIUM — Settings' "Rotating Messages" editor (`app/settings/page.tsx`) writes each item as `{message, emoji}`, but `lib/settings.ts`'s `AppSettings.dashboard.rotatingMessages` type/defaults and `app/page.tsx`'s `GreetingBanner` both use `{sub, emoji}` — and `lib/settings.ts`'s `deepMerge()` doesn't recurse into arrays, so saving any Rotating Message via Settings persists the `{message,...}` shape verbatim. `currentMsg.sub` is then `undefined` for every item — the greeting banner's rotating sub-text goes silently blank (only the emoji renders) for any org that has ever customized this field. Config collected in a form, never correctly read back — the same bug class this pass was specifically asked to hunt for | `app/settings/page.tsx`, `lib/settings.ts`, `app/page.tsx` | Fixed | Renamed the Settings page's local field from `message` to `sub`, matching `lib/settings.ts`'s type/defaults and `app/page.tsx`'s `GreetingBanner` reader — consistent shape end to end now |
| 221 | MEDIUM — unlike every other cron loop in `app/api/cron/route.ts` (which all claim a row via a conditional `UPDATE ... WHERE <state>` before acting, per #12/#13's atomic-claim precedent), `runUptimeChecks()` only checks `last_check_at` in memory with no claim before running the HTTP check. The GH Actions cron pings every 5 minutes with its own retry logic, so an overlapping tick/retry can both read the same stale `last_check_at`, both call `recordCheck()`, and both see the pre-transition status — sending duplicate down-alert emails and writing duplicate `uptime_checks` rows for one real outage | `app/api/cron/route.ts` (`runUptimeChecks`), `lib/uptime.ts` (`recordCheck`) | Fixed | Added an atomic claim (conditional `UPDATE ... WHERE last_check_at` matches what was just read) in `runUptimeChecks()` before running each site's check, matching the claim-before-work pattern already used elsewhere in `app/api/cron/route.ts` |
| 222 | MEDIUM — `PATCH /api/tasks/[id]` passes `status`/`priority`/`category`/`department` straight through with no validation, unlike `POST /api/tasks` which validates all four against fixed enums. Any caller who can PATCH a task can set any of these to an arbitrary string; the frontend's `getTaskColumn()`/`priorityConfig`/`categoryColors` key off exact enum values, so an invalid `status` makes the task vanish from the Kanban board (while still counting in "Total Tasks") with no visible error | `app/api/tasks/[id]/route.ts` | Fixed | `PATCH /api/tasks/[id]` now validates `status`/`priority`/`category`/`department` against the same enums `POST /api/tasks` already uses (constants exported from the list route and reused) |
| 223 | MEDIUM — `app/time-tracking/page.tsx`'s `handleBulkApproval()` never checks `res.ok` on the bulk PATCH; on a non-2xx response `res.json()` still parses successfully but `Array.isArray(updated)` is false, so the function falls through with neither a success nor an error toast — a manager clicking bulk Approve/Reject gets zero feedback that anything went wrong | `app/time-tracking/page.tsx` | Fixed | `handleBulkApproval()` now checks `res.ok` before branching on the parsed body, with an error toast on failure |
| 224 | MEDIUM — `time_entries.invoiced` is read (filtered on) by `app/api/time-entries/billable-summary/route.ts` but never written by any code path in the app — `POST`/`PATCH /api/time-entries` never set it, and `app/api/invoices/**` never references `time_entries`/`invoiced` at all. The "Unbilled Time" panel (`app/billing/page.tsx`) will permanently show every billable time entry ever logged as "Not yet invoiced," forever, regardless of whether it's actually been billed — a UI/backend promise with no real link to the invoice lifecycle | `app/api/time-entries/billable-summary/route.ts`, `app/billing/page.tsx`, `app/api/invoices/**` | Needs Decision | Needs a decision on the actual billing workflow — should adding time entries to an invoice mark them `invoiced`? Real feature work, not a quick fix |
| 225 | MEDIUM — `app/projects/[id]/page.tsx`'s milestone/note create-only affordances (`addMilestone`, `addNote`) have no corresponding delete/edit path anywhere in the file — once created, a milestone or note is permanent short of a DB admin | `app/projects/[id]/page.tsx` | Fixed | Added `deleteMilestone`/`deleteNote` handlers (via the same `updateProject()` PATCH helper `toggleMilestone` already used) plus delete buttons in both the Milestones and Notes lists |
| 226 | MEDIUM — `NewProjectModal`/`ProjectSettingsModal`'s `CompanySelect` `onChange(name, companyId)` callback discards the `companyId` argument, only calling `setCompany(name)` — every project created via "New Project" persists with `company_id: null`, and renaming a company via Project Settings changes only the `company` text column, never `company_id`, letting the two diverge on a project that did have one. Practical consequence: the Files tab fetches `/api/files?company=${project.company}` and Storage folders are keyed by the literal company name — renaming the company via Settings silently orphans all previously-uploaded files (Files tab shows empty, files remain under the stale folder name with no error) | `app/projects/page.tsx` (`NewProjectModal`), `app/projects/[id]/page.tsx` (`ProjectSettingsModal`) | Fixed | Both `NewProjectModal` and `ProjectSettingsModal` now thread `companyId` from `CompanySelect`'s `onChange(name, id)` into the create/update payload instead of discarding it |
| 227 | MEDIUM — the `projects.tasks` JSONB column is set to `[]` at creation and never written to again (real task management moved entirely to the `app_tasks` table via `/api/tasks`, per #117), but `app/projects/page.tsx`'s grid card and table view still compute the "X overdue" badge from `project.tasks.filter(...)` — always `[]`, so the badge is permanently stuck at 0 regardless of how many real `app_tasks` are actually overdue on that project. Looks like real data but is structurally guaranteed to always read empty | `app/projects/page.tsx` | Fixed | The "X overdue" badge on both grid and table views now computes from real `app_tasks` (fetched via `fetchAllPages('/api/tasks')` and grouped by `projectId`) instead of the always-empty `project.tasks` JSONB column |
| 228 | MEDIUM — the new-flow public `POST /api/calendar/bookings` has none of the protections its legacy sibling (`app/api/bookings/route.ts`) has: no IP rate limiter, no timezone-correct past-date rejection, and no check that the submitted time actually falls inside `booking_types.availability`/matches `duration_minutes` — a caller can POST arbitrary past dates, off-hours slots, or oversized ranges directly, each triggering a live Google Calendar API call and a real email send with no throttling (spam/cost-abuse vector) | `app/api/calendar/bookings/route.ts` | Fixed | Ported the legacy route's protections into `POST /api/calendar/bookings`: the same per-IP rate limiter, `zonedWallTimeToUtc`-based past-date rejection, and new availability/duration validation against the booking type's `availability`/`duration_minutes` |
| 229 | MEDIUM — guests answer custom `intake_questions` on `/go/book/[slug]`, persisted to `booking_type_bookings.intake_answers`, but no staff-facing surface (`app/calendar/page.tsx`'s detail panel, `app/calendar/booking/page.tsx`) ever reads or renders that column — staff can require guests to answer questions that are then permanently invisible to them | `app/calendar/page.tsx`, `app/calendar/booking/page.tsx` | Fixed | `GET /api/calendar/bookings`'s internal staff listing now joins `booking_types.intake_questions`, and `app/calendar/page.tsx`'s detail panel renders an "Intake Answers" section pairing each question's label with the guest's answer |
| 230 | MEDIUM — `getBookingsForDate()` merges legacy `bookings` and converted `booking_type_bookings` into one list for the Week/Day calendar views, but the Cancel button's `PATCH /api/bookings/[id]` only ever looks up the id in the `bookings` table (unlike the sibling `DELETE`, which was correctly extended per #123 to handle both tables) — cancelling a new-flow appointment from the week/day view always 404s and shows a generic "Failed to cancel booking" toast | `app/calendar/page.tsx`, `app/api/bookings/[id]/route.ts` (PATCH) | Fixed | `PATCH /api/bookings/[id]` now checks `booking_type_bookings` when the id isn't found in the legacy `bookings` table (mirroring the split `DELETE` already handles per #123), so cancelling a new-flow appointment from the week/day view works instead of always 404ing |
| 231 | MEDIUM — `app/api/calendar/feed/[userId]/route.ts`'s new-flow query (`booking_type_bookings` portion) has no per-staff scoping at all (only `status='confirmed'`), unlike its correctly-scoped legacy-flow portion (`.eq('calendar_slug', cal.slug)`) — since `booking_types`/`booking_type_bookings` have no per-staff ownership column, every staff member's personal iCal feed URL (presented in Settings as "**your** iCal feed URL") returns the exact same full company-wide set of new-flow bookings (guest name/email/phone/notes), leaking every colleague's client-meeting details into each other's personal calendar subscriptions | `app/api/calendar/feed/[userId]/route.ts` | Open | Needs a per-staff ownership column on `booking_types`/`booking_type_bookings` (or a mapping to `calendar_settings`) to scope the new-flow query the same way the legacy one already is |
| 232 | MEDIUM — the admin "SEO Strategy (Legacy)" textarea (`app/admin/portal-management/page.tsx`) stores `portal_config.seoStrategy` as a plain string, but every consumer (`app/portal/services/seo/page.tsx` and the structurally identical `content-creation`/`marketing-strategy`/`ppc` pages) does `{...DEFAULT_STRATEGY, ...seoData.strategy}` expecting a rich object — spreading a string copies its characters as numeric-indexed props matching none of the real fields, so every field silently falls through to the hardcoded placeholder (`'Your SEO strategy is being prepared...'`, all metrics 0). An admin can type a real strategy and it will never appear anywhere. Severity dampened but not eliminated by #154 (these pages are currently client-unreachable) | `app/api/portal/seo/route.ts`, `app/admin/portal-management/page.tsx`, `app/portal/services/seo/page.tsx` (+3 sibling service pages) | Fixed | `GET /api/portal/seo` now detects when `portal_config.seoStrategy` is a plain string (the admin editor's actual output) and wraps it into `{overview: <string>}` instead of passing it straight through to be spread — the admin's real strategy text now displays instead of being silently replaced by the hardcoded "being prepared" placeholder |
| 233 | MEDIUM — `app/client/page.tsx`'s account/service label fetches `GET /api/portal-clients`, which is `requireAdmin`-gated — a real portal-client caller always gets 401/403 (silently swallowed to `[]`), so the header permanently shows the generic "Client Portal" fallback instead of the real service name. Ironically, `contexts/AuthContext.tsx` already fetches the full `portal_clients` row (including `service`) directly during login, but `clientToAuthUser()` discards it before it ever reaches `/client` | `app/client/page.tsx`, `contexts/AuthContext.tsx` | Fixed | Added `service` to `AuthUser`/`clientToAuthUser()` (the row is already fetched during login/session-restore) and switched `app/client/page.tsx` to read `user.service` directly instead of an extra `requireAdmin`-gated `/api/portal-clients` fetch that always 401'd for a real portal client |
| 234 | MEDIUM-HIGH — `portal_role` is referenced across 4 files (`app/api/portal-clients/route.ts`, `[id]/route.ts`, `invite/route.ts`, `magic-link/verify/route.ts`, the last via an explicit column-list `.select()`) but has no corresponding `CREATE`/`ALTER` anywhere in `supabase/schema.sql` or `supabase/migrations/*.sql` — if genuinely missing from the live DB, `magic-link/verify/route.ts`'s explicit-column select would error on every magic-link login. Given this codebase's documented precedent of `schema.sql` drifting from live state (#47's deployment-gap note), this needs a direct live-DB check before assuming either way | `app/api/portal-clients/route.ts`, `[id]/route.ts`, `invite/route.ts`, `magic-link/verify/route.ts` | Needs Decision | Requires a live DB check (`\d portal_clients` or equivalent) — this sandbox has no live Supabase access. If the column is genuinely missing, add a migration; if it exists but was never migration-tracked, just add the missing migration file for hygiene |
| 235 | MEDIUM — `PATCH /api/portal-clients/[id]` allows any active staff member (no role-tier floor beyond "authenticated staff") to reassign a portal client's `company`/`portal_role`/`services` — inconsistent with the page's own sibling actions (invite, company-config, list GET) which all correctly require `requireAdmin` | `app/api/portal-clients/[id]/route.ts` | Fixed | The non-self-service branch of `PATCH /api/portal-clients/[id]` now requires `requireAdmin`, matching the page's sibling actions (invite, company-config, list GET) |
| 236 | MEDIUM-HIGH — `app/admin/portal-management/page.tsx`'s "Add Company" `addCompany()` never calls `/api/crm/companies` (unlike `new-client`/`NewClientModal`, which use `CompanySelect`) — companies added here always get `company_id: null`, depending entirely on the fragile free-text name match (#187/#239). Separately, the no-primary-contact branch of the same function doesn't send `services`/`portalConfig`, so the Service Type dropdown selection is lost to the legacy `service` string column only — the exact bug #188 already fixed, but only for the with-contact branch of this same function | `app/admin/portal-management/page.tsx` | Fixed | `addCompany()` now uses `CompanySelect` (real `companyId`, threaded into both the invite and no-contact payloads; `POST /api/portal-clients` now persists `company_id`), and the no-contact branch now sends `services`/`portalConfig` too, applying #188's already-fixed pattern to this branch as well |
| 237 | MEDIUM — `app/admin/portal-management/page.tsx`'s `removeMember` action requires only `requireRole('Team Member')` (the lowest staff tier), unlike every sibling action on the same admin-only page (`requireAdmin`) — any non-admin staff member can remove a client's portal access | `app/admin/portal-management/page.tsx` | Fixed | `DELETE /api/portal-clients/[id]` (the only caller of `removeMember`) now requires `requireAdmin`, matching every sibling action on the page |
| 238 | MEDIUM — the #187 free-text-company-name-match pattern (tracked, originally scoped to admin display/config only) also governs real *authorization*: `lib/portal-auth.ts`'s `requirePortalClient()` — the gate behind nearly every portal-scoped route (projects, contracts, invoices, tickets, files, social-posts, insights) — authorizes via case-insensitive string equality on `company`, not the indexed `company_id` that exists on `portal_clients`. Two companies sharing a name would let one client's account pass authorization for the other's data across every route using this helper — a materially broader blast radius than #187's original "admin-gated, not externally exploitable" framing | `lib/portal-auth.ts` | Related to #187 | Same underlying fix as #187 (switch to `company_id`-based scoping) — flagging here because the real impact extends well beyond the admin panel #187 originally described |
| 239 | MEDIUM — `app/courses/[id]/page.tsx` (a fully-functional video/quiz/progress course viewer) has no link pointing to it anywhere in the live UI — neither the staff management list (`app/courses/page.tsx`) nor the client-facing course list (`app/portal/services/sales-training/page.tsx`) render an `href` into it; reachable only by hand-typing the URL | `app/courses/page.tsx`, `app/portal/services/sales-training/page.tsx` | Fixed | Added a "Preview" link/icon on each course card in the staff list (`app/courses/page.tsx`) and made the client-facing course rows in `app/portal/services/sales-training/page.tsx` clickable through to `/courses/[id]` (with `?enrollment=` when one exists) |
| 240 | MEDIUM-HIGH — `app/portal/services/sales-training/page.tsx` is unreachable by real portal clients for two independent reasons: it sits under `/portal/**` (redirected away per #154) AND is separately unit-restricted to staff in `AppShell.tsx`. #123's changelog explicitly claimed its auth relaxation "backs the portal-visible Sales Training page" for real clients — that claim doesn't currently hold, given both blockers | `app/portal/services/sales-training/page.tsx`, `components/layout/AppShell.tsx` | Open | Needs resolving alongside #154 (portal reachability) — the unit restriction in `AppShell` would also need lifting for real clients specifically |
| 241 | MEDIUM-HIGH — `app/admin/custom-fields/page.tsx` and `app/admin/document-templates/page.tsx` have no `useAuth()`/admin gate at all, unlike every sibling `/admin/*` page (per #163's precedent) — and unlike #163's original finding (where the backend was independently admin-gated, making the frontend gap purely cosmetic), here the backend genuinely does NOT require admin: both `app/api/custom-field-definitions/route.ts` and `app/api/document-templates/**` gate writes at `requireRole('Team Member')` (document-templates DELETE is the one exception, at `Leadership`). Any authenticated Team Member — not just Super Admins — can create/delete org-wide custom field definitions or edit/create document templates, despite living under `/admin/*` and the Permissions reference table explicitly listing "Admin Panel: Full / None / None / None / None / None" | `app/admin/custom-fields/page.tsx`, `app/admin/document-templates/page.tsx`, `app/api/custom-field-definitions/route.ts`, `app/api/document-templates/**` | Fixed | Added the standard `useAuth()`+redirect gate to both pages, matching #163's pattern, AND bumped the backend write routes to `requireAdmin`: `POST /api/custom-field-definitions`, `PATCH`/`DELETE /api/custom-field-definitions/[id]`, `POST /api/document-templates`, `PATCH /api/document-templates/[id]` (DELETE was already `Leadership`) |
| 242 | LOW — `GET /api/auth/health` (deliberately public, comment claims "safe to expose because values are masked") returns `first4...last4 (length)` fragments for `SUPABASE_SERVICE_ROLE_KEY`, `GOOGLE_CLIENT_SECRET`, `TOKEN_ENCRYPTION_KEY`, `CRON_SECRET`, `RESEND_API_KEY`, `GROQ_API_KEY` plus Node version/build marker to any unauthenticated caller — real secret-prefix/suffix disclosure and precise integration-reconnaissance with no auth or rate limit | `app/api/auth/health/route.ts` | Fixed | Switched `GET /api/auth/health` from real secret-prefix/suffix fragments to presence-only booleans (`set (N chars)` / `(not set)`), keeping it deliberately public/unauthenticated (a broken auth system shouldn't lock an engineer out of diagnosing it) but dropping the actual disclosure |
| 243 | MEDIUM-HIGH — the new-client wizard's writers (`app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx`) never persist the newly-created company's `id` back into React state after a successful `POST /api/crm/companies` (`companyId`/`data.companyId` is only ever set when picking an *existing* company via `CompanySelect`), and `POST /api/crm/companies` has no name-dedupe. If any later step throws (contract creation, contact 409, portal invite failure — all now throw-visible by this session's own error-surfacing fixes, previously silently swallowed), the user's only recourse is to retry the same form, which re-POSTs company creation with the same name and creates a second `crm_companies` row. Found by an adversarial regression review: making failures loud (this session's stated goal) increased real exposure to this pre-existing gap, since before these fixes users never saw an error prompting a retry at all | `app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx`, `app/api/crm/companies/route.ts` | Fixed | Both `app/admin/new-client/page.tsx` and `NewClientModal.tsx` now persist the newly-created company's `id` into state immediately after `POST /api/crm/companies` succeeds, so a retry after a later-step failure reuses the existing company instead of re-creating a duplicate |
| 244 | MEDIUM — `app/admin/audit-log/page.tsx`'s new fetch-failure handling (#179) only covers the cheap first-page probe request — `fetchAllPages()` itself silently `break`s and returns partial results on any later-page failure (acknowledged in its own code comment). A session expiring or a transient network blip between page 1 and page N produces a silently-truncated audit log with no error banner — the exact "false all-clear" class of bug #179 was meant to eliminate, just not fully closed for the multi-page case | `app/admin/audit-log/page.tsx`, `lib/fetch-all-pages.ts` | Fixed | `fetchAllPages()` now throws on any page failure (first or later) instead of silently `break`ing and returning a partial result — every call site across the app already had a `.catch()`/try-catch written expecting this, it just never fired before. Verified all ~30 call sites have proper error handling (one background audit found and fixed 2 that didn't: `app/crm/pipeline/page.tsx`'s HubSpot-import refresh, `app/billing/page.tsx`'s CSV-import refresh) |
| 245 | MEDIUM — the WordPress plugin dashboard's "Modules" grid (SEO Analysis, Focus Keywords, Meta Management, XML Sitemap, Social Previews toggles) persists to `gravhub_module_states` but nothing else in the plugin ever reads that option — SEO analysis always runs, meta tags always output, the Focus Keyword field always renders regardless of the toggle. The "XML Sitemap" card here is also a second, non-functional toggle sitting on the same settings page as the genuinely-working `gravhub_sitemap_enabled` switch in the separate "Sitemap Settings" card — same label, only one of the two actually does anything | `wordpress/gravhub-seo/admin/class-admin-page.php` (`get_module_catalog`), `admin/views/settings-page.php` | Open | Either wire each toggle to actually gate its feature, or relabel the grid as informational/status-only if real per-module disabling isn't intended |
| 246 | LOW-MEDIUM — `app/reputation/requests/page.tsx`'s `handleCreate()` has no error handling at all — the file doesn't import `useToast` (unlike `app/reputation/page.tsx`, fixed for this exact bug class at #119). A failed campaign create (validation, auth, DB error) silently leaves the modal open with zero feedback | `app/reputation/requests/page.tsx` | Fixed | Added `useToast` + full error handling to `handleCreate()` and the initial load, matching `app/reputation/page.tsx`'s already-fixed pattern |
| 247 | LOW — non-atomic read-then-write click/open counters on the newer email-tracking-extension endpoints (`track/click/[token]`, `track/click/ext/[token]`, `track/open/[id]`) — increment via plain `(x ?? 0) + 1` instead of an atomic RPC, unlike this codebase's established pattern (`adjust_sequence_counts`, `increment_review_campaign_counts`). Concurrent opens/clicks on the same tracked email could undercount by one — cosmetic-analytics risk only | `app/api/track/click/[token]/route.ts`, `app/api/track/click/ext/[token]/route.ts`, `app/api/track/open/[id]/route.ts` | Fixed | Added atomic increment RPCs (`increment_tracked_email_counts`, `increment_broadcast_clicked`; migration `add_email_tracking_atomic_counts.sql`) and switched all 3 endpoints to use them instead of a read-then-write increment |
| 248 | LOW — `GET /api/reputation/reviews` returns `[]` with a 200 status on a real Supabase query error rather than an error status — a DB outage is indistinguishable from "no reviews yet" | `app/api/reputation/reviews/route.ts` | Fixed | `GET /api/reputation/reviews` now throws (→ real 500) on a query error instead of returning `[]` with a 200 |
| 249 | LOW — `app/intelligence/page.tsx`'s `fetchData()` only surfaces a toast for the visitors/people call's failure; companies/stats call failures (e.g. Maverick misconfigured/rate-limited) are silently dropped, rendering as an honest-looking empty section with the specific error reason lost | `app/intelligence/page.tsx` | Fixed | `fetchData()` now checks and toasts errors from all 3 calls (people/companies/stats), not just the first |
| 250 | LOW — `PATCH /api/funnels/[id]/pages/route.ts` accepts client-supplied `views`/`conversions` from any Team Member-role caller, not just `sort_order`/`name`/`blocks` — dormant today (no UI call site sends those fields) but funnel analytics aren't tamper-proof against a malicious/buggy authenticated client | `app/api/funnels/[id]/pages/route.ts` | Fixed | `PATCH /api/funnels/[id]/pages` no longer accepts `views`/`conversions` — whitelist trimmed to the fields the real UI actually sends |
| 251 | LOW — `TaskRow` (`app/projects/[id]/page.tsx`) renders a `GripVertical` drag handle on every task and `addTask` sets a `sortOrder` field, but no `onDragStart`/`onDrop`/DnD library is wired anywhere in the file, and tasks are never actually sorted by `sortOrder` — the grip icon promises reordering that doesn't exist | `app/projects/[id]/page.tsx` | Fixed | Removed the misleading `GripVertical` drag handle from `TaskRow` since no DnD is wired |
| 252 | LOW — `projects.overview` (a distinct 5000-char field from `description`) round-trips through `mapProject()` on both API routes and the client mapping, but the "Overview" tab never renders it and `ProjectSettingsModal` has no field to edit it — effectively unreachable from the UI, easy to miss given the tab is also named "Overview" | `app/projects/[id]/page.tsx` | Fixed | Added an "Overview" card to the Overview tab (renders `project.overview` when set) and an editable Overview textarea to `ProjectSettingsModal`, wired through the already-supported `overview` field on both API routes |
| 253 | LOW — `GET /api/calendar/booking-types` (public) has no `.eq('active', true)` filter, unlike the slot-check/creation endpoints which both correctly filter on it — a disabled booking type's full config is still exposed publicly, and visiting its `/go/book/{slug}` renders the full form before silently dead-ending on every date ("No available times") instead of a clear "not available" message | `app/api/calendar/booking-types/route.ts` | Fixed | `GET /api/calendar/booking-types` now filters `active=true` for unauthenticated callers (the public `/go/book/[slug]` path), while staff callers (the admin management page, which needs to see inactive types to re-enable them) still see the full list |
| 254 | LOW — `app/api/gmail/messages/route.ts` and `gmail/message/route.ts` rely solely on `proxy.ts`'s outer gate (which only checks *presence* of an Authorization header, not cryptographic verification) rather than their own identity check, unlike sibling Gmail routes (`gmail/send` verifies via `getAuthenticatedEmail`, `gmail/token` uses `verifyOwnership`). Low practical severity — the real capability boundary is the caller-supplied Gmail access token itself | `app/api/gmail/messages/route.ts`, `app/api/gmail/message/route.ts` | Fixed | Added `getAuthenticatedEmail` identity checks to both `gmail/messages` and `gmail/message` POST routes, matching sibling Gmail routes |
| 255 | LOW — `lib/file-upload.ts` (`uploadFile`/`getFileUrl`/`deleteFile`/`validateFile`/`STORAGE_BUCKETS`) and `POST /api/storage/setup` have zero importers/callers anywhere in the codebase — real file storage goes through separate, unrelated buckets (`app/api/files/*`, `crm/companies/[id]/files`). Orphaned scaffolding | `lib/file-upload.ts`, `app/api/storage/setup/route.ts` | Fixed | Deleted `lib/file-upload.ts` and `app/api/storage/setup/route.ts` — confirmed zero importers/callers anywhere in the codebase |
| 256 | LOW — `PushNotificationBanner.tsx`'s `enable()` and `lib/push-notifications.ts`'s `subscribeToPush()` both POST to `/api/push/subscribe` without checking `res.ok` — a failed server call leaves the browser holding a live Push subscription with no corresponding `push_subscriptions` row, silently defeating future `sendPushNotification()` calls with no error shown | `components/ui/PushNotificationBanner.tsx`, `lib/push-notifications.ts` | Fixed | Both `PushNotificationBanner.tsx`'s `enable()` and `lib/push-notifications.ts`'s `subscribeToPush()` now check `res.ok`, unsubscribe the dangling browser Push subscription on a failed server call, and surface an error (toast / thrown error for the caller to handle) |
| 257 | LOW-MEDIUM — `app/inbox/unified/page.tsx`'s "View CRM contact" deep link uses `?email=${contactEmail}`, but `app/crm/contacts/page.tsx` only ever reads `?open=<id>` — clicking through from a Unified Inbox thread always lands on the plain unfiltered contacts list instead of the matched contact. Same bug class as the already-fixed #145, but a fresh regression introduced by the B9 Unified Inbox build after #145 shipped | `app/inbox/unified/page.tsx`, `app/crm/contacts/page.tsx` | Fixed | `GET /api/inbox/unified` now resolves and returns a real `contactId` per thread (batch email→id lookup against `crm_contacts`), and the "View CRM contact" link uses `?open=<contactId>` — matching what `app/crm/contacts/page.tsx` actually reads — instead of `?email=` |
| 258 | LOW — `lib/wordpress.ts`'s `coreUpdateAvailable` is initialized to `false` and never actually computed anywhere in `checkWordPress()` (only plugin/theme `update` objects are parsed from the REST API) — persisted and exposed via the WordPress GET route as if real. Not currently rendered in `app/monitoring/page.tsx`, so dead/incomplete rather than an active false claim, but would mislead if wired into the UI later | `lib/wordpress.ts` | Fixed | `checkWordPress()` now does a real core-update check: compares the detected WP version against WordPress.org's own public version-check API. Also wired the result into `app/monitoring/page.tsx`'s WP Version stat card (previously not rendered anywhere) |
| 259 | LOW-MEDIUM — `computeUptime30d()` returns a hardcoded `100` on a real Supabase query error with no logging, identical to its handling of "no checks yet" — a DB error and a genuinely-healthy site currently look the same. Not rendered by the current frontend (which discards this response field and re-fetches separately), but a live API-contract bug for any future consumer | `lib/uptime.ts` | Fixed | `computeUptime30d()` now logs the error and returns `null` (signature changed to `Promise<number | null>`) on a real query error instead of returning a fabricated `100` indistinguishable from "no checks yet" |
| 260 | LOW — `POST /api/monitored-sites` only validates the target is a well-formed `http(s)://` URL, with no private/internal-range denylist, and `lib/uptime.ts` fetches it following redirects — a staff member (or a compromised client site redirecting internally) can cause the server to request internal/metadata endpoints. Low severity since only Team Member+ roles can create monitors | `app/api/monitored-sites/route.ts`, `lib/uptime.ts` | Fixed | Added `lib/ssrf-guard.ts` (`isPrivateOrInternalUrl()` — resolves the hostname and checks against loopback/private/link-local ranges including the 169.254.169.254 cloud metadata address) and wired it into both `POST /api/monitored-sites` and `PATCH /api/monitored-sites/[id]` |
| 261 | LOW — `PATCH /api/time-entries/[id]` sets `approval_status` from `body.approvalStatus` with no enum validation, unlike the bulk endpoint which correctly validates against `['pending','approved','rejected']` — a Dept Manager+ caller can set an arbitrary string, which `ApprovalBadge` doesn't recognize (falls back to "pending" styling) and which the bulk-approvals filter also won't match | `app/api/time-entries/[id]/route.ts` | Fixed | `PATCH /api/time-entries/[id]` now validates `approvalStatus` against the same `['pending','approved','rejected']` enum the bulk endpoint already uses |
| 262 | LOW — `course_enrollments.certificate_id` is schema/API plumbing with zero implementation behind it — no auto-generation on course completion, no UI to view/download anywhere | `supabase/schema.sql`, `app/api/courses/**` | Open | Build real certificate generation, or drop the unused column |
| 263 | LOW — `app/tickets/page.tsx`'s "Linked Project" banner links to the static `/projects` list instead of `/projects/${ticket.projectId}` despite the dynamic route existing; separately, `linkedTaskId` is fetched but never displayed anywhere on the page — a dead field | `app/tickets/page.tsx` | Fixed | "Linked Project" now links to `/projects/${ticket.projectId}`. Added a matching "Linked Task" banner (linking to `/tasks?open=<id>`) so `linkedTaskId` is no longer a dead field |
| 264 | LOW — `app/api/chatbots/[id]/chat/route.ts`'s conversation-continuation branch checks only `id`+`chatbot_id`, no visitor-ownership check — anyone who obtains a `conversationId` could append to another visitor's thread. Low practical risk (unguessable id, IP rate-limited) but unaudited until now | `app/api/chatbots/[id]/chat/route.ts` | Fixed | The conversation-continuation branch now checks the stored `visitor_id` against the request's `visitorId` (the widget always sends both, persisted in localStorage) — a mismatch starts a fresh conversation instead of appending to someone else's thread |
| 265 | LOW — small silent-failure grab-bag: `components/ui/CompanySelect.tsx`'s `handleCreate` has an empty `catch` (no feedback if company creation fails inside the new-client wizard); `lib/useTeamMembers.ts` swallows fetch failures to `[]` (the Account Manager dropdown just looks empty, no error); `app/admin/new-client/page.tsx`/`NewClientModal.tsx` throw generic error strings instead of parsing the real API error body for the company/invite calls | `components/ui/CompanySelect.tsx`, `lib/useTeamMembers.ts`, `app/admin/new-client/page.tsx`, `components/admin/NewClientModal.tsx` | Fixed | Added a real error toast to `CompanySelect.tsx`'s `handleCreate` (parses the API error body), `lib/useTeamMembers.ts` now toasts on fetch failure instead of silently showing an empty dropdown, and both `app/admin/new-client/page.tsx` and `NewClientModal.tsx` now parse the real API error body instead of throwing a generic string for the company/invite calls |
| 266 | LOW — `app/api/rank-tracker/competitors/route.ts` validates a `name` field the frontend never sends (it sends `label`, read correctly elsewhere) — harmless dead validation since `name` isn't required, just confusing | `app/api/rank-tracker/competitors/route.ts` | Fixed | `POST /api/rank-tracker/competitors` now validates the `label` field that's actually sent/used, instead of the unused `name` field |
| 267 | LOW — `app/admin/scheduled-emails/page.tsx` fetches `/api/email/scheduled?limit=200` with no cursor/offset follow-up despite `getScheduledEmails()` supporting `offset` — the "Pending"/"Sent Today"/"Failed" counts and list would silently undercount past 200 entries in one status. Lower risk than #206/#209/#212 since scheduled-email volume in one status is unlikely to exceed 200 in practice | `app/admin/scheduled-emails/page.tsx`, `lib/email-scheduler.ts` | Fixed | `app/admin/scheduled-emails/page.tsx` now pages through via `offset` until a page comes back short of the page size, instead of a single fixed `limit=200` fetch |
| 268 | LOW — `IntegrationCard`'s toggle switch (`app/admin/page.tsx`) only calls `onConnect()` when switched ON — switching OFF just flips local `enabled` state with no backend call and no `onDisconnect` prop exists at all. A Super Admin toggling off an integration sees the switch move but nothing is actually disconnected | `app/admin/page.tsx` | Fixed | `IntegrationCard`'s toggle no longer silently flips to "off" with zero backend effect — switching off now shows a toast explaining disconnection isn't supported here yet, since no `onDisconnect` handler exists for any of the 4 integrations using this card |
| 269 | LOW — the WordPress plugin's `assets/metabox.js`/`assets/metabox.css` implement a complete earlier version of the metabox UI (different id/class scheme than what `render_metabox()` actually outputs today) — still enqueued on every post-edit screen, but ~90% of its functions silently no-op against selectors that no longer exist, and no `wp_localize_script()` ever provides the `gravhubMetabox` object it expects. Wasted requests, not a live bug (the working inline script covers everything correctly), but real dead code | `wordpress/gravhub-seo/assets/metabox.js`, `assets/metabox.css`, `includes/class-seo-metabox.php` | Fixed | Removed the dead `wp_enqueue_style`/`wp_enqueue_script` calls for `assets/metabox.js`/`metabox.css` and deleted both files — the working inline `<script>` in `render_metabox()` already fully replaced them |
| 270 | LOW — `wordpress/gravhub-seo/admin/views/redirects-page.php`'s `loadRedirects()`/`load404Log()`/`loadBrokenLinks()` and the "Add Redirect" POST handler have no `.catch()` on their `fetch()` chains — on a network failure/expired nonce/5xx, the corresponding table is stuck on "Loading…" forever with no error shown. The "Scan Now" button on the same page does have a `.catch()`, so this is an inconsistency within one file, same bug class already fixed for the live-readability AJAX call at #174 | `wordpress/gravhub-seo/admin/views/redirects-page.php` | Fixed | Added `.catch()` handlers to `loadRedirects()`, `load404Log()`, `loadBrokenLinks()`, and the "Add Redirect" POST handler, matching the "Scan Now" button's existing pattern — each now shows a real error message instead of getting stuck on "Loading…" forever |
| 271 | LOW — the new-client wizard's contract-duration math (`app/admin/new-client/page.tsx`) computes `months` from year/month difference only (ignores day-of-month, can be off by nearly a full month at boundaries), and if an admin enters an end date before the start date, `months` goes negative/zero, `duration` silently stays `undefined`, and the contract gets created with the server's default 12-month duration with no validation error or warning | `app/admin/new-client/page.tsx` | Fixed | Added a day-aware duration calculation (accounts for day-of-month instead of only year/month) and validates Agreement End > Agreement Start (blocks proceeding past the step, with an inline error message) before submitting |
| 272 | LOW — the ~62-site `logAudit` real-attribution fix (#177) has no regression test coverage — `tests/integration/audits-delete.test.ts`/`contracts.test.ts` only added a `getAuthUser` mock stub to keep the route from throwing, but neither asserts anything about the resulting `logAudit` call's `userName`. Correctness was verified by reading code during the adversarial review, not by test evidence, so a future refactor could silently reintroduce fake attribution with no test catching it | `tests/integration/audits-delete.test.ts`, `tests/integration/contracts.test.ts` | Fixed | Added a real `logAudit` mock-call assertion to both `tests/integration/audits-delete.test.ts` (DELETE /api/ai/audit) and `tests/integration/contracts.test.ts` (DELETE /api/contracts/[id]), asserting the real authenticated caller's name is used, not a hardcoded/fake one |
| 273 | HIGH — `components/ui/CompanySelect.tsx` (the Company search/picker used by New Contact, New Ticket, invoices, and other forms across the app) fetched `/api/crm/companies` with a raw, unpaginated `fetch()` — same bug class as #48/#151/#206/#209/#212, newly found here by the user directly ("not all showing up... show me all the records"). Any org with more than 100 companies had real companies silently invisible/unselectable in this picker everywhere it's used | `components/ui/CompanySelect.tsx` | Fixed | Swapped for `fetchAllPages()`, the same fix pattern already proven throughout this session |
| 274 | HIGH — `app/crm/contacts/page.tsx`'s `handleNewContact` fabricated a fake `companyId` (`` `co-${Date.now()}` ``) whenever the new contact's company name didn't exactly match an existing `crm_companies` row — this id never corresponded to any real company, so the contact silently ended up linked to nothing. In practice this was the common case, not an edge case: the company name is frequently auto-filled by `NewContactPanel`'s email-domain enrichment or freely typed, and an exact string match against the existing list is fragile (casing, "Inc" vs "Inc.", etc.) — compounded by #273's truncated company list making a real, already-existing company invisible to select in the first place. Found and reported directly by the user ("connect the contacts to the companies... if there is no company record attached to the contact record add it") | `app/crm/contacts/page.tsx` | Fixed | Company resolution now tries an email-domain match against existing companies' `website` first (more reliable than a free-text name — the same real company can be typed multiple ways but the domain doesn't change), falls back to a case-insensitive exact name match, and — only if neither hits — actually creates a real `crm_companies` row via `POST /api/crm/companies` (name from the form, website derived from the contact's email domain when not already provided) and links the contact to its real id. Never fabricates an id. Common public email providers (gmail.com, yahoo.com, etc., extracted into a new shared `lib/domain-utils.ts` used by both this file and `NewContactPanel.tsx`'s existing enrichment-on-blur) are excluded from domain-matching/company auto-creation, matching the assumption already built into the enrichment flow. **Known gap, not fixed this pass**: existing contacts already saved with a stale fake `co-<timestamp>` id from before this fix remain orphaned in the live database — this pass only fixes new contact creation going forward; cleaning up already-orphaned rows needs a live-DB query (`select * from crm_contacts where company_id is not null and company_id not in (select id from crm_companies)`) that wasn't run this pass (no live Supabase access in this session) |
| 275 | HIGH — the staff Courses list (`app/courses/page.tsx`'s `fetchCourses`), the course editor's Enrollments tab (`fetchEnrollments`), and the course viewer's enrollment lookup (`app/courses/[id]/page.tsx`) all read `json.data` off `GET /api/courses`/`GET /api/courses/[id]/enrollments`, both of which return a bare array via `paginatedJson()` (cursor in the `X-Next-Cursor` header, not `{data: [...]}`) — same bug class as #212's sales-enablement finding, never applied here. `.data` was always `undefined`, so the staff Courses list was always empty regardless of real courses, the Enrollments tab always showed "No students enrolled yet," and — more seriously — the course viewer's `enrollment` state never resolved, making progress tracking, quiz completion, and the "Course Completed" banner unreachable for any real user going through the page | `app/courses/page.tsx`, `app/courses/[id]/page.tsx` | Fixed | Swapped all 3 call sites to `fetchAllPages()`, matching the fix already proven for sales-enablement's identical bug |
| 276 | LOW — `knowledge_articles.views` and `courses.enrolled_count` still used a plain read-then-write increment/decrement, unlike this codebase's established atomic-RPC pattern (`adjust_sequence_counts`, `increment_review_campaign_counts`, `increment_template_usage`) — concurrent views/enrollments/unenrollments could race and lose a count. Cosmetic, display-only counters with no gating logic depending on them, same rationale as the already-fixed #167 | `app/api/knowledge-base/[id]/route.ts`, `app/api/courses/[id]/enrollments/route.ts`, `app/api/courses/[id]/enrollments/[enrollmentId]/route.ts` | Fixed | Added `increment_kb_article_views()`/`adjust_course_enrolled_count()` RPCs (migration `add_kb_courses_atomic_counts.sql`) and wired all 3 call sites to use them |
| 277 | HIGH — `app/api/crm/import/route.ts` (CSV import for contacts/companies/deals) has set `import_batch_id` on every insert since the feature was introduced (commit `3d02fa4`, "Bulk select/delete + duplicate merge"), but no migration ever added that column to `crm_contacts`/`crm_companies`/`deals` — confirmed via full-repo grep against `schema.sql` and every migration file, zero matches. Every CSV-imported row has been failing outright (Postgres/PostgREST rejects an insert referencing an unknown column) since that commit; the per-row error handling swallows it into a generic "import failed" per row, so `inserted` stays 0 and nothing in the UI indicates a systemic cause. The dependent Undo feature (`import/undo/route.ts`, which deletes by this same column) is consequently unreachable since nothing ever successfully imports. The "Direct API" HubSpot import mode is unaffected — it doesn't use `import_batch_id` and inserts real rows correctly | `app/api/crm/import/route.ts`, `app/api/crm/import/undo/route.ts` | Fixed | Added the missing `import_batch_id text` column (+ index) to all 3 tables via migration `add_import_batch_id.sql` |
| 278 | HIGH — `app/api/crm/enrich/route.ts`'s SSRF protection (`isSafeToFetch`) only validated the caller-supplied URL itself, but the actual fetch used `redirect: 'follow'` with zero re-validation of where a 3xx response actually points — a malicious or compromised external site could pass the initial check, then redirect to a private/internal/cloud-metadata address (e.g. `169.254.169.254`), and this route would transparently follow it and reflect the response back to the caller. A more specific gap than what a prior pass verified (that pass checked the IP-blocklist logic itself, not its interaction with redirect-following) | `app/api/crm/enrich/route.ts` | Fixed | Switched to `redirect: 'manual'` with a bounded (max 5 hops) manual redirect loop that re-runs `isSafeToFetch()` on every hop's `Location` before following it |
| 279 | MEDIUM — `components/crm/DuplicatesPanel.tsx`'s `handleIgnore` removed the duplicate group from local state and showed a "dismissed" success toast before checking `res.ok` on the persist call — a server error (e.g. a transient DB error in `POST /api/crm/duplicates/ignore`) meant the dismissal never actually persisted, but the user already saw success and the group vanished, only to silently reappear on the next duplicates scan | `components/crm/DuplicatesPanel.tsx` | Fixed | Now checks `res.ok` before toasting success; reverts the optimistic removal (re-adds the group back to local state) and shows an error toast on failure |
| 280 | HIGH — `POST /api/portal-clients/magic-link` sends a real login-link email on every call and its response shape doubles as an unauthenticated account-enumeration oracle (404 for an unknown email vs 200 for a real one), yet unlike every other brute-force-sensitive public endpoint in `proxy.ts` (booking, chatbot, verify-code/setup-account, portal verify-code/complete-setup, 2fa-verify) it had zero request throttling — an attacker could mass-probe emails or mail-bomb a real client's inbox with unlimited magic links | `proxy.ts` | Fixed | Added a per-IP `memoryLimited()` block (10 requests/hour), matching the existing convention for this route class |
| 281 | HIGH — `POST /api/auth/verify-email` is an unauthenticated boolean account-enumeration oracle (checks both `team_members` and `portal_clients` and reflects which table matched) with no throttling at all — trivially scriptable to enumerate every staff and client email in the system | `proxy.ts` | Fixed | Added a per-IP `memoryLimited()` block (20 requests/hour) |
| 282 | MEDIUM — `/client` portal's ticket-submission handler had no `else` branch on a failed `res.ok` and no real `try/catch` around the `fetch` itself — a server error or thrown network exception left the submit button in a stuck/ambiguous state with no error shown to the client, who'd have no way to know their support ticket never actually saved | `app/client/page.tsx` | Fixed | Added an `else { toast(...) }` branch on `!res.ok` and wrapped the handler in a real `try/catch` with an error toast |
| 283 | MEDIUM — same bug class as #282 in the same page's file-upload handler — a failed upload (bad file, server error, network drop) gave the portal client no feedback at all; the UI simply looked like nothing happened | `app/client/page.tsx` | Fixed | Added matching `else`/`catch` branches with error toasts |
| 284 | HIGH — `GET /api/gmail/messages` and `GET /api/gmail/message` only called `getAuthenticatedEmail(req)`, which proves the caller holds a valid session but never checks `team_members.status` — a suspended/deactivated staff member's still-valid session cookie could keep reading a connected Gmail inbox's messages indefinitely after their account was suspended, the same gap already fixed on other routes at #141 | `app/api/gmail/messages/route.ts`, `app/api/gmail/message/route.ts` | Fixed | Added the standard `team_members.status === 'active'` follow-up check (matching `gmail/token`'s `verifyOwnership()` convention) to both routes |
| 285 | MEDIUM — `app/time-tracking/page.tsx`'s initial load used a raw, unpaginated `fetch('/api/time-entries')` against a cursor-paginated (100/page) route — same bug class as #48/#76/#151/#206/#209/#212/#273 — anyone with more than 100 time entries had older entries silently invisible, which also skews approval totals and billing exports computed client-side from this list | `app/time-tracking/page.tsx` | Fixed | Swapped for `fetchAllPages()` |
| 286 | MEDIUM — `canApprove` on the same page only checked `user?.role === 'Manager'`, but the role enum also has a distinct `'Dept Manager'` role that is elsewhere treated as approval-capable — a Dept Manager could see time entries needing approval with no way to actually approve them | `app/time-tracking/page.tsx` | Fixed | `canApprove` now also allows `user?.role === 'Dept Manager'` |
| 287 | MEDIUM — `app/maintenance/page.tsx`'s `handleAddRecord` didn't check `res.ok` on the create call — on a server error it still closed the add-modal and appended a fabricated local-only record to the list (fake id, never actually saved), which vanished on the next real refetch with no explanation to the user of why "their" record disappeared | `app/maintenance/page.tsx` | Fixed | Now checks `res.ok`, only closes the modal on real success, and shows an error toast (leaving the modal open to retry) instead of fabricating a fake local record |
| 288 | MEDIUM — `lib/utils.ts`'s `formatDate()` parsed a bare `YYYY-MM-DD` date-only string directly via `new Date(str)`, which is UTC midnight; every US timezone is behind UTC, so `toLocaleDateString()` (no explicit `timeZone`) then rendered it as the previous day — the same off-by-one bug already worked around ad hoc in 12+ other files by manually appending `T12:00:00` before parsing, but `formatDate()` itself, the shared helper, never got the fix | `lib/utils.ts` | Fixed | `formatDate()` now normalizes a bare `YYYY-MM-DD` input to `${dateStr}T12:00:00` before parsing; real timestamps (already containing a `T`) pass through unchanged |
| 289 | LOW — `AudienceFilter` (`lib/broadcasts.ts`, mirrored locally in `app/marketing/page.tsx`) declared `hasEmail?: boolean` and `companyStatus?: string` fields that `applyAudienceFilter()`/`resolveEngagementFilters()` never read and no UI ever set — dead, misleading surface area that looked like real filter capability | `lib/broadcasts.ts`, `app/marketing/page.tsx` | Fixed | Removed both dead fields from both interfaces |
| 290 | LOW — `app/workspace/page.tsx`'s pending-tasks load used a raw `fetch()` against the cursor-paginated (100/page) tasks route instead of `fetchAllPages()`, unlike this same page's deals fetch a few lines below it — same bug class as #285, realistically low-impact here (unlikely for one rep to have >100 pending tasks) but inconsistent with the page's own established convention | `app/workspace/page.tsx` | Fixed | Swapped for `fetchAllPages()` |
| 291 | HIGH — WordPress plugin: `gravhub_sitemap_enabled`, `gravhub_sitemap_post_types`, and `gravhub_module_states` were `register_setting()`'d under the same options-group (`gravhub_seo_settings`) as the fields actually rendered on the visible Settings form. WordPress core's `options.php` update handler, on any Settings-page save, iterates *every* option registered under that group and calls `update_option()` on all of them — including ones with no rendered form field — passing `null`/empty for anything absent from `$_POST`. Result: saving the visible Settings form silently reset sitemap enablement, sitemap post-type selection, and per-module enable/disable state to blank/false on every save, with no indication anything besides the visibly-edited fields had changed | `wordpress/gravhub-seo/admin/class-admin-page.php` | Fixed | Moved these 3 options to a separate, non-form-rendering options group (`gravhub_seo_internal_settings`) so WP core no longer treats them as part of the visible form's save whitelist; their `sanitize_option_{name}` filters (needed by the separate REST save path) are unaffected since those hook per-option-name, independent of group. **Requires re-uploading the plugin to WordPress** |
| 292 | HIGH — `isPrivateOrInternalUrl()` (#260) is only enforced at monitored-site create/update time. Two periodic-recheck code paths fetch the stored URL on every cron cycle with no re-validation: `lib/uptime.ts`'s `checkSite()` used `redirect: 'follow'` with no guard at all (DNS-rebinding a previously-public URL to an internal/metadata address after creation would go unnoticed by every future check), and `lib/wordpress.ts`'s `checkWordPress()`/`fetchWithTimeout()` had the same gap across 6 fetch call sites — most seriously the authenticated plugin/theme-listing requests, which attach a real decrypted WordPress Application Password; a redirect off-site there could exfiltrate that credential, not just bypass the guard | `lib/uptime.ts`, `lib/wordpress.ts` | Fixed | `uptime.ts`: re-validates via `isPrivateOrInternalUrl()` at the top of `checkSite()` and switched to `redirect: 'manual'` (a 3xx already counts as "up" per the existing status-range check, so no need to actually follow it). `wordpress.ts`: `fetchWithTimeout()` now re-validates every URL — including each redirect hop — before fetching it, bounded at 5 hops; the one caller that explicitly wants the raw redirect response (the wp-login.php exposure check, which needs to see an un-followed 3xx) still gets it via an explicit `redirect: 'manual'` passthrough |
| 293 | MEDIUM — `app/projects/[id]/page.tsx`'s `updateProject()` fired its PATCH `fetch()` as a side effect *inside* the `setProject()` updater callback (needed to correctly capture the pre-update snapshot for revert-on-failure). React requires updater functions to be pure — it invokes them twice under `<StrictMode>` in development specifically to catch this, and can re-invoke them in other batching/interruption scenarios — so every call risked firing the PATCH request twice. Found by an adversarial regression-review agent auditing this session's newest commits | `app/projects/[id]/page.tsx` | Fixed | Added a `projectRef` kept in sync via a `useEffect`; `updateProject()` now reads the pre-update snapshot from the ref and fires the `fetch()` as a normal statement outside the (now-pure) `setProject()` updater |
| 294 | MEDIUM — systemic race across every "optimistic update, revert to captured `previous` on failure" handler added this session (`app/crm/companies/page.tsx`, `app/crm/contacts/page.tsx`, `app/maintenance/page.tsx`, `app/tasks/page.tsx`, `app/projects/[id]/page.tsx`): `previous` is a closure snapshot taken when the edit fires. If two edits to the *same* entity fire back-to-back and the **first** request's failure response arrives *after* the **second** request has already succeeded, the first request's failure handler reverts local state to a value that predates the second (successful) edit — clobbering it in the UI until the next refetch. The server's data is never wrong (only the client's optimistic state regresses), but it's a confusing, incorrect display that could prompt a user to redundantly redo an edit that already succeeded. Found by an adversarial regression-review agent; not fixed this pass — a real fix needs a considered decision (e.g. revert-by-refetch instead of revert-to-snapshot, or per-field request sequencing) applied consistently across ~5 files rather than a rushed multi-file patch | — | Needs Decision | Recommend: replace "revert to captured previous" with "refetch the single record from the server" on failure across these handlers, so a stale snapshot can never overwrite a since-succeeded edit |
| 295 | MEDIUM — the "Rotate Contact Owner" automation action is permanently unreachable in every currently-buildable configuration. It refuses to execute whenever `context._publicSource` is true (`lib/automations-engine.ts:956`, a deliberate security gate from #46, since it reassigns real CRM ownership and shouldn't be forgeable by a spoofed public form submission). But the *only* UI surface that can ever attach "Rotate Contact Owner" to an automation is `SequenceAutomateTab.tsx`, which always creates it with `trigger: 'Form Submitted'` — and every code path that ever fires that trigger (`app/api/forms/public/[slug]/route.ts`, `app/api/forms/public/funnel-submit/route.ts`) stamps `_publicSource: true` unconditionally. There is no non-public path that fires `form_submitted`, and no other UI offers "Rotate Contact Owner" against a different trigger. Net effect: the feature is fully built, testable, and documented, but structurally can never actually run. Found by the Marketing/Automation audit agent | `lib/automations-engine.ts`, `components/crm/SequenceAutomateTab.tsx` | Needs Decision | Two real options, both product calls: (a) let staff attach Rotate Contact Owner to a *non-public* trigger too (e.g. "Contact Created" via staff-side CRM entry, or a dedicated manual trigger) so it has a reachable path, or (b) if round-robin assignment is only ever meant to run off form submissions, relax the gate to allow it from public sources but add a lighter-weight abuse guard (e.g. require the submission to also pass `create_contact`/match an existing contact, or rate-limit rotations per contact) instead of a hard `break`. Not unilaterally changed — this is a security-gate/product-intent tradeoff, not a bug with one correct fix |
| 296 | LOW — `forms.create_deal` (aka `createDeal`) is a real, persisted, editable config field on every form (`GET`/`PATCH`/`POST /api/forms[/[id]]` all read and write it) that looks like a working "auto-create a deal from this submission" toggle, but is never consulted anywhere a form is actually submitted — `app/api/forms/public/[slug]/route.ts` and `funnel-submit/route.ts` both handle `form.create_contact` (creates/matches a CRM contact) but neither references `create_deal` at all. Toggling it on has zero effect; any admin relying on it believes deals are being auto-created when none are. Found by the Marketing/Automation audit agent | `lib/forms.ts`, `app/api/forms/public/[slug]/route.ts`, `app/api/forms/public/funnel-submit/route.ts` | Needs Decision | Needs a product decision on what an auto-created deal from a form should actually contain (deal name, value, stage, owner assignment) before building it — flagging rather than guessing those defaults |
| 297 | New capability: AI proposal generator, requested directly by the user (a "Proposal Generator Cowork Kit" — brand fonts, an ADCO gold-standard HTML/CSS template, and a house-style/section-structure spec, to be turned into a repeatable proposal machine) | `lib/proposal-template.ts`, `lib/proposal-generator.ts`, `lib/proposal-render.ts`, `lib/proposal-template/fonts/*`, `app/api/proposals/generate/route.ts`, `app/api/proposals/[id]/pdf/route.ts`, `components/crm/GenerateProposalPanel.tsx`, `app/proposals/page.tsx`, `lib/automations-engine.ts`, `app/automation/page.tsx`, `app/automation/builder/page.tsx` | Fixed | Replaces the old in-app `ProposalBuilderPanel` (a manual item/addon picker with a client-side jsPDF export, now deleted) with a real generation pipeline: a client's custom-built intake form submission (every client's form is bespoke, no fixed schema, per the kit's design) is flattened to raw field/answer text, drafted into the kit's exact 10-section structure by `lib/ai-client.ts`'s existing Ollama/Groq/template chain (never fabricates pricing outside the Investment Summary, no em/en dashes, matching house style), cloned into the ADCO template's HTML/CSS (Terracotta `#CC7853` locked in as the accent per `brand-style.md`'s own documented conflict with the gold-standard example's gold), and rendered to PDF. **Renderer decision**: the kit's template used CSS Paged Media (`@page` named pages, `@bottom-left`/`@bottom-right` footer content) which only WeasyPrint fully supports — but this app deploys on Vercel, where WeasyPrint's native Pango/Cairo dependencies have no clean serverless path. Rendering instead uses Playwright + Chromium (`playwright-core` locally against the pre-installed sandbox browser, `@sparticuz/chromium` wired in for the Vercel deploy path — unverified, see Known gaps), with the cover's full-bleed background done via a negative-page-margin trick and the footer via Playwright's own header/footer template API. A real bug was caught and fixed during this pass's smoke test: the cover's height was hardcoded to A4 (297mm) against a Letter-format page (279.4mm), overflowing the "Prepared For/By/Date" block onto a blank second page and silently losing it — fixed by deriving the cover's height from the actual page format. Verified by rendering the ADCO example itself end-to-end and visually comparing every page against `ADCO_Proposal_GOLD_STANDARD.pdf` (see `reference/template/` — layout, fonts, colors, and pagination all match). New `proposals` columns (`pdf_path`, `form_submission_id`, `generation_notes`) plus a new `Generate Proposal` automation action (selectable in both `/automation` and the drag-and-drop builder) so a Form Submitted trigger can auto-draft a proposal from a client's intake form; also closed a real pre-existing gap while wiring this — neither Automations UI ever exposed a way to scope "Form Submitted" to one specific form (`config.formScope`/`formId`, which `executeWorkflow()` already read but nothing ever set outside `SequenceAutomateTab`'s own one-off code path), so any such automation would have fired on every form submission app-wide, not just its intended one. **Known gaps, explicitly not done this pass**: (1) `@sparticuz/chromium` is installed but the production/Vercel launch path (`lib/proposal-render.ts`'s `process.env.VERCEL` branch) is unverified — only the local Playwright-Chromium path was smoke-tested in this sandbox. (2) **Needs migration**: `supabase/migrations/add_proposal_generation.sql` (3 new `proposals` columns + a new `proposal-pdfs` Storage bucket + its RLS policies) — not yet applied; this session had no live Supabase access. (3) No preset "Generate Proposal" automation was created — doing so needs a real target form (`config.formId`), and no client-specific intake form exists in production yet; once the user builds their first one via the Form Builder, duplicating it as a `Form Submitted` → `Generate Proposal` automation (now that the "Which Form" picker exists in `/automation`) takes under a minute. (4) Source-gathering connectors (Granola/Gmail/Drive) were explicitly scoped out for this pass per the user's decision — the generator only ever reads the form submission itself |

## Medium

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 16 | Uptime monitoring UI promises checks "every 5 minutes" / configurable interval, but the cron that drives it only runs once a day (`vercel.json`) — a site going down and back up between runs is never detected | `app/monitoring/page.tsx`, `vercel.json` | Fixed | Added `.github/workflows/cron-ping.yml` — a GitHub Actions scheduled workflow hitting the existing `CRON_SECRET`-gated `/api/cron` every 5 minutes, no cost, no app code change (`/api/cron` already self-throttles per site/sequence). Requires two GitHub repo secrets to be set: `CRON_TARGET_URL` and `CRON_SECRET` (matching the Vercel env var) |
| 17 | WordPress credential fields (`wpUsername`/`wpAppPassword`) are fully implemented server-side for plugin/theme scanning, but there's no UI input to actually enter them | `app/monitoring/page.tsx` | Fixed | Added a WordPress Credentials form (username + Application Password, masked input) to the site detail panel's WordPress tab, wired to the existing PATCH endpoint; `GET .../wordpress` now also reports `hasCredentials` |
| 18 | Funnel conversion tracking always credits the funnel's first page regardless of which step the form was actually on — skews per-step conversion/drop-off analytics for multi-step funnels | `app/api/forms/public/funnel-submit/route.ts` | Fixed | The public funnel page now passes the actual `pageId` (from its `?step=` route) through to the submit endpoint, which credits that page's conversion counter — falls back to the first page only if `pageId` is missing or invalid |
| 19 | Admin "System Health" panel hardcodes Authentication/Database rows to always show "Operational"/"Healthy" — never actually checked | `app/admin/page.tsx` | Fixed | New `database`/`auth` fields on `GET /api/admin/integration-health`: database does a real round-trip query, auth checks `SESSION_SIGNING_KEY` is configured. Rows and the "ALL SYSTEMS GO"/"DEGRADED" banner now reflect real state |
| 20 | Admin "Permissions" role×module matrix is fully static/decorative — no fetch, no save, no `onChange`; doesn't reflect the real (much simpler) RBAC hierarchy in `lib/rbac.ts` | `app/admin/page.tsx` | Fixed | Relabeled as "Reference only" with copy explaining the real hierarchy, since building true per-module enforcement is a much larger feature than a copy fix |
| 21 | Renewals page "Renewal Sequence" panel describes automatic 60/30/14/7-day notifications to rep + client; only 90/30-day triggers actually exist, and even those require a manually-built Active automation to send anything | `app/renewals/page.tsx` | Fixed | Copy now describes the real 90/30-day triggers; page fetches `/api/automations` and shows an explicit warning + link to Automation Builder when no Active automation is actually listening for either trigger |
| 22 | Settings' "Delivery Steps" claims to be a configurable list (add/remove/reorder), but the backend hard-locks to exactly 8 fixed-semantic steps tied to specific DB columns | `app/settings/page.tsx`, `app/api/delivery/workflow/[id]/step/route.ts` | Fixed | Real per-tenant configurability would need a schema change (`delivery_workflows` has one dedicated column per step); instead made the list honest — Settings now shows the 8 fixed steps read-only (new `lib/services`-style single source `lib/delivery-steps.ts`), removing the add/remove/reorder controls that never actually persisted anywhere real. Also fixed a related bug found during this pass: the Delivery Dashboard's "new workflow" create flow inserted an optimistic row with a fake local id that was never reconciled with the server's real record, so a page reload silently diverged from what was created |
| 23 | No Gmail refresh-token usage anywhere despite `gmail_refresh_token` being stored — tokens silently stop working ~1hr after connecting until the user manually reconnects (affects sequence reply-detection and inbox) | `app/api/sequences/reply-check/route.ts`, `contexts/AuthContext.tsx` | Fixed (partial) | Root cause: Gmail connect uses Google Identity Services' browser token client, which by design never issues a refresh token — only a server-side authorization-code OAuth flow does, which is a much larger change (new redirect/callback route, consent screen config) not attempted this pass. `reply-check`/`tickets/from-email` already skipped expired tokens gracefully (no crash); the actual gap was that Settings silently showed "Not Connected" instead of telling the user to reconnect. Settings now shows a distinct "Reconnect needed" state with a one-click reconnect button when a Gmail account is known but its token has expired |
| 24 | A/B testing for email sequences is dead scaffolding — types, analytics computation, and enrollment fields exist, but no editor UI to configure a variant and no execution code that ever assigns/sends one | `app/crm/sequences/page.tsx`, `app/api/sequences/analytics/route.ts` | Fixed | Built the previously-missing step editor (`components/crm/SequenceStepEditor.tsx`) with an A/B test toggle/split/Variant B fields; `execute/route.ts`'s `resolveAbVariant()` assigns and persists a variant once per enrollment; webhook/reply-check activity inserts stamp the variant; the sequence detail page's new Performance tab renders real per-step and A/B results (with an honest "need ≥5 sends per variant" fallback instead of a fabricated winner) |
| 25 | `proxy.ts`'s outer auth gate fails open (`NextResponse.next()`) on any unhandled exception, and `crm/companies/[id]` and `crm/contacts/[id]` PUT/PATCH have no independent auth check of their own — if the proxy ever throws, those become briefly unauthenticated | `proxy.ts`, `app/api/crm/companies/[id]/route.ts`, `app/api/crm/contacts/[id]/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` directly to both routes' PUT/PATCH as defense-in-depth (DELETE already had role checks) |
| 26 | Logout doesn't await/verify the server-side cookie-clear call — if it fails (network blip, tab closed), the UI shows "logged out" but the signed cookie remains valid for up to 7 days with no revocation mechanism | `contexts/AuthContext.tsx` | Fixed | `clearAuthCookie()` now awaits the `DELETE /api/auth/session` call with up to 3 retries and logs an error if all attempts fail; `logout()` awaits it before resolving |
| 27 | Google Sign-In audience check silently no-ops if both `GOOGLE_CLIENT_ID`/`NEXT_PUBLIC_GOOGLE_CLIENT_ID` are unset — would accept a validly-signed token from any Google OAuth client, not just this app | `app/api/auth/google-verify/route.ts` | Fixed | Now rejects sign-in with a 500 if neither env var is configured, instead of silently skipping the audience check |
| 35 | Sequence email engagement (opened/clicked/bounced) and step-task activities were mirrored to `crm_activities` with `company_id` hardcoded to `null` (or never set) — always attributed to the contact, never surfaced on the company record, since `CompanyPanel`'s Activity tab filters strictly on `company_id`. The engagement-event insert also used column names (`description`/`logged_by`) that don't exist on `crm_activities`, so it was silently failing on every event | `app/api/sequences/webhooks/route.ts`, `app/api/sequences/execute/route.ts` | Fixed | Both inserts now resolve `company_id` from the enrollment's `contact_id` via `crm_contacts.company_id` (the enrollment's own `company` field is a denormalized name, not an FK); the webhook insert also switched to the real schema (`title`/`user_name` instead of `description`/`logged_by`) |
| 36 | `app/api/sequences/[id]` PATCH/DELETE and `app/api/automations` POST/PATCH/DELETE had no auth check at all — any caller could rewrite or delete any sequence/automation. Higher blast radius than a typical missing-auth gap since this session added `Rotate Contact Owner` (reassigns a real CRM contact's owner) and dynamic-sender config to what flows through these routes | `app/api/sequences/[id]/route.ts`, `app/api/automations/route.ts`, `app/api/automations/[id]/route.ts` | Fixed | Added `requireRole(req, 'Team Member')` to all five mutation handlers, matching the pattern already used for #15/#25 |
| 37 | Sequences' "Automatic unenrollment" UI claims reply-based unenrollment is "Always on, can't be turned off," but reply detection only works when the enrollment's assigned rep has a connected Gmail account — `reply-check`'s cron entirely skips reps without a Gmail token and treats every `message_ids[0]` as a Gmail thread id regardless of channel. Sequences configured to send via Resend (a first-class Settings option), or enrollments assigned to a rep without Gmail connected, silently never detect a reply — the contact keeps receiving scripted emails after replying | `app/api/sequences/reply-check/route.ts`, `components/crm/SequenceAutomateTab.tsx` | Fixed (partial) | UI now discloses the Gmail-only limitation inline instead of claiming unconditional coverage. The underlying capability gap is real and unfixed — detecting replies to Resend-sent mail would need new infra (IMAP polling or an inbound-email-parsing provider), not attempted this pass |
| 38 | The sequences list page's "Default Unenrollment Triggers" panel showed 4 toggle switches (reply/meeting-booked/bounce/company-level-smart-unenroll) with no `onClick` handler at all — purely decorative — and 2 of the 4 (`meeting_booked` trigger, company-level unenroll) correspond to zero real implementation anywhere in the codebase, not even partially | `app/crm/sequences/page.tsx` | Fixed | Removed the two fully-fake rows and the non-functional toggle-switch affordance for the two real ones (reply/bounce, which aren't actually per-sequence togglable — they're blanket cron behavior); added an explicit "not yet available" note for meeting-booked and company-level unenroll |
| 39 | `PATCH /api/deals/[id]` unconditionally reset `last_activity` to today on every field edit — a probability tweak, a close-date typo fix, or a contact/company relink all made a deal with zero real customer contact look "active today," corrupting Deal Score's engagement factor and silently hiding stale deals from the Prospecting Workspace's guided actions | `app/api/deals/[id]/route.ts` | Fixed | `last_activity` now only bumps on an actual stage change — the one field on this route that represents a real pipeline event |
| 40 | `getGuidedActions`' "closing this week" bucket excluded any deal closing today — `close_date` (a date-only string) parses to UTC midnight, which is always earlier than `now()` for the rest of the day, so `>= now` was false for the single most urgent case | `lib/guided-actions.ts` | Fixed | Compares against today's UTC midnight instead of the current instant |
| 41 | This session's services-catalog consolidation (`lib/services.ts`) repointed 5 forms' service dropdown options to the new canonical list but left their `useState` default values pointing at the old literal `'Website'`, which is no longer a valid option — a user who opens the form and saves without touching the dropdown silently persists a stale, non-canonical value | `app/maintenance/page.tsx`, `app/renewals/page.tsx`, `components/admin/NewClientModal.tsx`, `app/admin/new-client/page.tsx`, `app/portal/page.tsx` | Fixed | Updated defaults to the first real entry in each file's own options list (`'Website Build'` for the two files using the canonical `SERVICE_NAMES`; each local `SERVICES[0]` for the two client-intake forms that use their own separate onboarding-interest taxonomy; `SERVICE_NAMES` directly for the portal client form, which previously had a fully hardcoded, unrelated `<option>` list) |
| 42 | Settings' multi-pipeline editor (`ManagePipelinesPanel`) supported adding a pipeline and adding/renaming/reordering its stages, but had no way to rename or delete a whole pipeline — and even if it had, the save handler POSTed each pipeline individually via the upsert-by-id branch of `/api/pipelines`, which can add or update a pipeline but can never remove one, so a delete control would have silently failed to persist | `app/crm/pipeline/page.tsx` | Fixed | Added rename (inline edit) and delete (guarded — can't delete the last pipeline, warns and requires confirmation if the pipeline has deals in it) controls; save handler now POSTs the full array once via `/api/pipelines`' array-replace branch, which correctly persists removals; `activePipelineId` resets to a valid pipeline if the active one is deleted |
| 61 | Automation builder's "Send Email" node has a "From Name" field that's collected in the UI and correctly translated into engine context (`fromName`), but the actual send call never read it — every automated email always sent from the account-wide default sender name regardless of what was configured, with no error or indication the field did nothing | `lib/automations-engine.ts` | Fixed | `sendEmail()` now receives a `from` override built from `context.fromName` + the account's real send address when a From Name is configured |
| 62 | `Add Tag`/`Remove Tag`/`Update Contact` automation actions require `context.contactId`, which is only ever populated by deal-based triggers — contract/invoice/proposal triggers carry a `company` name but no contact FK, so any automation built on those triggers with one of these actions silently no-ops forever (the step still records `status: 'success'` since the case just `break`s) | `lib/automations-engine.ts` | Fixed | Added a shared `resolveContactId()` fallback that looks up the company's primary contact by name — the same lookup `Send Email` already does — instead of requiring `contactId` to already be in context |
| 63 | `PATCH /api/proposals/[id]` and `PATCH /api/contracts/[id]` have no ownership scoping — any authenticated caller, including a portal client account for a *different* company, could PATCH any proposal or contract by id (change its value, status, assigned rep, etc.), not just their own. The portal client's own Approvals page (`app/portal/approvals/page.tsx`) calls these exact routes directly by id, which is why a blanket staff-only role check isn't the right fix — it would have broken that real, in-use feature | `app/api/proposals/[id]/route.ts`, `app/api/contracts/[id]/route.ts` | Fixed | Added `requirePortalClient(req, record.company)` (an existing helper already used by other portal-scoped routes) after fetching the target record — staff pass through unconditionally, portal clients are now restricted to records belonging to their own company. A residual, lower-severity gap remains: a portal client can still send fields their UI never would (e.g. `value`) for their *own* company's records, since field-level restriction wasn't attempted this pass |
| 64 | Renaming a pipeline stage in Settings only updated the stage config — `deals.stage` stores the stage's NAME (not its id), so every deal currently in that stage kept the old name and silently vanished from every pipeline view (Kanban filters by `stage.name`) with no way to fix it in-app (no stage picker exists on the deal-edit form) | `app/api/pipelines/route.ts` | Fixed | Found by an adversarial regression-review agent auditing this session's pipeline rename/delete feature (#42). The array-replace save path now diffs incoming stages against what's currently stored by id, and for any same-id/different-name stage, bulk-updates affected deals' `stage` to the new name before saving the config |
| 65 | Deleting a pipeline stage had no warning if deals were currently in it — unlike the pipeline-level delete guard shipped in the same feature (#42), which explicitly warns and requires confirmation when deals are present | `app/crm/pipeline/page.tsx` | Fixed | Added a matching confirm-with-deal-count guard to stage delete, using the same pattern as pipeline delete |

## Medium — Newly flagged, not fixed this pass

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 43 | "Rotate Contact Owner" round-robin does a read-then-write on `rotation_state` with no locking — two automations firing close together (a funnel traffic burst, a retried webhook) can both read the same `last_index`, both compute the same next rep, and both assign the *same* rep to two different contacts, silently desyncing the rotation counter | `lib/automations-engine.ts` | Fixed | Added `next_rotation_index()` — a Postgres function that does the increment inside a single atomic UPSERT (the row lock the UPSERT holds makes the read-modify-write safe under concurrency). App code now calls it via `.rpc()` instead of select-then-upsert. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 44 | Sequence enrollment does a check-then-insert (dedupe query, then a separate insert) with no DB-level unique constraint — two near-simultaneous enroll calls for the same contact (a double-click, or a form automation firing alongside a manual bulk enroll) can both pass the dedupe check and create two active enrollments, breaking the "one sequence at a time" guarantee | `app/api/sequences/[id]/enroll/route.ts`, `lib/automations-engine.ts` | Fixed | Added a partial unique index — at most one `status='active'` row per `contact_email` — as the real DB-level invariant. Both insert call sites now insert per-contact (not batched) and catch a `23505` conflict as "already active elsewhere, skip" instead of erroring. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 45 | `training_progress`'s PATCH does a read-then-full-overwrite upsert of `checklist_state` — toggling two checklist items on the same content in quick succession can lose one (both requests read the same pre-update state, the second write clobbers the first) | `app/api/training/progress/route.ts` | Fixed | Added `upsert_training_progress()` — a Postgres function that does the JSONB `||` merge inside a single atomic UPSERT. Route now calls it via `.rpc()` instead of select-then-upsert. **Needs migration**: `supabase/migrations/add_race_condition_fixes.sql` |
| 46 | Public, unauthenticated `funnel-submit` endpoint matches a submitted email against existing `crm_contacts` and fires `form_submitted` using that contact's real id — combined with this session's new `Enroll in Sequence`/`Rotate Contact Owner` automation actions, anyone who knows a funnel slug and an existing contact's email can trigger real CRM side effects (sequence enrollment, owner reassignment) for that contact with no authentication, if an admin has an "any form" automation configured | `app/api/forms/public/funnel-submit/route.ts` | Fixed | Didn't authenticate the endpoint (it has to stay public — funnels are embedded on external sites) — instead narrowed what a public trigger is allowed to do. Both public form-submission routes now stamp `_publicSource: true` on the trigger context; `Rotate Contact Owner` (which reassigns real CRM ownership) refuses to execute when it sees that flag, while `Enroll in Sequence` (lower risk — worst case, an unwanted nurture email) still works from a public trigger as intended |
| 47 | `training_progress` and `rotation_state`'s RLS policies allow any authenticated user to read every row (`using (true)` for `authenticated`) via a direct Supabase REST call, wider than the API layer's own per-user scoping. Same `using (true)` pattern is used on ~24 other tables in this codebase — systemic, not a one-off | `supabase/migrations/add_training_progress.sql`, `supabase/migrations/add_automation_sequence_config.sql` | Fixed | Confirmed this was a real, not theoretical, gap — staff genuinely hold live Supabase Auth sessions in-browser (used during magic-link/Google login). Extended the proof-of-concept to all 68 remaining tables using a blanket `authenticated using (true)` (or an anon-role equivalent) pattern, plus a separate proof of concept on `training_progress`/`rotation_state` shipped earlier this session. **Needs 2 migrations**: `supabase/migrations/drop_unused_anon_rls_policies.sql` (drops ~19 dead `to anon` policies — confirmed via exhaustive grep that no legitimate app flow, including every public/embed/booking/signing path, ever calls Supabase directly with the anon key; two were CRITICAL live-token leaks — `calendar_settings` exposed staff Google OAuth tokens, `signature_requests`' "by token" policies didn't actually validate the token since RLS can't compare against a client-supplied value) and `supabase/migrations/tighten_authenticated_rls.sql` (adds a `public.is_staff()` gate, matching the existing `auth_user_is_admin()`/`same_unit_or_admin()` pattern, to every remaining blanket `authenticated` policy). The tightening migration went through a full reconciliation pass against the real chronological policy history before shipping — a first draft would have shipped two real, opposite-direction bugs: (a) `deals`/`proposals`/`contracts`/`renewals` already carry live unit-scoped write policies from `add_unit_rls_policies.sql` under `unit_*` names, not the `auth_*` names visible in `schema.sql` (which has drifted from live state); replacing them with a blanket staff-only policy would have **erased** the real unit-scoping (`is_staff()` is a strict superset), while a naive "drop the old auth_* name" would have silently no-op'd and left the real `unit_*` policies live and OR'd against the new one, achieving nothing. (b) ~16 tables have a genuinely separate `auth_insert_*` policy distinct from their `auth_read_*`/`auth_update_*`/`auth_delete_*` policies; a first pass only targeted the latter three, leaving INSERT wide open on all 16. Also found: `projects`/`maintenance_records`/`tickets` have a real admin-only DELETE policy that a blanket `for all` replacement would have widened to any staff member, and `calendar_settings`/`bookings`/`revenue_months`/`audit_logs`/`processed_emails`/`bcc_processed_emails` never had an UPDATE and/or DELETE policy at all (default-deny) — granting one via `for all` would have been a new widening, not a tightening. Each table's replacement policy set was individually verified against the real live command coverage instead of assuming a uniform shape. **Deployment-gap correction (2026-07-15)**: this row was marked `Fixed` when the migration files were written and verified against tsc/build, but the migrations themselves sat unapplied on the live database for the rest of the session — surfaced when a later, unrelated migration failed with `function public.is_staff() does not exist`. The OAuth-token-leak fix (`google_integrations`/`social_connections`) was NOT actually live until the user applied `tighten_authenticated_rls.sql` and `tighten_authenticated_rls_part2.sql` in this later pass. Confirmed applied now — the gap is genuinely closed. Lesson: a migration file shipping in a commit is not evidence it reached the live database; this session has no live DB access to verify application directly |
| 48 | Pipeline board (Kanban) and List view fetch deals via `GET /api/deals`, which paginates at 100 rows server-side (`X-Next-Cursor` header) — neither the pipeline page nor the Prospecting Workspace reads that header or requests more, so once total deals exceed 100, board totals, weighted value, and workspace stats silently omit older deals with no indication anything is missing | `app/crm/pipeline/page.tsx`, `app/workspace/page.tsx` | Fixed | Added `lib/fetch-all-pages.ts`, a shared helper that follows `X-Next-Cursor` until exhausted; both pages' deal fetches now use it |
| 49 | `DealListView` (pipeline's List view) has no selection checkboxes, unlike `DealCard` in Kanban — selection state itself survives switching views, but bulk select/export/delete can only be *initiated* from Kanban; switching to List view leaves a rep unable to select anything for bulk action | `app/crm/pipeline/page.tsx` | Fixed | Added a checkbox column wired to the same `selectedIds`/`toggleSelect` state Kanban uses |
| 66 | Sequence enrollment's `enrolled_count`/`active_count` display counters are still a plain read-then-write (read the current count, write back `+1`) even after #44's fix — two concurrent enroll calls for *different* contacts on the same sequence can both read the same base count and one increment gets lost | `app/api/sequences/[id]/enroll/route.ts`, `lib/automations-engine.ts` | Fixed | Added `adjust_sequence_counts()` — a Postgres function doing the increment/decrement inside a single atomic `UPDATE`, same pattern as `next_rotation_index()`/`upsert_training_progress()` (#43/#45). All 3 call sites (manual bulk enroll, automation auto-enroll, automation unenroll) now call it via `.rpc()` instead of read-then-write. **Needs migration**: `supabase/migrations/add_sequence_count_rpc.sql` |

## Low

| # | Finding | Location | Status | Notes |
|---|---|---|---|---|
| 28 | "Add Deliverable" button in Delivery Dashboard timeline is a no-op toast despite a working API existing | `app/crm/delivery-dashboard/page.tsx:387` | Fixed | Button now opens a real modal (name/type/file URL/description) that POSTs to the existing `/api/delivery/add-deliverable` |
| 29 | Monthly/client report "Recommendations" section always hard-coded to `[]` | `app/api/delivery/monthly-report/[id]/route.ts`, `lib/seo-report-sender.ts` | Fixed | Added `buildReportRecommendations()` to `lib/client-reports.ts` — rule-based thresholds against the real metrics already computed for the report (traffic/SEO trend, keyword ranking declines, review activity, uptime incidents), shared across all three call sites so none of them fabricate anything not backed by the data on the report |
| 30 | Sales-enablement training completion/checklist/quiz progress lives only in `localStorage` — no team-wide visibility, lost on storage clear or device switch | `app/sales-enablement/page.tsx` | Fixed | Added `training_progress` table + `GET`/`PATCH /api/training/progress`; page now persists through the API, and Leadership/Super Admin/admins can view another user's progress via `?userEmail=`. Follow-up: `toggleCompletion`/`toggleChecklistItem`'s optimistic update didn't revert on a failed PATCH, silently showing a checked box that was never actually saved — now reverts with an error toast, matching #31's pattern |
| 31 | Time-entry edit (PATCH) failures are silently swallowed client-side — optimistic UI shows success even if the server rejected it | `app/time-tracking/page.tsx` | Fixed | `handleSave`/`handleDelete` now check `res.ok` and revert the optimistic update with an error toast if the server rejected it, instead of only catching network-level failures |
| 32 | CSRF check is skipped for the entire `/api/wordpress/seo/` prefix (needed for the plugin's own unauthenticated calls), including the staff-only company-assignment PATCH | `proxy.ts` | Fixed | Every other route under this prefix (including `settings` PATCH) is called by the plugin via a pre-shared `X-GravHub-Key` header, immune to CSRF by construction — only `health` PATCH uses staff cookie-auth (`requireRole`) and is genuinely exploitable cross-site. Narrowed the exemption to exclude that one specific route+method instead of the whole prefix or all PATCH/DELETE (which would have broken the plugin's real `settings` PATCH calls) |
| 33 | GravIntel visitor geolocation/ISP fields (`city`, `region`, `country`, `rdns_company`, `isp`) are rendered in the UI but never populated — no IP lookup exists | `app/api/intelligence/track/route.ts`, `app/intelligence/page.tsx` | Fixed | Vendor decision put on hold, but the integration itself is fully built and wired against ipinfo.io (`lib/geolocation.ts`) — inert (zero network calls, no fabricated data) until `IPINFO_API_KEY` is set, at which point it activates automatically on the next deploy, no code changes needed. Admin's System Health panel and the Intelligence detail panel both show an honest "not configured" state until then |
| 34 | `withErrorHandler` returns raw `err.message` to the client on any thrown error, including internal config errors — minor information disclosure about deployment misconfig | `lib/api-handler.ts` | Fixed | Client now gets a generic "Internal server error" message when `NODE_ENV === 'production'`; full detail still goes to console + Sentry. Dev/test behavior unchanged |
| 50 | `ProposalBuilderPanel`'s `getServiceType()` never considered the "Website Management" monthly checkbox — a proposal consisting only of Website Management got tagged `serviceType: 'Custom'` instead of the accurate value. Pre-existing gap, not introduced by this session's services-catalog work | `components/crm/ProposalBuilderPanel.tsx` | Fixed | Added `websiteMgmt` to the selected-services list `getServiceType()` checks |
| 51 | Sequence step delay is labeled "N business day(s) after previous step" in the step editor, but the executor schedules it as raw calendar days (no weekend-skipping in the day-offset math) — a "wait 2 business days" step actually waits 2 calendar days | `components/crm/SequenceStepEditor.tsx`, `app/api/sequences/execute/route.ts` | Fixed | Relabeled to "N day(s) after previous step" — accurate to what's actually scheduled. Real business-day arithmetic would be a larger follow-up if it's wanted |
| 52 | `{{sender_name}}`/`{{sender_email}}` merge-field tokens render blank with no warning when neither the enrollment nor the sequence has an `assigned_rep_id` resolvable to a real team member, even though the step editor's token bar advertises them unconditionally | `app/api/sequences/execute/route.ts`, `components/crm/SequenceStepEditor.tsx` | Fixed | Now falls back to the same step/sequence/settings default `fromName`/`fromEmail` chain `execute/route.ts` already uses to pick the actual "From" header, instead of an empty string |
| 53 | "Closed" stage detection is inconsistent between screens: the Prospecting Workspace uses an exact match (`stage !== 'Closed Won' && stage !== 'Closed Lost'`) while the Pipeline page uses `!stage.startsWith('Closed')` — a custom stage name like "Closed — Duplicate" (pipelines now support custom stages, see #42) would count as closed on one screen and open on the other, producing mismatched pipeline totals | `app/workspace/page.tsx`, `app/crm/pipeline/page.tsx` | Fixed | Workspace now uses the same `startsWith('Closed')` check as the Pipeline page for its "open deals" bucket |
| 67 | New capability: Granola (AI meeting notes) integration, requested directly by the user | `lib/granola.ts`, `app/api/integrations/granola/*`, `app/settings/page.tsx` | Fixed | Built against Granola's official REST API (`public-api.granola.ai/v1`, Bearer `grn_...` key, Business plan+) — the exact request/response contract could not be independently verified against live docs while building this (docs.granola.ai blocked automated fetches; implemented against the most consistently corroborated third-party documentation available). Follows the same inert-until-configured pattern as #33: zero network calls until a key is saved in Settings > Integrations, where "Test Connection" surfaces the real HTTP response so the wiring can be corrected against the live API if the guessed shape is wrong. Once connected: cron polls for new/updated meeting notes, matches attendees to `crm_contacts` by email, and logs a real `crm_activities` "meeting" entry on the matched contact/company — reuses the existing Activity timeline UI rather than building a new one. New `granola_meeting_notes` table records every synced note (matched or not) for visibility even when no CRM contact matches. **Needs migration**: `supabase/migrations/add_granola_integration.sql` |
| 68 | New capability: AI-assisted sequence email drafting via Ollama, requested directly by the user | `components/crm/SequenceStepEditor.tsx`, `app/api/ai/generate/route.ts` | Fixed | `lib/ai-client.ts` already tried Ollama first (only when `OLLAMA_URL` is set — inert otherwise, same pattern as every other integration this session) with Groq and a template as fallbacks, and `/api/ai/generate` was already live infra used by 6+ other pages — the missing piece was a sequences-specific generation type and UI, not new provider plumbing. Added a `sequence_email` prompt (aware of step position, day offset, target segment, and prior steps' subjects so it doesn't repeat itself) and an "AI Assist" block in the step editor: type what the email should say, get a draft, review it, then explicitly "Use this draft" to fill Subject/Body — never silently overwrites hand-typed content. The draft preview labels its actual source ("local AI" / "AI" / "template — no AI provider configured") so it's never presented as AI-written when the offline fallback served it |
| 69 | User-reported: a pasted call transcript logged as a note rendered as one flat, unformatted paragraph, no dedicated "Call Notes" type existed, and no note/activity could be edited after saving | `components/crm/activityUtils.tsx`, `app/crm/contacts/page.tsx`, `components/crm/LogActivityForm.tsx`, `app/api/crm/activities/[id]/route.ts` (new) | Fixed | Root cause of the "1 paragraph" bug: newlines survived the DB round trip fine, but two of the three places that render an activity's body used a plain `<p>` with no `white-space` handling, which collapses newlines per normal HTML rendering (the third, Contacts' dedicated Notes list, already had `whitespace-pre-line` and wasn't affected — confirming the bug lived specifically in the Log Activity/`crm_activities` path). Fixed both with `whitespace-pre-wrap`. Added `call_note` as a distinct `ActivityType` with its own icon/color and a larger transcript-sized textarea in Log Activity. Editing was entirely unbuilt for `crm_activities` (no PATCH route existed at all) — added one (`requireRole`-gated, 50,000-char limit for long transcripts) plus inline edit UI (pencil → title/body → save/cancel) in the shared `ActivityTimeline` component (covers Companies + Pipeline deals) and the Contact page's separate unified-timeline renderer, gated on a new `editable` flag so only entries backed by a real activity row (not derived facts like "email opened") show the edit affordance. Also fixed a related bug found while wiring this: the deal panel's "Log Activity" never actually persisted to the server at all (state-only, silently lost on refresh) — it now does, matching the same pattern already used on Companies/Contacts |
| 70 | User-reported: "finish the WordPress SEO section to 100%." A research pass found the section overall is genuinely solid (real plugin with a non-trivial on-page analyzer, real health/score/meta sync, honest UI, no fake data anywhere) — but the polished per-site SEO report (score distribution, worst pages, top issues, security/plugin-update flags) was 100% manual "Generate Report" only, with no scheduled or automated delivery of any kind | `app/api/wordpress/seo/reports/route.ts`, `lib/client-reports.ts`, `lib/seo-report-sender.ts`, `lib/templates/generate-monthly-report.ts` | Fixed | Rather than building a second, parallel scheduling/email system, wired this data into the *existing* monthly client-report cron+email pipeline (`seoReportsDue()`/`sendMonthlyClientReports()`, already fires on the 1st of each month for any `client_integrations` row with `seo_reports_enabled`). `buildClientReport()` now looks up a `wordpress_site_health` row by `company_id` (silently omitted if none linked, matching the existing "missing source = omitted, not an error" pattern for GSC/GA4/GBP) and folds in a real "Website SEO Health" section — avg. page score, pages needing attention, plugin-update count, security issues — plus 3 new rule-based recommendations (poor-scoring pages, outdated plugins, security issues). Extracted the report-computation logic (previously duplicated inline in the manual-report route) into `lib/wordpress-seo-report.ts` as a single shared source, so the manual Reports tab and the new automated email can never drift apart |
| 71 | Meta tab's edit modal exposed 4 of the 6 fields the backend/DB (and the plugin's own per-post metabox) already fully support — `og_image` and `schema_markup` were writable via the API and rendered by the plugin's `wp_head` output, but had no GravHub UI to set them at all | `app/rank-tracker/wordpress/page.tsx` | Fixed | Added OG Image URL and Schema Markup (raw JSON-LD, client-side validated before save with an inline error instead of silently sending malformed JSON) fields to the existing edit modal, wired to the already-working `PATCH /api/wordpress/seo/settings` |
| 72 | The plugin's analyzer (`class-seo-analyzer.php`) computes `word_count`, `readability_score`, and `focus_keyword` per page and sends all three on every sync, but `wordpress_seo_scores` had no columns for them — the API route silently discarded real, already-being-sent data on every single sync | `app/api/wordpress/seo/scores/route.ts`, `app/rank-tracker/wordpress/page.tsx` | Fixed | Added the 3 missing columns and updated the POST handler to store them; the Scores tab's expandable row now shows word count, readability score, and focus keyword alongside the existing issue list. **Needs migration**: `supabase/migrations/add_wordpress_seo_score_details.sql` |
| 73 | User-reported: no way to delete a website audit — there was zero delete UI anywhere (list or detail page) and no `DELETE` handler at all in the API route; neither existing method (`GET`/`POST`) had any auth check either | `app/api/ai/audit/route.ts`, `app/audits/page.tsx`, `app/audits/[id]/page.tsx` | Fixed | Added a `requireRole`-gated `DELETE` handler (matching the `?id=` query-param convention the existing `GET` already uses, since there's no `[id]` sub-route) plus `logAudit` on delete. Confirmed no other table has a foreign key to `audits`, so deletion needed no cascade/orphan handling. Added a delete (trash icon) affordance to both the list page (per-row, optimistic removal with revert-on-failure) and the detail page (header button), each behind the existing shared `ConfirmModal` component |
| 74 | User-reported: the AI Assistant chat button (fixed bottom-right) had no way to be repositioned — a real problem if it ever overlaps other fixed UI on a given page | `components/layout/AppShell.tsx` | Fixed | Made the button draggable via native pointer events, clamped to the viewport, with position persisted per-device in `localStorage` (defaults to the original bottom-right corner until first dragged). A drag-vs-click distinction (`fabDragMoved` ref) ensures a drag doesn't also fire the button's normal "open assistant" click |
| 75 | User-reported: no way to edit a contract's record — the table's "Edit" (pencil) button was a dead affordance calling the exact same handler as "View", opening only a read-only detail panel; the detail panel itself had zero editable fields anywhere across all 4 tabs. Separately found while wiring this: `PATCH /api/contracts/[id]` never supported editing `company`, `serviceType`, `startDate`, or `duration` at all (create-only fields), and `NewContractPanel`'s own Billing Structure dropdown (`'One-Time'`, `'Monthly Retainer'`, ...) didn't match the server's validation enum (`'One-time'`, `'Monthly'`, ...) — meaning **creating a new contract with the form's own default billing structure value has always failed validation** with a 400 | `app/contracts/page.tsx`, `components/crm/NewContractPanel.tsx`, `app/api/contracts/[id]/route.ts` | Fixed | Added `initialData?: Contract` to `NewContractPanel` (mirroring how `ProposalBuilderPanel`/`app/proposals/page.tsx` already share one panel for create+edit) — the same panel now edits an existing contract when opened via the (now-real) Edit button on the list row or a new Edit button in the detail panel header. Extended the PATCH route to accept `company`/`companyId`/`serviceType`/`startDate`/`duration`, and fixed `CompanySelect`'s `companyId` callback (previously discarded) so company edits keep `company`/`company_id` in sync rather than silently desyncing them. Fixed the billing-structure enum mismatch by aligning the dropdown to the server's real values. The proposal-linking section is hidden in edit mode (`proposalId` isn't PATCH-able, so a relink control there would silently no-op) and status changes stay on the existing dedicated transition buttons rather than being duplicated into this form. **Known, deliberately out-of-scope residual risk**: renaming a contract's `company` doesn't retroactively fix `app/crm/pipeline/page.tsx`/`app/renewals/page.tsx`, which still link deals/renewals to a contract by matching the `company` name string, not `companyId` — flagged in code but not fixed here (separate, larger effort to migrate those lookups) |
| 76 | User-reported: sequence enrollment "only lets me send to one contact, need the full client list." Bulk multi-select enrollment already existed and worked (`EnrollContactsModal`, checkboxes + "Select all" + array-based enroll API) — the actual bug was that the modal's contact fetch used a raw, unpaginated `fetch('/api/crm/contacts')`, and that endpoint is cursor-paginated at 100 rows/page. The modal silently only ever showed/searched the 100 most-recently-created contacts, with no indication anything past that was excluded — exactly the "not the full list" the user was seeing. Same bug class as the already-fixed #48 (deals pagination) | `app/crm/sequences/[id]/page.tsx` | Fixed | Swapped the raw `fetch()` for `fetchAllPages()` (the same helper #48 introduced), which follows `X-Next-Cursor` until the complete contact list is loaded, matching what the Contacts list page itself already does via `fetchCrmContacts()` |
| 77 | #63's fix scoped `PATCH /api/proposals/[id]`/`PATCH /api/contracts/[id]` to the caller's own company, but only checked which record a portal client may touch — not which fields they may set it to. A portal client could still PATCH their own record's `company`/`companyId` to a *different* company's name, silently reassigning their own proposal/contract (with fully attacker-controlled value/items/billing) into another client's portal view. Found by an adversarial regression-review agent auditing this session's newest routes | `app/api/contracts/[id]/route.ts`, `app/api/proposals/[id]/route.ts`, `lib/portal-auth.ts` | Fixed | Added `isStaffCaller()` to `lib/portal-auth.ts`; both routes now whitelist the exact fields a non-staff (portal client) caller may set — `{status, clientSigned}` for contracts, `{status, respondedDate, renewalNotes}` for proposals, matching what each real portal Approvals UI actually ever sends. Staff callers are unaffected (unrestricted field set, as before) |
| 78 | Draggable AI Assistant FAB (#74) only re-clamps its saved position on initial mount and during a drag — a window resize or device rotation after dragging it near an edge could leave it off-screen with no way to reach it again | `components/layout/AppShell.tsx` | Fixed | Added a `resize` listener that re-clamps the current position whenever the viewport changes |
| 79 | Enroll Contacts modal's "Select all" fully replaced the selection with only the currently search-filtered contacts — selecting someone under one search query, then searching something else and clicking "Select all," silently discarded the first selection. Also had no debounce on the search filter, re-filtering the full (potentially large) contact list synchronously on every keystroke. Found by an adversarial regression-review agent | `app/crm/sequences/[id]/page.tsx` | Fixed | `selectAll()` now adds/removes only the currently-filtered eligible ids into/from the existing selection instead of replacing it wholesale; the "Select all"/"Deselect all" label now reflects whether every currently-filtered contact is selected, not a raw size comparison. Added `useDeferredValue` on the search input so typing stays responsive while filtering is deferred. List virtualization (windowing the DOM nodes for very large contact lists) was not attempted — would require a new dependency (e.g. `react-window`) and is a larger change than this pass |
| 80 | Contract edit/create form's `canSave` didn't validate `duration` at all — an empty, zero, negative, or >120 value looked saveable in the UI and only failed after a round trip to the server's own `validate()` (min 1, max 120). The input's own `max="60"` HTML hint was also inconsistent with the server's real max of 120 | `components/crm/NewContractPanel.tsx` | Fixed | `canSave` now requires `duration` to be a non-empty number in `[1, 120]`, matching the server; aligned the input's `max` attribute to 120 |
| 81 | The WordPress plugin's `get_page_settings()` returned per-post meta OR GravHub-app-managed settings, whichever was present, not a merge — if a page had ANY per-post meta field set locally in WordPress (even a single legacy `meta_title` from before this session's Meta tab existed), the entire GravHub API response for that page was discarded, silently dropping `og_image`/`schema_markup` (and any other app-set field) set via the app's Meta tab editor (#71) | `wordpress/gravhub-seo/includes/class-meta-manager.php` | Fixed | `get_page_settings()` now fetches API settings as the base layer first, then overlays only the individual per-post fields that are actually locally set — matching the function's own stated intent ("per-post meta highest priority") on a per-field basis instead of an all-or-nothing either/or. `get_seo_settings()` is transient-cached, so calling it unconditionally instead of short-circuiting has no real perf cost. **Requires re-uploading the plugin to WordPress** — this repo has no build/zip step; the source PHP file is the distributable |
| 82 | New capability: Gmail email tracking browser extension, requested directly by the user ("track emails from my actual Gmail inbox... like HubSpot") | `browser-extension/*`, `lib/extension-auth.ts`, `lib/tracked-emails.ts`, `app/api/extension/*`, `app/api/track/open/[id]`, `app/api/track/click/ext/[token]`, `app/settings/page.tsx` | Fixed | Existing sequence/broadcast open-click tracking is 100% Resend-webhook-based and can't observe mail sent directly from a real Gmail account, so this is new infrastructure, not a reuse: a Manifest V3 unpacked extension (`browser-extension/`) content-script-injects into `mail.google.com`, intercepts the Send button click in the capture phase, mints a `tracked_emails` row via a new token-authed API, embeds a tracking pixel + rewrites links to route through new public `/api/track/open/[id]` and `/api/track/click/ext/[token]` endpoints (mirroring the existing broadcast click-redirect token pattern), then re-dispatches the click so Gmail's own send handler fires on the modified DOM. Opens/clicks are logged and mirrored onto the matching CRM contact's activity timeline (`mirrorTrackedEmailActivity`, no-ops without a contact match). Extension auth uses a long-lived Bearer token (`ghext_...`, only its SHA-256 hash stored server-side, generated/revoked from a new Settings > Email Tracking section) since the extension runs on a different origin and can't share GravHub's session cookie; `requireExtensionToken()` fail-closed rejects revoked tokens and non-active team members, matching the #114/#115 suspended-staff convention. **What's independently verified**: server-side pixel/click logging, activity mirroring, token issuance/hashing/revocation, and the Settings UI all compile clean and pass `tsc`/build/vitest. **What genuinely needs live-Gmail testing, not yet possible in this environment**: the DOM-injection selectors in `content.js` (`findSendButton`/`extractRecipientEmail`/`findComposeBodies`) — Gmail's DOM is unversioned and the send-button selector is explicitly locale-dependent; if Gmail's markup doesn't match, the extension degrades silently to "sends untracked" rather than erroring, by design (a tracking failure must never block sending mail), but that also means a selector break wouldn't be obviously visible without someone checking the popup feed. See `browser-extension/README.md` for install steps and the full fragility list. **Needs migration**: `supabase/migrations/add_email_tracking_extension.sql` (not yet applied — sent to user for manual application per this session's no-live-DB-access convention). Deliberately broad `host_permissions: ["https://*/*"]` in `manifest.json`, documented as a hardening TODO once the production GravHub URL is finalized |

**Coverage note (2026-07-14 sweep):** `app/admin/{portal-management,scheduled-emails,sops,audit-log,new-client}` pages weren't independently deep-audited this pass (the sub-agent covering them didn't return in time) — a quick manual spot-check of their backing routes (`portal-clients`, `email/scheduled`, `settings`, `audit-logs`) found them all correctly gated (`requireAdmin`/`requireRole`), but they haven't had the same deep trace-through-the-UI review as the rest of this pass. Worth a dedicated look in a future sweep.

---

## Confirmed working (no action needed)

Verified real and functioning during the 2026-07 audit pass — listed so future runs don't re-flag them:
Google Calendar OAuth/sync/availability/booking emails, public booking widget, delivery-dashboard backend CRUD, sales-enablement playbooks/templates, all six `/reports` pages' underlying math, renewals/maintenance CRUD, Gmail inbox integration, unified inbox, uptime-check engine logic itself (only the trigger cadence is the problem), WordPress security/plugin scanning logic itself, email marketing/broadcasts (full audience filtering, A/B subject tests, real sends), funnel builder + public serving (except conversion attribution), Maverick visitor intelligence, GravIntel event capture pipeline itself (except visit_count/scoring), operations/admin dashboard KPIs, data-audit computations, core automation dispatch + the ~25 action types that ARE reachable from real triggers, the full email-sequence send pipeline (throttling, windows, merge fields, dual-channel send, webhook engagement tracking), ticket CRUD + routing rules, time-entry CRUD + approval workflow, knowledge-base CRUD + portal-scoped visibility.

Plus everything from the prior (2026-07, earlier) pass: social publishing, Resend/email, Mercury, Maverick, HubSpot import, e-signing, global search, the WordPress plugin as an installable artifact, CRM core, forms, proposals, rank tracker, billing/invoices core, portal, reputation management.

Plus, verified during the follow-up pass auditing this session's sequencing/workspace/services-catalog work: A/B variant assignment/reuse and A/B analytics/winner computation, sequence enrollment dedupe against already-enrolled/suppressed/active-elsewhere contacts (logic itself correct, modulo the race in #44), the funnel→contact→sequence bridge's field-type-based email/name resolution, Gmail token expiry buffering and Gmail→Resend send fallback, `rotation_state`'s durable persistence (just not atomic, see #43), Resend webhook signature verification (Svix + HMAC fallback) and bounce/complaint→suppression wiring, `rep-activity`'s RBAC scoping, `computeDealScore`'s math and its consistency between the deals list and detail routes, `DealListView`'s sort logic and asc/desc toggling, selection state and the open deal panel surviving Kanban↔List view switches, the training-progress upsert's `onConflict` key matching a real unique index, and the delivery-dashboard's create-workflow optimistic-then-reconcile logic against the real `mapWorkflow()` response shape.

Plus, verified during a fresh 3-agent pass auditing this session's automation-config/Wait-Resume rebuild (#12/#13) and the #16/#32/#33/#46/#47 recommendations, after the fixes above landed: all 9 `ACTION_CONFIG_ADAPTERS` entries' translated keys are actually read by their corresponding `executeAction` case (no builder-field/adapter-key drift for any of the 9 configurable action types); the `resumeContext`/`context` separation correctly prevents a Wait's own config from leaking into a later action; the atomic `DELETE ... RETURNING` claim in `resumePendingAutomationSteps` is real Postgres-level atomicity, not a mock artifact; `normalizeAction`/`normalizeActionsForStorage` correctly handle both legacy bare-string and `{type,config}` shapes everywhere they're read or written; `SequenceAutomateTab.tsx`'s unchanged bare-string POST shape is correctly normalized server-side; #47's RLS policy logic is sound (`auth.jwt() ->> 'email'` is genuinely populated, no INSERT/UPDATE/DELETE policy gap introduced, the app's own service-role API layer is unaffected by the tightening); #33's geolocation integration is genuinely inert with zero network calls when unconfigured and activates correctly when `IPINFO_API_KEY` is set; #46's `_publicSource` stamping is complete for both genuinely-public trigger paths and correctly excluded from every authenticated/cron/webhook-gated call site; cron's cadence self-throttling is real and independently verified for uptime checks, sequence sends, scheduled social publishing, email-to-ticket conversion, and rank/SEO-report daily gating.

Plus, verified during an 11-agent full-app audit sweep (2026-07-14) covering every module: real, working, no fake data found in — Google Calendar OAuth/token-refresh/sync (old `/book/[slug]` public flow), all 6 real social-platform publish integrations (Facebook/Instagram/LinkedIn/GBP real API calls, correct OAuth, honest partial-success handling, no hardcoded `success: true`), scheduled social-post cron dispatch (same code path as manual publish, not a stub), broadcast sends via Resend (real per-contact sends, real suppression checks, real Svix-verified webhook-driven aggregate open/click/bounce counters), the WordPress plugin's on-page SEO analyzer and metabox save/sanitization, `checkWordPress()`'s real plugin/theme/security scans, rank-tracker's real Google Search Console-backed position data (no `Math.random()`), uptime-check engine logic, all 13 third-party integration OAuth/report flows (Google Marketing suite, HubSpot, Mercury, Maverick, Apollo, Granola — each either genuinely live or honestly inert-until-configured with zero fabricated data), GravIntel's previously-fixed `visit_count`/lead-scoring/geolocation behavior, the portal's dashboard/insights/SEO routes' `requirePortalClient` scoping (re-verified correct, matching variable reused for every subsequent query — no checked-vs-fetched mismatch), and every one of this session's own recent commits (RLS migration internals, portal-client field whitelists, FAB/Enroll-Contacts/contract-duration fixes, the WordPress plugin metabox merge fix) — all independently re-verified correct by a dedicated adversarial regression-review pass, which also caught and got fixed two real gaps in the RLS migration itself (~33 tables, including live OAuth-token leaks on `google_integrations`/`social_connections`, still using a stale or entirely un-migrated blanket-access policy) before they could be run against the real database.

Plus, verified during a fresh 6-agent sweep (2026-07-14, this pass) covering every module again plus a dedicated adversarial review of this session's most recent commits: `lib/timezone.ts`'s DST-boundary math (executed directly against real probes, not just read — CDT/CST/JST and both sides of spring-forward/fall-back all correct); `lib/oauth-state.ts` and all 4 OAuth connect/callback pairs (cookie scoping, `sameSite`/`secure` correctness, fail-closed on invalid state); `lib/broadcasts.ts`'s `resolveEngagementFilters()` applied identically in preview and real-send; the company-delete/merge id-first matching logic itself (only the missing tables and the activities-cascade name-fallback were real gaps, both fixed — see #102); `app/api/crm/merge/route.ts`'s contact-merge FK repoint; portal-client field whitelisting on contracts/proposals/invoices; the automation engine's Wait/Resume, per-action config adapters, and `Rotate Contact Owner` public-trigger restriction; `components/layout/PageLoadingOverlay.tsx`'s timer cleanup and rapid-double-navigation handling (only the `defaultPrevented` capture-phase ordering and `pointer-events-none` were real, currently-latent gaps — see #116); the e-signature flow's dual client/internal signature requirement; `lib/portal-auth.ts`'s staff-bypass-first ownership check; monitored-sites CRUD and WordPress credential handling; Gmail send/token per-user ownership; sales-enablement's optimistic-update revert-on-failure; and Courses' module viewer/progress-tracking/quiz-threshold logic.

Plus, roadmap B4 (bulk operations, 2026-07-15): `BulkActionBar`-driven bulk Export/Delete on contacts/companies/deals were already real and functional prior to this pass (verified via `app/api/crm/bulk-delete`'s per-type table mapping and company-cascade-block logic); added bulk Reassign (owner/`assigned_rep`) to all three list pages and bulk Tag to companies (contacts already had it) — both follow the existing per-record PATCH-loop pattern with optimistic local state and no dedicated bulk-mutate endpoint. Bulk email remains unbuilt (needs a send-path decision, not just UI) and deals have no `tags` column at all (schema decision, not attempted here).

Plus, roadmap B3 (smart lists, 2026-07-15): new `saved_filters` table + `/api/saved-filters` CRUD, gated with the current `staff_all_X`/`is_staff()` RLS convention (not the older blanket-`using(true)` pattern). `SmartListBar` is a reusable component wired into contacts (search+stageFilter), companies (search+statusFilter), and deals (filterRep) — saving stores each page's actual live filter-state keys as `criteria` JSON, and applying just re-runs `setSearch`/`setStageFilter`/etc. against whatever the page already fetched, so lists are "dynamic" by construction rather than a stored/cached result set. **Correction during this pass**: the user's live database rejected the first version of this migration with `function public.is_staff() does not exist` — the two AUDIT #47 RLS-tightening migrations (`tighten_authenticated_rls.sql`/`_part2.sql`) had never actually been applied, meaning the OAuth-token-leak fix on `google_integrations`/`social_connections` was still live-exposed the whole time despite being marked `Fixed` earlier this session. All three migrations were applied by the user in this pass (confirmed) — the RLS gap is now genuinely closed, not just fixed-in-a-file. `add_saved_filters.sql` was also made self-contained (redefines `is_staff()` idempotently) so it no longer silently depends on load order.

Plus, roadmap B2 (custom fields, 2026-07-15): new `custom_field_definitions` table (staff define arbitrary text/number/date/boolean/select fields per entity type) + a `custom_fields jsonb` column on `crm_contacts`/`crm_companies`/`deals`. `/api/custom-field-definitions` CRUD + admin UI at `/admin/custom-fields`. Reusable `CustomFieldsSection` component (row/card variants) wired into both display and edit surfaces for all three entities — `ContactPanel`/`EditContactPanel`, `CompanyPanel`/`EditCompanyPanel`, and `DealPanel`'s inline edit (deals have no separate edit panel). Scope decision: custom fields are edit-only for v1, not exposed on the New Contact/Company/Deal creation panels — fill them in after creating the record. Deleting a field definition doesn't touch already-stored values on records (they just stop rendering); recreating the same field_key brings them back.

Plus, built this pass (2026-07-16, growth report automation): the monthly SEO email pipeline (`lib/seo-report-sender.ts`'s `sendMonthlyClientReports`/`sendSingleReport`, cron-triggered on the 1st of each month) now renders `lib/templates/generate-growth-report.ts` instead of the older `generate-monthly-report.ts` — a sectioned, narrative report (cover, work completed, search visibility, traffic channels, engagement, keyword ranking wins, next month priorities) built from real GSC/GA4/rank-tracker data, with month-over-month deltas pulled from `client_data_snapshots` via new `getPreviousSnapshot()`. Two real gaps found and closed in the same pass: `getGA4TrafficSources` and `getGSCIndexCoverage` already existed but were never wired into `buildClientReport()` — now they are (the latter honestly labeled "pages appearing in Google Search," not "indexed pages," since it's a search-analytics-rows proxy, not real URL Inspection API index-status data). Narrative paragraphs come from `lib/report-narrative.ts`, which grounds an AI prompt strictly in the real numbers already on the report (never invents a figure) and falls back to a deterministic template-built sentence when no AI provider is configured or the call fails/times out — verified both paths never produce an empty section. "Keyword ranking win" cards replace the example report's manually-screenshotted Google search results with real position-history deltas from `tracked_keywords`/`keyword_rank_history` — deliberately not automated screenshot capture (fragile, ToS/bot-detection risk, no infra for it). New `report_work_log` table + `/reports/seo-reports`' "Work Log" modal let staff enter real deliverable bullets (backlinks built, GBP posts, content published) since none of those are tracked anywhere in the app — explicitly NOT auto-fabricated. Output stays a single HTML document (used for both the email send and an in-app "Print / PDF" browser-print preview) rather than a new PPTX dependency. `generate-monthly-report.ts` itself was left untouched since it's still used by the generic delivery-workflow template picker (`app/api/delivery/send-template`, `send-monthly-report`) — only `seo-report-sender.ts`'s usage was swapped.

Plus, built this pass (2026-07-16, roadmap B9): the Unified Inbox (`app/api/inbox/unified/route.ts`) genuinely merged tickets/sequences/broadcasts/CRM-activities into one contact-keyed view, but Gmail — despite a code comment claiming "the page layer merges the Gmail thread list" — was never actually fetched anywhere in `app/inbox/unified/page.tsx` (confirmed via grep, zero matches). Gmail is deliberately never mirrored into Postgres (live API only), so this required a live per-caller fetch: extracted the list+metadata Gmail API logic from `app/api/gmail/messages/route.ts` into a shared `lib/gmail-fetch.ts` (also fixing a regression risk in the extraction — the original route propagated Gmail's real HTTP status, e.g. 401 for an expired token, which `app/inbox/page.tsx` depends on to trigger its reconnect flow; a naive refactor would have flattened every failure to a generic 502 and silently broken that). The unified-inbox route now accepts the caller's own `gmailToken`/`gmailEmail` as query params (sourced from `useAuth()`, which already loads them for the existing Gmail inbox page) and merges up to 20 recent messages per load, resolving each message's counterparty address (whichever side of From/To isn't the caller) and skipping the source entirely — not erroring the whole request — if Gmail isn't connected or the API call fails. Drive-by fix: `inbox/unified`'s GET route had no `requireRole` call at all (relying solely on the outer proxy's authenticated-cookie-presence check, not real role verification) — added, matching every other route's convention.

Plus, fixed this pass (2026-07-16): `/reports/seo-reports`'s Recipients column had a real type/render bug — `client_integrations.seo_report_recipients` is a `text[]` column, but the frontend `Integration` interface typed it as `string | null` and rendered it directly as JSX children (`{integration.seo_report_recipients || 'Primary contact'}`). React renders array children as adjacent text nodes with no separator, so any client with 2+ recipients would have displayed as `emailoneemailtwo` with no space or comma — currently latent (nothing ever wrote to that column, since there was no edit UI), but would have surfaced silently the first time someone used the field. Fixed the type, fixed the render to join with `, `, and added an actual edit affordance (click-to-edit inline text input, comma-separated, saves via the already-existing `PATCH /api/seo-reports` `seoReportRecipients` field — the backend support existed, only the UI was missing).

Plus, fixed this pass (2026-07-16): `/reports/seo-reports` had no way to add a new company to the list at all — the only path that ever creates a `client_integrations` row (`POST /api/client-integrations`, via `ClientIntegrationsPanel`) was only reachable from inside an already-selected CRM company record's own Integrations panel, with no company-search entry point from the SEO Reports page itself. Added an "Add Client" button → `CompanySelect` (existing reusable search/create-new-company picker) → `ClientIntegrationsPanel` (existing GSC/GA4/Ads/Meta/GBP binding form, reused as-is) → list refresh. New row lands with `seo_reports_enabled` at its real DB default (`false`); staff still flips the Auto-Report toggle on the row once bindings are confirmed working via Preview, matching the page's existing per-row workflow. Real, not hidden — confirmed via full-codebase grep that no other insert path or UI entry point for `client_integrations` existed before this.

Plus, built this pass (2026-07-16, roadmap B7 completion): landing pages (`app/funnels`) were previously flagged as "always nested under a funnel rather than standalone, reorder is up/down buttons not drag-drop." Page reorder (funnels list) and block reorder (editor) now use real drag-drop (`@hello-pangea/dnd`, already a dependency, same pattern as the pipeline-stage reorder in `app/crm/pipeline/page.tsx`) instead of swap-based up/down buttons — the new reorder always reindexes every item to its new position (0..n-1) rather than diffing just the two swapped rows, so it self-heals any pre-existing `sort_order` gaps instead of just working around them. Pages are now inline-renamable directly from the funnels list (previously only possible by opening the editor). Single-page funnels — the common case for a plain landing page, not a multi-step campaign — now render as a "Landing Page" with a plain "Page" label and no "Step 1"/pipeline-arrow chrome, instead of looking like an unfinished multi-step funnel. The editor gained a page switcher (funnel's other pages, sorted by the same `sort_order` the API already returns) so editing a multi-page funnel no longer requires returning to the list between pages. Custom domains remain unbuilt — a hosting/DNS decision, out of scope for this pass. No live-browser click-through was possible in this environment (no network path to the live Supabase database from this sandbox, same restriction behind this session's no-live-DB-access convention) — verified via `tsc`/`next build`/`vitest` only, same standard used for every other UI-only change this session.

Plus, verified/built this pass (2026-07-16, roadmap Phase D reality check): a research pass checked all 10 non-mobile Phase D items against actual code, since several earlier-flagged roadmap items (B5/B6/B9) had turned out to already be built. Found 4 stale entries needing no work — D2 (AI lead scoring, `lib/ai/lead-scoring.ts`'s `scoreContact()`, real LLM call + weighted score), D5 (reputation management), D6 (social media scheduler), D8 (knowledge base) — all already fully built and previously confirmed working, just not reflected in the roadmap doc. Closed D9's real gap: the chatbot widget (`public/chatbot.js` → `app/api/chatbots/[id]/chat`) was already a genuine LLM-backed live chat, but conversations lived in their own silo — `app/api/inbox/unified/route.ts` never surfaced them. Added chatbot conversations (only ones with a captured `visitor_email` — anonymous sessions have nothing to key a thread on) as a 6th merged source, read-only by design since chat has no staff-reply path at all (`chatbot_conversations` has no outbound-message mechanism); a `flagged` conversation counts as unread, matching how `ticket.status === 'Open'` is treated. D1 (workflow engine), D3 (custom report builder), D4 (attribution reporting), D10 (public API) confirmed genuinely unbuilt, each 3-6 days of net-new work — not attempted this pass. D7 (client portal 2.0) confirmed already fully built but unreachable by design (AUDIT #154) — surfaced to the user as a product decision rather than guessed at.

Plus, built this pass (2026-07-16, roadmap D1 partial — generic webhook trigger): added a real `Webhook Received` automation trigger, the well-scoped subset of D1 worth building now — explicitly did NOT attempt wait-until-condition (redesigning the hardened Wait/Resume atomic-claim mechanism carries real regression risk for a gain not requested), goal events (too undefined a concept to build against), or custom-code action (arbitrary server-side code execution is a security/sandboxing decision for the user to make, not to build unilaterally). Each `Webhook Received` automation gets a random per-automation token generated client-side in the builder the moment that trigger is selected (not lazily on save, so an already-copied URL never silently changes), stored in `automations.config` (the same column `deal_stage_changed`'s stage filter and `invoice_overdue`'s day-threshold already use) and shown/copied/regenerated from the trigger's config panel. New public `POST /api/automations/webhook/[token]` route looks up the matching automation (application-code filtered, not a jsonb `->>` PostgREST query-string filter — this codebase has never exercised that syntax elsewhere and it can't be verified against a live DB from this sandbox), fires it via the existing `fireAutomations()`/`executeWorkflow()` pipeline with `_publicSource: true` so ownership-changing actions like Rotate Contact Owner still correctly refuse to run from an externally-triggered call (AUDIT #46's protection is generic across trigger types, not form/funnel-specific, confirmed by reading the actual check). An automation with no token configured can never fire regardless of what a caller sends — verified with 3 new unit tests (token matches / token mismatches / neither side has a token) alongside the existing `deal_stage_changed`/`invoice_overdue` scoping-check tests it was modeled on.

Plus, built this pass (2026-07-16, roadmap D4 completion): a follow-up research pass mapped exactly what UTM/source infrastructure existed before building — GravIntel already captured 3 of 5 standard UTM params, but only onto anonymous `gi_visitors` rows with a dead, never-written `crm_contact_id` link column; `deals`/`crm_contacts` had zero source columns at all; and neither public-facing submission surface (the funnel `FormBlock`, the standalone Forms product's `/go/form/[slug]`) ever read the landing URL's query string in the first place, so UTM params were dropped before they ever reached a request body. Built first-touch attribution instead of duplicating GravIntel's separate anonymous-visitor tracking: new `lib/attribution.ts` (`utmFromLocation()` client-side, `extractUtmFromBody()` server-side, shared so both ends agree on field shape/caps) wired into both submission paths; new UTM/landing-URL columns on `crm_contacts`, stamped only when a *new* contact is created (an existing contact's original source is never overwritten by a later touch). Found and fixed a real, load-bearing bug along the way: `lib/automations-engine.ts`'s `Create Deal` action never set `contact_id`/`company_id` on the deal it inserted, even though the trigger context (`form_submitted`) already carries both — meaning every deal auto-created from a form/funnel submission (the single most common real path into this feature) would have been permanently unattributable without this fix, regardless of how good the UTM capture was. New `/api/reports/attribution` (joins deals → contacts via `contact_id`, buckets by source/medium/campaign, real `'Closed Won'`-exact revenue vs. open-pipeline value, capped at 5000 rows rather than Supabase's implicit 1000-row default) and `/reports/attribution` page — contacts/deals with no linked source honestly show in an "Unknown / no source" bucket rather than being hidden or guessed at, and the page explicitly tells staff that pre-existing contacts will never show a source since capture only starts from this deploy forward. **Needs migration**: `supabase/migrations/add_lead_attribution.sql`. Deliberately left GravIntel's `gi_visitors`/`crm_contact_id` linkage unbuilt — that's a different (pre-conversion, anonymous-visit) signal from this feature's (post-conversion, named-contact) one, and connecting them is a real future enhancement, not a blocker for this to already be useful.

Plus, built this pass (2026-07-16, WordPress SEO plugin — user-requested feature set): the user's own punch list assumed a Schema Type selector, FAQ Q&A fields, Organization schema, a redirect manager, and a 404 monitor already existed and just needed data entered or a small enhancement added. A codebase-wide check (both the plugin and the Next.js app) found **none of these existed at all** — confirmed and surfaced to the user before building (they confirmed: build the full thing, not a stub). Built the 4 flagged-high-priority items:
- **Image sitemap**: `class-sitemap.php`'s `render_post_type_sitemap()` now declares the `xmlns:image` namespace and emits `<image:image>` entries per URL (featured image + any literal `<img>` tags in `post_content`, which covers both classic-editor HTML and Gutenberg image blocks) via a new `get_post_images()` helper, capped at 1,000 images/URL per the sitemap image-extension spec.
- **Organization + WebSite + LocalBusiness schema**: new "Organization & Local Business Schema" settings section (`class-admin-page.php`) — name/logo/sameAs for Organization+WebSite, and a checkbox-gated LocalBusiness type/address/phone. `class-meta-manager.php`'s `output_meta_tags()` was restructured so site-wide schema can print on the homepage independent of whether any page-specific meta exists (it previously bailed out entirely when there was nothing page-specific to say). Each piece is genuinely inert — prints nothing — until its own real minimum fields are filled in; nothing fabricated. LocalBusiness is opt-in (checkbox) since not every WordPress site the plugin runs on has an addressable local presence worth marking up.
- **Schema Type selector (Article/Service/FAQ) + visible FAQ accordion**: new "Schema" tab in the per-post metabox (`class-seo-metabox.php`) with a type dropdown and, for FAQ, a Q&A repeater (add/remove rows, JS-serialized to a hidden JSON field on form submit, re-validated server-side in `save_metabox()`). `class-meta-manager.php` builds real Article/Service/FAQPage JSON-LD from these fields (`build_typed_schema()`), taking priority over the older freeform API-sourced `schema_markup` when both exist. New `[gravhub_faq]` shortcode (`class-shortcodes.php`) renders the exact same `_gravhub_faq_items` post meta as a visible accordion — schema and visible content read from one field, so they can't drift out of sync the way a hand-typed schema blob with no matching UI would (the user's own stated concern, since Google increasingly wants the two to match 1:1).
- **Redirect manager + 404 monitor + fuzzy-match suggestions**: new `class-redirect-manager.php`, fully self-contained (own DB tables via `dbDelta`, own `template_redirect` hooks, own REST routes, own "Redirects" submenu page). Two new tables (`{prefix}gravhub_redirects`, `{prefix}gravhub_404_log`) created on activation and self-healed on `admin_init` the same way capabilities/cron already self-heal in this plugin (version-gated so `dbDelta` doesn't run on every request). A redirect match always wins over a 404 (checked at `template_redirect` priority 1, before WordPress's own 404 determination fully matters); an actual 404 is logged with hit-count/last-seen once the redirect check has passed. Suggestions use `similar_text()` slug-similarity against the 300 most recently modified published posts/pages — filtered in application code, not a jsonb/SQL fuzzy-match extension, kept fast enough for an on-demand click.

Verification is PHP-syntax-only (`php -l` clean on every file, both new and modified) — this environment has no live WordPress install to actually load the plugin against, so table creation, REST route behavior, and frontend rendering (accordion, redirects, sitemap image tags) are unverified beyond that. **Requires re-uploading the plugin to the client's WordPress site** to take effect (matches AUDIT #81's precedent — this repo has no build/zip step, the source PHP is the distributable) and bumps the plugin to v1.3.0. The 3 items the user correctly flagged as pure data entry (focus keywords, Schema Type per page, Organization `sameAs` links) still need the user's own input — nothing in this pass touches live site content. 6 lower-priority items from the user's original list (bulk SEO editor, live readability feedback, redirect CSV import/export, internal-linking suggestions, hreflang, outbound broken-link scanning) were not attempted this pass — explicitly deferred, not silently dropped.

Plus, verified during this pass (2026-07-15, ~90-finding sweep, 8 finder agents + 1 adversarial regression agent covering the Forms submissions UI, Review Campaigns dispatch, Rank Tracker Scheduled Reports dispatch, email-deliverability DNS checks, and the PageLoadingOverlay fix): `lib/rbac.ts`/`lib/admin-auth.ts`/`lib/portal-auth.ts`'s auth-resolution paths are the only 5 places `team_members.status` needed checking (RLS policies, cron, and every other role check already route through one of these); the atomic-claim pattern (conditional `UPDATE ... WHERE <matches-current-state> RETURNING`) is correctly applied and race-free in all 6 of this pass's new/extended call sites (`spawnRecurringTasks`, `sendDueScheduledReports`, the review-request completion route, `tickets/from-email`'s dual-key claim, and both `adjust_sequence_counts()` new call sites); `SequenceAutomateTab.tsx`'s bare-string automation actions are unaffected by the sequence-count RPC's new `p_completed_delta` param (default-backward-compatible); the WordPress plugin, social publishing, e-signing, booking, Mercury, Maverick, and HubSpot integrations were untouched this pass and not re-verified (no code in those paths changed).

Plus, verified during this pass (2026-07-15, second sweep same day — 5 module finder agents + 1 adversarial regression agent covering every module again, plus a dedicated audit-log survey for roadmap A5 pagination): `app/api/ai/{generate,insights,recommendations,audit,analyze-website}` and the AI chat tool-calling loop's 20 CRM tools (correctly gated, honest template-fallback labeling, real schema-matching writes); `lib/automations-engine.ts`'s 25+ action implementations and all other 8 real trigger types (Manual Trigger, #143, was the one dead outlier); `app/social/page.tsx`'s composer and `social-posts/[id]`'s field whitelist; `app/api/sequences/rep-activity`'s RBAC scoping; every `ads`/`ga4`/`gsc`/`gbp` report/property route (correctly gated — the 5 gaps found, #144, were all Meta/LinkedIn/Google-Marketing specific); rank-tracker's real GSC-backed position data; Mercury, GBP review sync, and the chatbot's real LLM call + rate limiting; `app/api/proposals`/`proposals/[id]`, `app/api/contracts/[id]`/`addendums` (aside from #168), `app/api/renewals`/`renewals/[id]` — status-transition graphs, portal restrictions, and CRUD all sound; `lib/crm-cascade.ts`/`crm/merge`'s id-first/name-fallback matching logic across all 11 related tables; `app/api/crm/enrich`'s SSRF protections; `crm/import`/`duplicates` HubSpot-aware mapping; the portal's `/dashboard`/`/insights`/`/seo` routes and every portal-scoped API (`contracts`, `invoices`, `projects`, `files`, `tickets`, `social-posts`) — all correctly `requirePortalClient`-scoped and consistent with what the real live `/client` page actually calls; `lib/portal-auth.ts`'s `requirePortalClient`/`isStaffCaller`; `app/api/search`'s query logic itself (auth and ilike queries across all 8 tables were already correct — only the result hrefs, #145, were wrong); `app/api/cron/route.ts`'s other jobs (`dispatchScheduledReviewCampaigns`, `spawnRecurringTasks`, `resumePendingAutomationSteps`, `rankCheckDue`, `runUptimeChecks` — all still correctly atomic; #156-158 are jobs this pass found were never made atomic in the first place, not regressions); `app/api/team-members/**` role gating; `app/api/auth/google-verify`/`session`/`approve-setup`; `app/api/tasks` department-visibility scoping; `app/admin/sops`'s persistence round-trip; `app/operations/page.tsx`'s KPI fetches. This pass's fixes (AUDIT #140-151) were also independently re-verified clean by the adversarial agent: the suspended-staff status checks correctly gate all 5 originally-fixed paths with no fail-open risk (confirmed against the real `status` CHECK constraint and every insert path); ~14 spot-checked routes from the missing-auth batches matched their real caller's needs with no over/under-tightening; the CRM cascade `company_files` handling, the sequence-count RPC rollout (all 5 remaining call sites, no stale-variable reads, the new `p_completed_delta` param is backward-compatible with both pre-existing automations-engine.ts call sites), the cron month-overflow clamp (verified against Dec 31, Jan 31 non-leap, Jan 31 leap-year, and Mar 31 edge cases), the new-client wizard's exactly-one-of-two-routes branching, and the chatbot public route's field allowlist were all confirmed correct with no regressions introduced.

Plus, built this pass (2026-07-17, WordPress SEO plugin dashboard redesign): the user supplied 12 reference screenshots (AIOSEO, Rank Math, SEOPress, MonsterInsights, miniOrange) and asked for the plugin's main dashboard rebuilt to match that caliber. Two new Next.js endpoints follow the existing `app/api/wordpress/seo/*` pattern exactly (`requireWordPressAuth`, `?site=` → `wordpress_site_health.company_id` → `client_integrations`): `analytics/route.ts` pulls real GA4 (`getGA4Report`/`getGA4TrafficSources`/`getGA4TopPages`) and GSC (`getGSCSummary`) data, each source independently optional so a GA4-only or GSC-only client still gets an honest partial payload instead of an error; `keywords/route.ts` computes position-bucket counts (Top 3/4-10/11-20/21-50/50+) and winning/losing keyword deltas from `tracked_keywords`, ported from the same bucket logic `app/rank-tracker/page.tsx` already uses client-side. Both return `{connected: false}`/`{configured: false}` rather than zeros when a site isn't linked or isn't integrated — no fabricated numbers. **Deliberately out of scope**: Device/Age/Gender traffic breakdown (MonsterInsights-style) — confirmed via full-repo grep that zero GA4 demographic/device-dimension calls exist anywhere in this codebase; faking these would violate this project's no-fabricated-data rule, so the dashboard simply omits them rather than showing placeholder numbers. Bonus fix bundled separately: `keyword_rank_history` was missing `clicks`/`impressions` columns that `sync-gsc/route.ts` had been silently failing to write since that route was built (`add_keyword_rank_history_columns.sql`, not yet applied — sent to the user). On the plugin side: `class-api-client.php` gained `get_dashboard_analytics()`/`get_dashboard_keywords()` (15-minute transient cache, same request-boilerplate pattern as the 3 existing API methods); `class-health-reporter.php`'s `send_report()` now caches its full result to `gravhub_last_health_data` and a new `get_cheap_health_signals()` method (no live HTTP calls) exposes just the plugin/theme-update flags — both added specifically so the new dashboard's Notifications feed never has to call the 4-live-HTTP-request `collect_health_data()` synchronously on page load; `class-redirect-manager.php` gained `get_redirect_count()`/`get_404_count()` (plain `COUNT(*)`, cheap). `class-admin-page.php`'s constructor now threads `$health_reporter`/`$redirect_manager`; `render_page()` builds a `$notifications` array from real sources only (analysis issue count, cheap update signals, the last cached security-check result, live 404 count) and a shared `$modules` catalog (the original 5 toggleable modules plus 4 new purely-navigational cards — Redirections, 404 Monitor, Schema Markup, Local SEO — linking to their real pages, since none of them has a natural single on/off state). `settings-page.php` gained a Notifications feed card, the extended Modules grid, and new Site Analytics / Keywords sections that fetch `/dashboard-analytics`/`/dashboard-keywords` client-side via two new REST routes (`gravhub-seo.php`'s `rest_dashboard_analytics`/`rest_dashboard_keywords`) rather than blocking `render_page()` — those routes proxy cross-origin calls to the GravHub app that can take up to 30s on a cache miss. All rendering for the fetched sections builds DOM nodes via `textContent` (not `innerHTML` string interpolation) to avoid any XSS surface from keyword/page-title text. **Verification**: `php -l` clean on all 6 touched/new PHP files; `tsc --noEmit`, `next build`, and the full `vitest` suite (156 tests) all pass. No live WordPress install or Supabase access in this environment — needs a real re-upload of the plugin (bumped to v1.3.1) and a spot-check on a site with a GravHub-linked company + configured GA4/GSC to visually verify the new sections end-to-end, matching every prior WP-plugin pass this session.

Plus, built this pass (2026-07-18, WordPress SEO plugin — the 6 originally-deferred items from #242's punch list): redirect CSV import/export, hreflang support, a weekly outbound broken-link scanner, internal-link suggestions, live readability feedback, and a bulk inline-editable Meta tab in the GravHub app — see the individual git commits for detail (`e7cd947`, `bb0fd38`). Bumped the plugin through 1.4.0 → 1.5.0. Also fixed in the same stretch, live on gravissmarketing.com: a third-party script (WPCode/Maverick Intelligence) mangling the sidebar's Redirects link when scoped to also run in wp-admin (defensive re-assertion guard, since the actual snippet can't be fixed from this repo); a genuine WordPress core quirk where the top-level "GravHub SEO" menu opened Redirects instead of the dashboard (the only-ever-registered submenu, when it doesn't share the parent's slug, silently becomes the parent's click target); the `manage_gravhub_seo` capability not being held by an admin whose role wasn't literally named `administrator` (added a `manage_options`-sufficient `map_meta_cap` fallback); and `rest_run_analysis()`/the Sync button reporting `{success: true}` (a green "Analysis complete!" toast) even when the actual push to GravHub failed — the real `{sent: false}` signal existed in the response but the frontend JS never checked it.

Plus, closed out this pass (2026-07-20, systematically working through the prior audit sweep's Open + Needs-Decision backlog, #176-193): fixed 16 of the 18 remaining findings, following a consistent triage rule — fix mechanically-clear bugs outright; for genuine product/schema "Needs Decision" items, fix them anyway if a safe, reversible, non-speculative fix exists regardless of the underlying open question (#180's contract creation, #184's key-mismatch — safe independent of #154's separate `/client` vs `/portal/**` reachability question, which remains open); leave truly open-ended ones alone rather than guessing. **Audit log** (#176/#177/#178/#179): real cursor pagination replacing the flat `limit=5000` cap; fixed fake `'system'`/`'admin'` attribution across 62 of ~63 real call sites (one, the pre-session magic-link route, correctly left as-is); added real `logAudit()` calls to the previously-silent team-member create/suspend/reinstate/delete/promote paths — the single most security-sensitive action class in the app now actually leaves a trail; distinct error state instead of a false "all clear" on fetch failure. **New-client wizard** (#180-183): Agreement fields now create a real `contracts` row when filled in (Contract ID itself still has nowhere honest to go without a schema change — left unpersisted, called out inline); portal-vs-catalog service taxonomy mismatch fixed via a new mapping table; `res.ok` checks added on the two silently-ignored POSTs; Account Manager now also lands on the company record, not just the contact. **Portal management** (#184/#186/#188 fixed; #185/#187/#189 left open/needs-decision by design): the Visibility-toggle and Welcome Message key-mismatch between admin writer and client-facing readers fixed; invite-email send failures now surface instead of a blanket success toast; newly-added companies' selected service now actually populates `services_config`, not just the legacy text column. Two integration tests' `@/lib/rbac` mocks needed a `getAuthUser` addition after the attribution fix (`audits-delete.test.ts`, `contracts.test.ts`) — caught by a full `vitest run`, not assumed clean. **WordPress plugin** (#190-193, all fixed): the broken-link scanner switched from `wp_remote_*` to `wp_safe_remote_*` (WP core's SSRF-safe variants — rejects loopback/private/link-local targets, including the cloud metadata address, instead of a hand-rolled blocklist); its insert now checks and logs failure instead of discarding it; the CSV redirect importer gained a 5,000-row cap with user-visible feedback when hit; `get_broken_link_count()` wired into the dashboard notification feed alongside its two siblings. Bumped the plugin to 1.5.2 and rebuilt `public/downloads/gravhub-seo.zip` via `scripts/build-plugin-zip.js` — the previous zip on disk turned out to be stale, missing the redirect manager/broken-link scanner/internal-linking/shortcodes files from the 2026-07-18 pass entirely. **Genuinely left open** (2 items, both real scope/regression-risk decisions rather than oversights): #185 (Strategy Scheduling panel has no consumer anywhere to reconcile against — same underlying `/portal/**` reachability question as #184, plus its own "was a consumer ever intended" question); #187 (portal-client company grouping by free-text name match, not `company_id` — fixing it is a real relational refactor with data-integrity risk for existing null-`company_id` rows, not a same-day fix); #189 (concurrent-autosave race + unsaved-changes gaps on several portal-management fields — low practical impact, single-admin editing is the realistic case, noted for completeness). **Verification**: `tsc --noEmit`, `next build`, `php -l` on all 5 touched PHP files, and the full `vitest` suite (156 tests, including the 2 mock fixes above) all pass clean.

Plus, ran a full `/audit` sweep this pass (2026-07-18, 3 parallel research agents — one deep-tracing the 5 admin pages AUDIT.md's own coverage note flagged as never independently reviewed, one adversarially reviewing this session's own newest WordPress-plugin commits, one diffing every `app/api/**/route.ts` and `app/**/page.tsx` against every path AUDIT.md has ever mentioned to find genuine zero-coverage gaps) plus direct fixes for everything well-scoped enough to fix immediately: **34 findings total** (#169-193 above, plus #160-167 finally closed out after sitting `Open` since 2026-07-17). Two were live, unauthenticated real-email-relay routes (#169/#170, same severity class as the CRITICAL #82) found by the coverage-gap sweep specifically because no prior pass had ever named either file — a reminder that "audited the whole app" passes built from a fixed module list will keep missing whatever wasn't on that list; the coverage-gap methodology (diff the real route tree against every mention in this file, not against a hand-maintained checklist) is worth repeating periodically rather than one-off. Deleted two genuinely dead routes in the same pass (`sequences/attribution`, `portal-clients/reset-password`) rather than patching bugs nothing calls. 7 new findings landed as `Needs Decision` (#180, #184, #185, #190, plus #168 already there) — three of them (#184/#185) turned out to be moot in production right now because they're entangled with the still-open #154 product-direction question (`/client` vs `/portal/**`), so fixing their symptom would be wasted work until that's resolved; flagged rather than guessed at, same as #155 already was. The remaining ~15 findings are genuine `Open` bugs not fixed this pass, ranging from HIGH (audit-log pagination silently truncating past 5,000 rows despite a prior pass's note claiming it was already covered; the audit trail's own "who did this" field being fabricated `'system'`/`'admin'` for nearly the entire log; privilege-escalation actions not being logged at all) to cosmetic LOW items — each has its own row with what a fix would need. **Verification**: `tsc --noEmit`, `next build`, and the full `vitest` suite (156 tests) all pass on every direct fix in this pass; WordPress-side fixes are `php -l` clean only, same live-install caveat as every prior WP-plugin pass this session.

Plus, ran a full clean-sweep `/audit` this pass (2026-07-20, at the user's explicit request to re-baseline the whole app before a fix→merge→verify→re-audit cycle, with hardcoded/mock/fabricated-data hunting called out as this pass's specific top priority): **7 parallel research agents** — one per functional area (CRM/Sales/Finance, Portal/Client-facing, Marketing/Growth/Automation, Admin/Ops/Infra, WordPress plugin, an adversarial review of this session's own most recent commits, plus a gap-fill agent for admin-core/settings/auth once the Admin/Ops agent's own sub-delegation left that scope uncovered) — several of which further split into their own sub-agents per module (operations/workspace, monitoring, tasks/time-tracking, projects, calendar/bookings, inbox/search/notifications/cron/drive/storage all reported independently). This is a **pure audit pass — no fixes were made** (per the user's explicit "audit → I'll ask for fixes as a separate step → merge → checklist → feedback → re-audit" process); every finding below is `Open`/`Needs Decision`, not `Fixed`. **80 new findings** (#194-273): 2 CRITICAL, 16 HIGH, ~35 MEDIUM, ~27 LOW. The two CRITICAL findings are both live, unauthenticated account-takeover paths that had escaped every prior pass: #194 (an unsigned OAuth `state` param lets anyone hijack any staff member's Google Calendar integration with zero GravHub access) and #195 (the portal-client "Pending Approval" gate the admin dashboard implies is enforced is never actually checked by any of the 3 real session-issuing paths — a client can self-approve by simply logging in). On the specific hardcoded/mock-data priority: no agent found `Math.random()`-faked stats, static demo arrays rendered as live data, or fabricated third-party-API success anywhere in the app — the codebase's real, longstanding "no fabricated data" discipline held up under a dedicated hunt. What the priority DID surface is a different, more common pattern this pass specifically named and tracked: **config collected in a form but never correctly read back** (a writer/reader field-name mismatch, silently discarding real user input while looking saved) — found fresh in 3 places (#211's fake proposal-approver attribution, #220's Rotating Messages `message`/`sub` mismatch, #232's SEO Strategy string-vs-object mismatch) on top of the 2 already-tracked instances of the same class (#184, #12). The other dominant pattern, spanning ~20 of the new findings, is the established pagination-truncation bug class (#48/#151) recurring on 15+ pages/features never previously swept for it (the entire Reports module, the "System Backup" feature explicitly promising a "full" export, and most of Contracts/Renewals/Maintenance/Billing/Marketing) — worth a dedicated systemic pass (audit every `fetch(` call site against every cursor-paginated route, once) rather than continuing to find it module-by-module. Full triage/fix prioritization deferred to the next phase of this cycle, per the established process.

Plus, fixed this pass (2026-07-20, all 15 HIGH-severity findings from the clean-sweep audit above, #196-210): at the user's explicit direction to work through severities top-down after the two CRITICAL fixes. 14 of 15 fixed directly; #207 (Security Settings panel) left `Needs Decision` since it's 6 real security features' worth of backend work, not a code fix — surfaced rather than built unilaterally. Highlights: the new-flow booking system now re-checks Google/imported-event conflicts at write time (#196), not just in the slot picker; recurring tasks stop losing their department/project scoping on every respawn (#197); the portal-client account-setup codes now share the same rate limit as the staff equivalent (#198); `/client` — the one portal surface real clients can actually reach — gained a working ticket-reply thread and a "Pay Now" button, closing two real capability gaps on top-used, already-built backend features (#200/#201, the latter also fixing the post-payment redirect target, which pointed at an unreachable page per #154); the 3-way ticket-message shape collision that crashed the staff viewer on any real client ticket is resolved (#199); `access_schedule` is now actually enforced server-side in both identity-resolution paths, closing the same class of gap the CRITICAL #121 fix addressed for a different field (#208); and a real client-site WordPress credential moved from plaintext to the same encryption every other stored credential in the app already uses (#210). The Reports module and the admin "System Backup" feature both got the same `fetchAllPages()` fix already proven elsewhere for the pagination-truncation bug class (#206/#209). **Verification**: `tsc --noEmit`, `next build`, and the full `vitest` suite (156 tests) all pass clean on every fix in this pass.

Plus, worked through the entire remaining Open backlog this pass (2026-07-21, at the user's explicit "work through the remaining Open findings" direction): 56 findings closed (#189, #211-272 except the findings noted below as deliberately left open), spanning MEDIUM through LOW severity, plus two findings from the prior pass's HIGH batch (#273/#274) that had already been fixed directly from the user's own live bug report before this systematic sweep began. Notable fixes beyond the mechanical bulk: `app/sales-enablement/page.tsx`'s Playbooks/Templates turned out to be a worse bug than #212 described — they were reading `json.data` off routes that return a bare array, so the lists were always empty, not just truncated past 200. `lib/fetch-all-pages.ts`'s `fetchAllPages()` (#244) now actually throws on a failed page instead of silently `break`ing — every call site across the app already had error handling written for this, it just never fired; a dedicated background audit verified all ~30 call sites and found (and fixed) the 2 that genuinely lacked it (`app/crm/pipeline/page.tsx`'s HubSpot-import refresh, `app/billing/page.tsx`'s CSV-import refresh). `lib/automations-engine.ts`'s status-casing bug (#216) turned out to have a third occurrence beyond the two described (round-robin rep assignment), fixed alongside the other two. Real infrastructure added where a fix genuinely required it rather than being deferred: a `notification_reads` table so "mark as read" persists instead of resetting on every navigation (#217); an `lib/ssrf-guard.ts` private/loopback/link-local denylist wired into monitored-site creation (#260); atomic increment RPCs for the email-tracking-extension counters (#247); a real WordPress-core-update check against WordPress.org's own version-check API, replacing a field that was always `false` (#258); and a per-company save queue plus `beforeunload` warning for `app/admin/portal-management/page.tsx`'s autosave race and unsaved-local-field gaps (#189). Found and fixed one additional live bug outside the numbered findings list, reported directly by the user mid-pass: the WordPress plugin's sitemap on/off toggle (`gravhub-seo.php`'s `rest_save_option()`) updated the option but never called `flush_rewrite_rules()`, so `/sitemap.xml` kept 404ing even after re-enabling it until something unrelated happened to flush WordPress's rewrite cache — fixed by flushing on that specific option's save. **Deliberately left open, not overlooked** (each already carries its own reasoning in its row): #187 (portal-client company grouping by name, not `company_id` — a real relational refactor); #224/#234 (`Needs Decision` — real product/schema decisions, not code fixes); #231 (new-flow booking iCal feeds need a per-staff ownership column that doesn't exist yet); #238 (`Related to #187` — same underlying fix); #240 (entangled with the still-open #154 portal-reachability question); #245 (WordPress Modules grid — cosmetic-vs-functional decision on 5 toggles); #262 (certificate generation is real net-new feature work, not a bug fix). **Verification**: `tsc --noEmit`, full-repo `eslint` (0 errors), `next build`, the full `vitest` suite (158 tests, including 2 new attribution-assertion tests for #272 and one test file updated for #215's casing fix), and `php -l` on all 4 touched WordPress PHP files all pass clean.

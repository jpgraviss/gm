// Next.js requires this file to be named middleware.ts at the project root.
// The implementation lives in proxy.ts — see that file for details.
export { proxy as middleware, config } from './proxy'
